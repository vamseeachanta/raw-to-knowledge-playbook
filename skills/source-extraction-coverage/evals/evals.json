{
  "skill": "source-extraction-coverage",
  "note": "Acceptance scenarios (Anthropic skill-creator convention; no standard runner yet). Adapt {{PLACEHOLDERS}} to your corpus.",
  "evals": [
    {
      "name": "estimate-then-yield-happy",
      "prompt": "Run /extract-source on a born-digital text PDF after declaring an extraction_estimate.",
      "expect": [
        "extraction_estimate is written to frontmatter BEFORE extraction",
        "extraction is deterministic (pdfplumber/Docling), not an LLM bulk pass",
        "extraction_yield recorded after; yield ~ matches estimate, source not flagged shallow"
      ]
    },
    {
      "name": "shallow-extraction-caught",
      "prompt": "Run /extract-source on an image-only PDF routed (wrongly) through the text recipe.",
      "expect": [
        "extraction_yield << extraction_estimate (the 2%-coverage signature)",
        "source marked extraction: shallow and queued for the scanned/OCR recipe",
        "it does NOT pass as 'done' into the trusted set"
      ]
    },
    {
      "name": "classify-by-structure-not-name",
      "prompt": "Run /extract-source --type auto on a workbook named 'summary.xlsx' that is actually formula-dense.",
      "expect": [
        "type/classification is decided on scanned structure (formula density), not the filename (incident D4)",
        "routes to the xlsx formula-graph recipe, not a data-only read"
      ]
    },
    {
      "id": "ace-61-success-denominator",
      "issue": 61,
      "name": "ace-success-denominator-excludes-hard-exclusions",
      "prompt": "Run /extract-source on an ACE bounded sample with hard exclusions and successful routed items.",
      "expect": [
        "uses eligible_candidate_items as the success denominator",
        "reports hard_excluded_items separately as % excluded"
      ]
    },
    {
      "id": "ace-52-kept-text-estimate-yield",
      "issue": 52,
      "name": "ace-kept-text-config-requires-estimate-yield",
      "prompt": "Run /extract-source on ACE wave-1 hand-authored text and small config JSON rows.",
      "expect": [
        "records extraction_estimate before extraction",
        "records extraction_yield after extraction",
        "kept text/config/code-doc rows fail validation if either field is missing"
      ]
    },
    {
      "id": "ace-52-generated-json-not-shallow",
      "issue": 52,
      "name": "ace-generated-json-is-exclusion-not-shallow-extract",
      "prompt": "Run /extract-source on repetitive generated JSON and lockfile-like JSON.",
      "expect": [
        "routes generated JSON excluded_no_ingest before extraction",
        "does not treat generated/noise exclusions as shallow extraction failures"
      ]
    }
  ]
}
