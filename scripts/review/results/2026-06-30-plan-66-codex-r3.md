## Verdict
MAJOR

## Retrieval
- Read `docs/plans/2026-06-30-issue-66-ace-public-token-fixtures-private-field-placeholders.md` lines 1-355.
- Read `artifacts/ace-wave0-ledger-schema.json` lines 1-328, especially private/source-like terms and #66 split row.
- Read `scripts/validate_ace_epic_wave_coordination.py` lines 106-128, 432-459, and 715-749.
- Read `scripts/validate_ace_wave0_schema_contract.py` lines 47-57, 80-149, 205-235, and 248-268.
- Read `.github/workflows/validate.yml` lines 1-55.
- Read `docs/plans/README.md` lines 1-57 and `docs/plans/ace-share-ingestion-wave-coordination.md` lines 1-90.
- Read `tests/test_validate_ace_epic_wave_coordination.py` lines 877-956 and `tests/test_validate_ace_wave0_schema_contract.py` lines 1-80, 159-211, 248-280.
- Read `docs/plans/2026-06-29-issue-63-ace-public-output-redaction-and-identifier-canary.md` lines 88-184.
- Checked issue #66 with `gh issue view 66 --repo vamseeachanta/raw-to-knowledge-playbook --json number,state,title,labels,url`.
- Ran public scan for the plan plus retained #66 review artifacts with `UV_CACHE_DIR=.claude/state/uv-cache uv run python scripts/validate_ace_epic_wave_coordination.py ...`; result: pass.
- Ran `UV_CACHE_DIR=.claude/state/uv-cache uv run python scripts/validate_ace_wave0_schema_contract.py`; result: pass.
- Ran `UV_CACHE_DIR=.claude/state/uv-cache uv run python -m unittest tests.test_validate_ace_epic_wave_coordination tests.test_validate_ace_wave0_schema_contract`; result: 97 tests OK.
- Checked retained #66 review artifact names/sizes and absence of `*plan-66*.err` sidecars.

## Findings
1. Plan omits `.planning/plan-approved/66.md` from the #66 public-scan path set even though the marker is a required public gate artifact. `docs/plans/2026-06-30-issue-66-ace-public-token-fixtures-private-field-placeholders.md:289` requires `.planning/plan-approved/66.md`, but the scan list at `:188-190` and test expectation at `:279` list plan, contract, scripts, tests, fixtures, workflow, README, coordination, and review artifacts only. Existing CI scans analogous approval markers for prior gates at `.github/workflows/validate.yml:35-38`. `scripts/validate_ace_epic_wave_coordination.py:432-459` validates approval-marker fields and artifact paths, but leak detection only happens when paths are passed to `validate_public_artifact_paths` at `:715-749`. A #66 approval marker could therefore pass marker validation while never being scanned for private/public-surface leakage.

2. Plan does not give #65 `source_like_raw_digest_terms` the same closed-schema import/test treatment as `private_source_field_terms`. The schema defines `source_like_raw_digest_terms` as `source_hash` and `provenance_pointer` at `artifacts/ace-wave0-ledger-schema.json:122-124`, and the #65 validator treats those as closed schema terms at `scripts/validate_ace_wave0_schema_contract.py:205-226`. The #66 plan imports only public token field name and private source field terms at `docs/plans/2026-06-30-issue-66-ace-public-token-fixtures-private-field-placeholders.md:291`. It says the generator rejects “raw provenance inputs” at `:152` and `:294`, but the TDD rows at `:254`, `:256`, and `:271` cover source/path/hash/key cases and do not require a `provenance_pointer` or full `source_like_raw_digest_terms` case. Under mandatory TDD, that leaves a privacy-sensitive schema term untested.

## Blockers
- Finding 1: add `.planning/plan-approved/66.md` to the planned public-scan path list and CI scan when present.
- Finding 2: import and test the full #65 `source_like_raw_digest_terms` set, including `provenance_pointer`, before implementation.
