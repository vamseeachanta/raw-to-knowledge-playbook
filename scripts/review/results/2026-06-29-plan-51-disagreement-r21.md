review_artifact_role: public_history
normalization_note: denied-token literal substitutions only; provider verdict unchanged

# Disagreement report - plan #51 (2026-06-29)

## Verdicts

| Provider | Verdict |
|---|---|
| claude | MAJOR |
| codex | MAJOR |
| gemini | UNAVAILABLE (gemini CLI failed, rc=41: Error authenticating: FatalAuthenticationError: Manual authorization is required but the current session is non-interactive. Please run the Gemini CLI in an interactive terminal to log in, provide a GEMINI_API_KEY, or ensure Application Default Credentials are configured.     at initOauthClient (<LOCAL_FILE_URI>)) |

## Findings unique to each provider

A finding is 'unique to X' if its text appears in X's artifact but not
verbatim in any other provider's artifact.

### claude

- **Self-scan target set omits the public docs the plan itself edits - the firewall doesn't cover its own blast radius.** The Files-to-Change table modifies published MkDocs surfaces: `docs/07-data-governance.md` (line 506), `docs/19-trust-boundary-and-private-mode.md` (508), `docs/plans/2026-06-29-issue-50-...epic.md` (509), and audits/conditionally modifies `docs/04-failure-modes.md`, `docs/05-good-practices.md`, `docs/18-security-and-pii.md` (504/505/507). But the self-scan target enumerations - Pseudocode "scan #51 public surfaces" (lines 302-313), "#51 generic self-scan boundary" (404-415), and Acceptance criterion (604) - list only the plan, #63 plan, README, coordination doc, artifacts/, scripts/, config/, good fixtures, touched skills, and review artifacts. None of `docs/07`, `docs/19`, `docs/04/05/18`, or the `issue-50` parent plan appear in any self-scan list. The hash-policy sweep touches these files but only classifies `sha256` hits - it is not a generic-leak scan. So a generic leak (raw path, identifier, private value) introduced while "reconciling" `docs/07`/`docs/19` would pass #51's own firewall. For a plan whose entire purpose is a public-surface firewall (line 328: "docs/plans/ is already a public methodology surface under MkDocs and must be treated as published"), editing published docs that the gate never re-scans is a correctness gap, not cosmetic. **Blocker.**
- **The manifest-gate reconciliation relies on the exact prose-parsing anti-pattern the plan forbids elsewhere.** `test_manifest_gate_boolean_matches_coordination_text` (line 576) and `test_manifest_snapshot_gate_matrix_covers_all_ingestion_waves` (558) validate the #51 `wave_class`/`requires_manifest_snapshot_id` boolean matrix against `ace-share-ingestion-wave-coordination.md`. But that coordination doc contains **zero** occurrences of `wave_class` or `requires_manifest_snapshot_id` (verified: `grep -c` -> 0) - it encodes the gate only as free English ("not applicable for own sampling; #62 gate applies to downstream sampling" / "...snapshot_id before sampling"). So the validator must regex-match prose to reconcile booleans against text. The plan explicitly condemns this for #63 - line 228: "#63 consumes this config rather than being parsed by #51 CI through brittle plan-prose matching" - yet adopts it for the manifest matrix. Internal inconsistency; brittle. Impact is bounded because line 268-269 makes the #51 matrix authoritative on disagreement, so I rate it MAJOR-leaning-MINOR rather than a hard blocker, but the test as specified is fragile.
- **Success-formula terminology diverges from the authoritative tracker.** The plan says "% ingested success formula" (lines 241, 615; `grep` -> 2 hits) and introduces `computed_sample` as the #52-#60 denominator source (258, 575, 615). The coordination doc - and every downstream wave row it owns - uses `success = successful_routed_items / eligible_candidate_items * 100` (`grep` -> 10 hits) and never the term `computed_sample`; the plan never uses `successful_routed_items` (`grep` -> 0). These likely denote the same percentage, but the plan's downstream-binding test (`test_every_downstream_wave_has_issue_skill_and_validator`, 577) and the coordination tracker speak different vocabularies for the same field, inviting drift the plan is otherwise obsessive about preventing. MINOR.
- **Acceptance criteria are compound, not atomic - they can't be checked off as written.** Single `[ ]` items bundle many independently-verifiable claims: e.g. line 592 packs ~10 distinct requirements (marker expansion, no-source-input signature, `secrets.token_hex(16)`, grammar/uniqueness/method validation, hand-authored-token rejection, four named modules not defining token helpers, no static-randomness claim, #63 ownership) into one checkbox; lines 596 and 604 are similarly overloaded. A reviewer cannot mark such an item "done" without it silently hiding an unmet sub-clause. This is a reviewability defect for a T3 acceptance gate. MINOR.
- **The plan's own defined path to `status:plan-review` is unreachable without an out-of-band user action it cannot self-supply, and 20 rounds confirm it.** Every round records `Gemini UNAVAILABLE ... rc=41` (lines 648-699). The plan states (462-465) that "Gemini noninteractive auth/config failure is not a quota outage and does not automatically authorize downgrade; status:plan-review is blocked until Gemini is restored or the user explicitly approves a one-round degraded review quorum." Since Gemini has failed identically for 20 consecutive rounds, the mechanical quorum gate is permanently blocked pending a manual user waiver. This is correctly designed (fail-closed), but it means the plan as-is cannot advance on its own evidence - surfacing it so the user knows a waiver decision, not another review round, is the actual gating action. Process MINOR (not a plan defect, but a standing blocker the review summary at line 701 understates by framing it as "a fresh post-patch review round must return no-MAJOR").

### codex

- The plan has no safe representation for required private ledger fields in canonical good/public fixtures. Plan lines 152-161 require ledger fields including `source_id`, `source_sha256`, `private_lookup_key`, and `share_relative_path_private_only`; lines 302-313 and acceptance line 604 put `tests/fixtures/ace-wave0-control-plane/good/` and the control-plane artifact in the public-surface scan set; lines 297-300, 353-364, 546, and 593 reject assigned private field values, key-value rows, path-bearing values, and raw digests everywhere. The only defined fixture marker grammar is for `public_source_token` at lines 199-203. Without an explicit placeholder grammar for the private-only required fields, full good ledger rows either fail the scanner or cannot exercise the required schema.
- The legal/security gate fallback is not executable from this repo as written. Acceptance line 623 says that if `scripts/legal/legal-sanity-scan.sh` is absent, closeout must run an equivalent `.legal-deny-list.yaml` diff scan. I verified `scripts/legal/legal-sanity-scan.sh`, `scripts/legal/**`, and `.legal-deny-list.yaml` are all absent, and the plan does not add a legal config/script or name a concrete fallback command. That leaves a hard gate dependent on an unspecified external artifact.
- The review-artifact scope is still internally loose. Artifact map lines 108-110 say historical/local `scripts/review/results/2026-06-29-plan-51-*-r*.md` artifacts carry explicit `review_artifact_role` and that every retained `plan-51-*.md` artifact is scanned. Pre-label line 633 narrows this to status-evidence artifacts and excludes untracked local r*-round residue. The current worktree has many untracked r1-r15 artifacts without role tags, and scanning the whole `scripts/review/results` directory fails on old transcripts. The plan should make the artifact map match the pre-label rule: tracked/cited `status_evidence` or `public_history` only, with local-transient residue explicitly outside the scan target.

### gemini

- (none)
