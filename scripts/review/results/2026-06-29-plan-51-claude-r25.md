---
review_artifact_role: public_history
normalization_note: Local absolute runtime paths, if present, are tokenized in this public copy; authoritative verdict value preserved.
---

I have verified the plan's factual scaffolding thoroughly. Every file-existence claim, the issue set (#50-#63), the Structural Wave Gate Registry, the workflow wiring, and the committed review-artifact role tags all check out exactly as the plan asserts. That is unusually clean. My adversarial findings are therefore concentrated in scope, unresolved process gates, and implementation risk rather than factual errors.

` Insight -`
Two things worth calling out about *how* this plan is built: (1) It pre-decomposes itself into four module files (`ace_public_tokens.py`, `ace_public_surface_scan.py`, `ace_sampling_firewall.py`, `validate_...py`) - which is good engineering, but it also proves the seams along which the issue *could* have been split into separate issues. (2) The plan declares itself a scanned surface ("this plan" is in its own self-scan target list) while also containing placeholder grammar (`<PRIVATE_LOOKUP_KEY>`, `pst_...` examples) that must *pass* - a self-referential scanner that must distinguish its own spec-prose from fixture data. That context-sensitivity is the single most-revised mechanism across 24 rounds.
`-`

## Verdict
MAJOR

## Retrieval
- Read the full plan `docs/plans/2026-06-29-issue-51-ace-wave-0-corpus-ledger-routing-firewall-sampling-protocol.md` (lines 1-737, both pages).
- Verified every file claim in the Evidence block (lines 64-78) and Files-to-Change table: all six claimed-MISSING artifacts (`artifacts/ace-share-wave0-control-plane.md`, `scripts/validate_ace_wave0_control_plane.py`, `scripts/validate_ace_public_artifacts.py`, `scripts/legal/legal-sanity-scan.sh`, `.legal-deny-list.yaml`, `docs/case-studies/...`) confirmed MISSING; all claimed-EXISTS skills/docs/coordination/validator confirmed present.
- `gh issue list` - confirmed #50-#63 exist and are OPEN with the asserted titles; #52-#60 are exactly "wave 1-9," #61-#63 the cross-wave gates, matching the plan's snapshot matrix (lines 286-289).
- `docs/plans/ace-share-ingestion-wave-coordination.md` lines 34-60 - confirmed the "Structural Wave Gate Registry" exists with the exact five columns and the exact false/true/false matrix the plan depends on (lines 272-289). Not a phantom dependency.
- `.github/workflows/validate.yml` - read the full file; confirmed `validate_ace_epic_wave_coordination.py` is wired with `--scan-public-path`, and that bare `uv run` is used (consistent with the plan's "bare `uv run` is CI-only" rule, line 403).
- `scripts/validate_ace_epic_wave_coordination.py` (810 lines) - confirmed it implements the `--scan-public-path` fallback scanner the Pre-Label Checklist (line 636) leans on; it exists today.
- Verified committed `plan-51-claude-r16...r24.md` artifacts all carry `review_artifact_role: public_history` frontmatter (the plan's Status line, line 10, requires this) and a `normalization_note`.

## Findings

1. **Scope is too large for one issue and has not converged - MAJOR.** The plan is 737 lines, T3, and bundles into a single issue: 5 new Python modules (lines 119-125), 36 TDD tests, 27 acceptance criteria, a legal scanner + hand-rolled YAML parser, a workflow edit, and 5 skill edits. The Adversarial Review Summary (lines 651-718) shows Codex returning MAJOR for **24 consecutive rounds** with Claude oscillating MINOR/MAJOR - empirical evidence the unit is too large to stabilize. The plan's own "Scope Decision" (line 115) only carves off *#63-owned* items and explicitly elects to "accept broad front-loaded scope" rather than split. The pre-decomposition into four module files proves clean seams exist (schema/validator vs. sampling firewall vs. public-surface scanner + legal gate). Recommendation: split into independently-reviewable issues; a single PR of this blast radius is hard to land green and perpetuates the non-convergence.

2. **The plan's own gate bars status transition and no fresh no-MAJOR round exists - BLOCKER.** "Overall result" (line 720) states the plan "remains draft-only and must not move to `status:plan-review`." The latest committed round (r24) carries an active-provider MAJOR (Codex, line 717). The plan claims the current patch "is intended to address the actionable r24 Codex blockers" but no fresh post-patch review has been committed to confirm no active-provider MAJOR (the rule on lines 479-481, 644). This review is part of that fresh round; until Codex (and the others) re-review the *current* committed draft and return no-MAJOR, the plan is not in an approvable state by its own contract.

3. **Gemini degraded-quorum decision is unresolved and unresolvable by the plan - BLOCKER.** Gemini is UNAVAILABLE in all 24 rounds (`auth rc=41`, lines 655-718). Per lines 472-483, `status:plan-review` is blocked until Gemini is restored *or the user explicitly approves* a one-round degraded quorum after seeing the auth-failure evidence. This is an operator/user decision the plan correctly documents but cannot self-clear; it must be settled before transition.

4. **`visibility` is referenced as a ledger field but never defined in the schema - MINOR.** Risk bullet (line 727) says "`visibility` remains hand-set; implementation must fail closed unless public clearance evidence...". The ledger required-fields list (lines 149-158) contains no `visibility` field - routing keys are `sensitivity`, `public_clearance_evidence`, and `route_target`. This is leftover terminology that will confuse an implementer about which field the firewall actually keys on. Either rename the risk to the real field(s) or add `visibility` to the schema.

5. **The leakage gate for planning surfaces is manual, not CI-enforced, during the pre-implementation window - risk (MAJOR-leaning).** The plan's own self-scan of "this plan," the #63 plan, review artifacts, and sidecars (lines 311-325, 606) only runs once `scripts/validate_ace_wave0_control_plane.py` exists and is wired (acceptance line 623). Today, `validate.yml`'s coordination-validator invocation passes only 6 `--scan-public-path` arguments and does **not** include the #51 plan, #63 plan, `docs/04/05/07/18`, or the `plan-51-*` review artifacts. So during exactly the window when 36 review artifacts and these plans are being committed, the only thing preventing a private-corpus leak in them is the *manual* operator fallback run (lines 636-641) plus hand normalization. For a plan whose entire purpose is a data-leakage firewall, gating its own authoring surfaces on operator diligence rather than CI is a real residual exposure. Mitigation exists (artifacts carry `normalization_note`), but it is unenforced.

6. **The legal deny-list is parsed by a hand-rolled bash YAML subset - implementation risk (MINOR/MAJOR).** Lines 448-454 specify a bash wrapper parsing strict-subset YAML "without yq," where pattern entries are regexes that may themselves contain YAML-significant characters, with literal single quotes "represented by two adjacent single quotes." Parsing regex-bearing quoted YAML in bash line-matching is a classic source of silent mis-parses (a pattern containing `:`, `#`, backslashes, or unbalanced quotes can break the parser or the downstream regex). The test (line 579) uses author-chosen fixtures, so an adversarial pattern can pass the spec while the parser mis-handles it. Consider a minimal Python parser (already a Python-heavy plan) instead of bespoke bash YAML.

7. **The self-referential scanner's context classifier is under-de-risked - risk.** The scanner must allow placeholder grammar (`<GENERATE_PUBLIC_SOURCE_TOKEN>`, lines 204-209) and fenced `<METADATA_EVIDENCE_SHAPE>` rows (lines 82-90) *in this plan's prose* while rejecting structurally-identical strings elsewhere, "never by raw substring matching over methodology prose" (line 209). This path/format-context classification (lines 198-218) is the most-revised mechanism in the plan, yet there is no spike or prototype evidence it can be implemented without a false-allow (which would defeat the firewall) or false-deny (which would block the plan's own commit). For a security-critical classifier, "described precisely" is not the same as "demonstrated buildable."

## Blockers
- **Finding 2** - a fresh post-patch review round with no active-provider MAJOR must be recorded before `status:plan-review`; r24 Codex MAJOR currently stands.
- **Finding 3** - the Gemini-unavailable / degraded-quorum decision must be settled by Gemini restoration or explicit user waiver before transition.
- **Finding 1** - the scope decision should be revisited (decompose, or get explicit user sign-off that the bundled scope is accepted despite 24-round non-convergence) before implementation begins.

Findings 4-7 are MINOR/risk and do not individually block, but Finding 5 (manual-only leak gate for the planning surfaces) should be resolved or explicitly accepted by the user given this is a data-leakage firewall.
