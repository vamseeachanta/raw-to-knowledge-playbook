review_artifact_role: public_history
normalization_note: denied-token literal substitutions only; provider verdict unchanged

## Verdict
MAJOR

## Retrieval
- Read `docs/plans/2026-06-29-issue-51-ace-wave-0-corpus-ledger-routing-firewall-sampling-protocol.md` lines 1-682.
- Read `scripts/validate_ace_epic_wave_coordination.py` lines 91-112 and 594-661.
- Read `tests/test_validate_ace_epic_wave_coordination.py` lines 787-819.
- Read `docs/plans/2026-06-29-issue-63-ace-public-output-redaction-and-identifier-canary.md` lines 80-152.
- Read `docs/plans/ace-share-ingestion-wave-coordination.md` lines 1-57 and `docs/plans/README.md` lines 11-32.
- Read `docs/07-data-governance.md` lines 54-66 and `docs/19-trust-boundary-and-private-mode.md` lines 73-83.
- Grep’d `docs` and `skills` for `sha256|source hash|provenance pointer`.
- Grep’d #51 review artifacts for `review_artifact_role`, verdict headings, identity fields, and marker/private-field terms.
- Listed tracked and untracked `scripts/review/results/2026-06-29-plan-51-*.md` artifacts.
- Ran the parent fallback scan against the current #51 plan, #63 plan, `docs/plans/README.md`, and coordination doc: it passed.
- Fetched live GitHub issues #50, #51, and #63 with `gh issue view`.

## Findings
1. Plan §Pre-Label Evidence Checklist lines 601-604 relies on the parent fallback scanner before `scripts/validate_ace_wave0_control_plane.py` exists, but that scanner cannot enforce the plan’s central raw-source/private-field policy. `scripts/validate_ace_epic_wave_coordination.py:91-112` only checks mount-path/email/client-ish/confidential/SSN/phone patterns plus traversal commands; it has no pattern for assigned `source_id`, assigned `source_sha256`, `private_lookup_key`, private lookup maps, `public_source_token`, or source-like digest values. `tests/test_validate_ace_epic_wave_coordination.py:794-819` likewise only tests email/path/traversal examples. This leaves the pre-label scan unable to prove the issue-body/comment promises in plan lines 601-602.

2. The #51 self-scan acceptance still overclaims generic leakage coverage. Plan lines 393-397 explicitly say #51 does not load private deny-lists, does not certify real public exposure, and leaves maintained client/project/internal-name denial to #63. But acceptance line 575 says #51 blocks “personal identifiers” and “proprietary snippets” across public surfaces. Generic regexes can catch some emails/phones or marker phrases; they cannot certify proprietary snippets without a maintained corpus/list, which the plan forbids #51 from loading. This is an objective acceptance criterion that cannot be verified by the proposed #51 mechanism.

3. The review-history role contract is inconsistent with the current artifact set. Plan line 106 says historical/local `scripts/review/results/2026-06-29-plan-51-*-r*.md` artifacts carry explicit `review_artifact_role`, and lines 423-429 require committed/cited artifacts to be role-tagged while local-transient artifacts are never cited. In the worktree, only r16-r18 are tracked with `review_artifact_role`; r1-r15 exist under the same named artifact glob, are untracked, and lack role tags. The plan’s Adversarial Review Summary cites r3-r15 outcomes at lines 616-654 without specifying whether those local files will be normalized/tagged, committed as public history, or excluded as transient residue.

## Blockers
1. Finding 1 blocks `status:plan-review`: the pre-label scanner cannot enforce the issue/comment sanitization policy it is used to certify.
2. Finding 2 blocks implementation approval: the #51 acceptance criteria claim proprietary/private-snippet detection that #51 explicitly does not implement.
3. Finding 3 blocks status evidence cleanup: the plan must disposition r1-r15 artifacts before relying on the review-history contract.
