# Disagreement report — plan #66 (2026-06-30)

## Verdicts

| Provider | Verdict |
|---|---|
| claude | **MAJOR** |
| codex | MAJOR |
| gemini | UNAVAILABLE (gemini CLI failed, rc=1: Warning: Basic terminal detected (TERM=dumb). Visual rendering will be limited. For the best experience, use a terminal emulator with truecolor support. Warning: 256-color support not detected. Using a terminal with at least 256-color support is recommended for a better visual experience. Error authenticating: IneligibleTierError: This client is no longer supported for Gemini Code Assist for indiv) |

## Findings unique to each provider

A finding is 'unique to X' if its text appears in X's artifact but not
verbatim in any other provider's artifact.

### claude

- **MAJOR — The fixture contract cannot both "map placeholder fields to #65 private source terms" and pass the parent scanner, as written.** Acceptance criteria (line 218, 241) require `config/ace-public-token-fixture-contract.json` to pass the parent public-surface scan; the pseudocode (lines 137–144) and criteria (line 233) require it to map placeholder fields to the #65 private source terms (`source_id`, `source_sha256`, `private_lookup_key`, `private_lookup_map`, `share_relative_path_private_only`). The parent scanner's `PRIVATE_SOURCE_FIELD_ASSIGNMENT_PATTERNS[0]` (validate_ace_epic_wave_coordination.py:125) flags any line where those terms appear as the left side of a colon/equal assignment. Empirically confirmed: JSON keyed by a private source term **matches and self-blocks**, while list form under a neutral key is safe. The plan's stated mitigation — "parser context, not raw substring allowlisting" (lines 144, 234) — governs the *new* #66 validator's parsing, not the parent regex scanner that the acceptance criteria require the contract to pass. An implementer who naively encodes the placeholder map as a JSON object keyed by private-field name will produce a self-blocking artifact. The plan must explicitly constrain private field names to appear **only as list values, never on the LHS of a `:`/`=` assignment**, in every committed artifact in the scan set (contract, scripts, fixtures).
- **MAJOR — The opaque fixture-token grammar is never specified, yet the parent scanner already hardcodes the concrete-token shape `pst_[0-9a-f]{32}`.** `PRIVATE_SOURCE_FIELD_ASSIGNMENT_PATTERNS[2]` (validate_ace_epic_wave_coordination.py:127) defines a concrete public token as `public_source_token … pst_[0-9a-f]{32}`. The plan asserts "emitted tokens are opaque … cannot be decoded into source-like parts" (line 134) and tests "hand-authored concrete public token assignments fail" (line 209) without ever pinning the token format. Two unaddressed consequences: (a) if the generator's opaque tokens match `pst_[0-9a-f]{32}`, any committed file containing a token literal (e.g. an expected-output fixture) self-blocks; (b) if they do *not* match that shape, the parent scanner and the new validator disagree on what "concrete token" means, so the bad-fixture rejection test and the public-scan gate test different things. The plan must pin the emitted/forbidden token grammar and reconcile it against the scanner's existing `pst_[0-9a-f]{32}` assumption.
- **MINOR — Contract ownership claim contradicts the #65 schema.** Pseudocode line 111 asserts the contract validates "owner_issue is #66" and the plan treats #63 as solely the publication-certification owner. But the #65 schema records `downstream_contracts.public_token.owner_issues = [66, 63]` (artifacts/ace-wave0-ledger-schema.json:126–133) — #63 co-owns the public-token contract, not just certification. The import test `test_contract_imports_65_public_token_field` (line 197) will check against an ownership model the schema contradicts; reconcile the singular `#66` owner assertion with the schema's dual `[66, 63]`.
- **MINOR — "Files to Change" overstates pending work; #66 is already recorded.** The plan lists `docs/plans/README.md` and `docs/plans/ace-share-ingestion-wave-coordination.md` as Modify → "Record #66 draft plan status and keep implementation readiness false" (lines 182–183). Both already contain #66 rows at status `draft` / readiness `false` (README:32,54; coordination:42). The modification is largely a no-op; the implementer should diff what actually needs to change rather than re-add rows. (No validator collision risk here — the coordination doc's "Wave 0 Split Registry" is a separate section the validator does not parse as a child-wave row.)
- **MINOR — The 3-provider review quorum assumes Gemini, which repo history shows is chronically unavailable.** Plan header (line 10) and Artifact Map pre-commit `scripts/review/results/2026-06-30-plan-66-gemini-r1.md`, and the Adversarial Review Summary (lines 251–257) lists Gemini PENDING as a gate. The #51 ledger row records Gemini as `unavailable: auth rc=41` and gates on "Gemini restoration or explicit degraded-quorum authorization." The plan should state the same degraded-quorum handling rather than implying a Gemini verdict is obtainable.

### codex

- `docs/plans/2026-06-30-issue-66-ace-public-token-fixtures-private-field-placeholders.md:267` leaves a scope-changing open question: whether #66 updates only its own contract/validator or also writes a machine-readable cross-link into the #65 schema. That is not a harmless open question: `artifacts/ace-wave0-ledger-schema.json:282-287` still records #66 with `plan_path: ""`, `status_snapshot: "plan-required"`, and `implementation_ready: false`, while the plan’s Files to Change table at `docs/plans/2026-06-30-issue-66-ace-public-token-fixtures-private-field-placeholders.md:176-188` omits `artifacts/ace-wave0-ledger-schema.json`. The plan must decide this before approval.
- The plan cites review artifact paths that do not exist or are unusable gate evidence. `docs/plans/2026-06-30-issue-66-ace-public-token-fixtures-private-field-placeholders.md:10` and `:92-94` name `scripts/review/results/2026-06-30-plan-66-{claude,codex,gemini}-r1.md`; `git ls-files` returned none of those files, and `find scripts/review/results -name '*plan-66*'` found only bare `.md`/`.err` names. `wc -c` showed `2026-06-30-plan-66-claude.md` and `2026-06-30-plan-66-codex.md` are 0 bytes, and `scripts/review/results/2026-06-30-plan-66-gemini.md:1-2` is an `UNAVAILABLE` auth/tier failure, not an adversarial review.
- The public-scan plan omits provider error sidecars even though a live #66 sidecar exists and contains local runtime material. The plan only lists `.md` review artifacts at `docs/plans/2026-06-30-issue-66-ace-public-token-fixtures-private-field-placeholders.md:92-94`, and `test_public_scan_paths_cover_66_artifacts` at `:218` says “retained plan review artifacts” without naming `.md.err` sidecars. The transient `scripts/review/results/2026-06-30-plan-66-codex.md.err` sidecar contained local repo/home path material, while `scripts/validate_ace_epic_wave_coordination.py:715-749` only scans paths explicitly passed to it. The plan needs an explicit sidecar disposition: normalize and scan, or declare transient and exclude from retained evidence.
- The legal/security gate is not satisfiable from this plan. The supplied AGENTS hard gate says code must pass `scripts/legal/legal-sanity-scan.sh`, but no such file exists in this repo, `docs/plans/2026-06-30-issue-66-ace-public-token-fixtures-private-field-placeholders.md:243-245` lists closeout commands without any legal/security scan, and `docs/plans/README.md:57` / `docs/plans/ace-share-ingestion-wave-coordination.md:45` still show #69 legal/security scan as future work. The plan must either add a concrete legal/security command or explicitly record the #69 deferral/unavailable status as a closeout condition.

### gemini

- (none)
