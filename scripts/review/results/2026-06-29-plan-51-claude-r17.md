review_artifact_role: public_history

I've verified the plan's concrete claims against the repo. Every file-existence assertion, issue state, validator capability (`--scan-public-path`, leak/traversal patterns), and the coordination-doc reconciliation text check out. The residual defects are structural, not factual.

## Verdict
MAJOR

## Retrieval
- Read the full plan `docs/plans/2026-06-29-issue-51-...md` (lines 1–650, both pages).
- Verified existence of all 5 named skills + `skills/README.md` + `skills/validate_skill.py` — all EXIST.
- Verified `docs/{01,04,05,07,18,19}`, `docs/plans/README.md`, the coordination doc, parent #50 plan, #63 plan, `scripts/validate_ace_epic_wave_coordination.py`, `tests/test_validate_ace_epic_wave_coordination.py`, `.github/workflows/validate.yml` — all EXIST. Confirmed the 6 to-be-created files (artifact, validator, configs) are MISSING as the plan asserts.
- Ran the cited sweep `rg -n "sha256|source hash|provenance pointer" docs skills --glob "*.md" --glob "SKILL.md"` → **90 hits across 24 files**; broke down per-file (plan itself = 36 hits).
- Read `scripts/validate_ace_epic_wave_coordination.py` lines 580–654 → confirmed `--scan-public-path`, `validate_public_artifact_paths()` applies both `DENIED_TRAVERSAL_PATTERNS` and `PRIVATE_LEAK_PATTERNS`.
- Inspected coordination-doc header + rows #52–#60 (trailing column = "Publication exposure"=false, not the snapshot boolean) and Manifest-gate text for #51/#61/#62/#63 → matches the plan's claimed reconciliation phrasing.
- `gh issue view` for #50/#51/#61/#62/#63 → all OPEN, titles match.
- Confirmed `artifacts/` is outside the sweep scope (`docs skills`).

## Findings

1. **Cross-issue CI fragility: the hash-sweep report is keyed `path:line` over ~24 files maintained under other issues (plan §"Gaps", line 44; §reconcile, lines 341–342; TDD `test_existing_governance_docs_and_skills_do_not_publish_raw_hashes`, line 495).** The plan mandates that `artifacts/ace-source-hash-policy-sweep.md` classify "every unique `path:line` hit" from `rg ... docs skills`, and the test fails on any unclassified hit. The live sweep returns **90 hits across 24 files**; only a few are #51-owned. 36 hits are in this plan, the rest in sibling plans #50/#52–#63, `docs/{04,05,07,18,19}`, and 8 skill files — all edited under their own issues. Because dedup is explicitly "by canonical `path:line`" (line 342), any line insertion above a hit in any swept file shifts line numbers and invalidates the frozen report's keys, breaking #51's CI. The plan assigns no regeneration owner or staleness policy, so #51's green status is hostage to unrelated edits in files it does not own. This is the central structural risk and the wide blast radius (`Audit/conditional modify docs/plans/2026-06-29-issue-*.md` and "skill files returned by the sweep", lines 456–457) is disproportionate for a "wave 0 control plane" issue.

2. **The plan's own prose "Source inventory" lists only 4 of the 7 authoritative fixed paths — self-failing-fixture risk (lines 29–32 vs. lines 78–84 / 128–135; TDD `test_fixed_source_evidence_paths_are_authoritative`, line 504).** The Resource-Intelligence "Source inventory" prose names only `INDEX.md`, `assets.json`, `_cad-index/index-summary.json`, and `llm-wiki` — omitting `docs/master-index.jsonl`, `_cad-index/cad-readability-index.tsv`, and `.ace-knowledge/index.db`. But line 137–138 states "prose source-inventory lists and fenced evidence rows must be generated from or validated against this exact set [of 7]," and the test requires "prose inventory and fenced evidence rows must match the same set." The term "prose source-inventory lists" is never disambiguated as to *which* prose; if it includes this section, the plan's own structure fails its own acceptance test.

3. **`rg` reproducibility in CI is unspecified (validator orchestration, lines 113–119; acceptance lines 566, "validating repo fixtures/docs only").** To verify "every hit is classified," the validator must reproduce the 90-hit set, but the plan never says whether it shells out to `rg` (a Rust binary, not a Python stdlib dep — absent in a minimal `uv run` container, though present on GitHub ubuntu runners) or reimplements the alternation in Python `re`. The "CI must pass without ACE_SHARE_ROOT" criterion is asserted, but an unstated dependency on an external `rg` binary undercuts the "repo fixtures/docs only" claim and risks rg-version/Python-regex divergence for the "exact `rg` shape" the scanner must allowlist (line 252, `test_repo_local_hash_sweep_command_is_allowed`).

4. **(MINOR/cosmetic) The authoritative sweep string carries a redundant glob (lines 252, 341, 496, 541).** In `rg --glob "*.md" --glob "SKILL.md"`, inclusive globs OR together and `SKILL.md ⊂ *.md`, so the second glob is a no-op. Harmless, but the plan elevates this exact string to a frozen, scanner-allowlisted "exact shape," baking the redundancy into the contract.

## Blockers
- **Finding 1** — assign explicit regeneration ownership and a staleness/keying policy for the hash-sweep report before #51 can rely on `test_existing_governance_docs_and_skills_do_not_publish_raw_hashes` in CI. Key hits on `(path, normalized-content)` rather than `path:line`, or scope the sweep to #51-owned files, so #51's CI is not broken by edits in 23 files owned by other issues.
- **Finding 2** — disambiguate which "prose inventory" the authoritative-paths test binds to, and make the binding prose enumerate all 7 fixed paths (or exclude the Resource-Intelligence narrative explicitly), so the plan does not fail its own fixture.

Finding 3 should be resolved (state the reproduction mechanism) but can ride along with the Finding 1 fix. Finding 4 is non-blocking.

`★ Insight ─────────────────────────────────────`
- The sharpest defect here isn't a wrong fact — every citation in the plan is accurate — it's a **coupling-by-line-number**: a CI gate keyed on `path:line` across files owned by other issues converts routine, unrelated edits into spurious failures. The fix is the standard one for content-addressed checks: key on normalized content, not position.
- Heavily-iterated plans (16 review rounds here) tend to accrete *scope* defensively — #51 ends up auditing 24 files to be "thorough," which is exactly what creates the fragility. Thoroughness and blast-radius pull in opposite directions.
`─────────────────────────────────────────────────`
