review_artifact_role: public_history
normalization_note: denied-token literal substitutions only; provider verdict unchanged

## Verdict
MAJOR

## Retrieval
- Read `docs/plans/2026-06-29-issue-51-ace-wave-0-corpus-ledger-routing-firewall-sampling-protocol.md` lines 1-705.
- Read `scripts/validate_ace_epic_wave_coordination.py` lines 91-117, 260-325, 598-626, and 644-670.
- Read `tests/test_validate_ace_epic_wave_coordination.py` lines 547, 617-635, and 826-830.
- Read `.github/workflows/validate.yml` lines 21-32.
- Read `docs/plans/2026-06-29-issue-63-ace-public-output-redaction-and-identifier-canary.md` lines 80-152.
- Grep'd `docs`, `skills`, and `README.md` for `sha256`, `source hash`, `provenance pointer`, `source_sha256`, and public-safe source-reference language.
- Fetched live GitHub issue metadata for `#51` and `#63` with `gh issue view`.
- Ran `UV_CACHE_DIR=.claude/state/uv-cache uv run python scripts/validate_ace_epic_wave_coordination.py` and `python -m unittest tests.test_validate_ace_epic_wave_coordination`; both passed.
- Ran fallback scans against the #51 plan, `tests/test_validate_ace_epic_wave_coordination.py`, and synthetic raw-digest/table-row fixtures.
- Checked for `scripts/legal/legal-sanity-scan.sh`; it is absent.

## Findings
1. Plan self-scan scope is contradictory and currently self-blocking. `docs/plans/2026-06-29-issue-51...md:298-307` and `:395-405` include test files in #51 public-surface scan scope, and `:554` says the scan input includes "parent coordination validator/tests." But `:595` says "Test source files run through unit tests and restricted bad-fixture harnesses rather than the generic public-text scan." Empirically, scanning `tests/test_validate_ace_epic_wave_coordination.py` with the parent fallback produces 26 denials because the test file intentionally contains denied commands and private-field fixtures at `tests/test_validate_ace_epic_wave_coordination.py:617-635` and `:826-830`. The plan must choose one rule and encode it; as written, either CI self-blocks or declared scan coverage is false.

2. The pre-label fallback scanner cannot enforce the raw digest/table-row policy the plan relies on before #51 exists. The plan says table rows containing real values and source-like raw digests are rejected everywhere (`docs/plans/2026-06-29-issue-51...md:357-360`, `:584`, `:595`) and says pre-label public surfaces are scanned by `scripts/validate_ace_epic_wave_coordination.py --scan-public-path` before the #51 validator exists (`:623`). But the fallback only checks key/value assignment regexes in `scripts/validate_ace_epic_wave_coordination.py:99-102`; it passed both `SOURCE_HASH_ASSIGNMENT_EXAMPLE` and `SOURCE_HASH_TABLE_EXAMPLE` synthetic fixtures. That leaves a status-transition path where forbidden hash evidence can be posted and still pass the required fallback scan.

3. The plan omits the inherited legal/security closeout gate. The user-provided AGENTS hard gate says code must pass `scripts/legal/legal-sanity-scan.sh`, but the plan's closeout acceptance commands list only the wave validator, unit tests, workflow invocation, ACE_SHARE_ROOT-unset behavior, and `skills/validate_skill.py` at `docs/plans/2026-06-29-issue-51...md:609-613`. The script is also absent in this repo. Since #51 creates scripts, config, fixtures, and workflow changes, the plan needs either to add the legal scan artifact/gate or explicitly resolve the missing required gate before implementation.

## Blockers
- Finding 1.
- Finding 2.
- Finding 3.
