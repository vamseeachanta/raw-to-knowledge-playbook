Verdict: MINOR

Findings:
- [MINOR] The issue-comment scan recipe is not executable as written and has a silent under-scan path. The plan's pseudocode (line 174) says "write planned issue comment body to a temp file and scan it with both scanners," and the verification commands pass `--scan-public-path "$ISSUE_52_COMMENT_BODY"`. Both scanners reject paths outside the repo (`scripts/ace_public_surface_rules.py:56` "scan-path-absolute: outside repo path is not allowed"; `scripts/legal/legal_sanity_scan.py:347` "path outside repository"), so a literal `/tmp` temp file makes both commands fail. Worse, if the operator instead places the file under `.claude/state/` (this repo's conventional scratch dir), `classify_path` (`scripts/legal/legal_sanity_scan.py:267`) returns "exclude" and `collect_explicit_candidates` silently drops it — the legal scan exits 0 without ever scanning the comment body, a vacuous pass of the exact gate the r1 patch added. The plan must pin a repo-local, include-classified location for the comment-body file (e.g. `.planning/*.md` or `scripts/review/results/*.md`), or use the public-surface scanner's purpose-built `--snapshot` issue/comment mode (`scripts/validate_ace_public_surface_scan.py:42`), which the plan never mentions.
- [MINOR] The pushed-evidence requirement from Claude r1 finding 5 was not patched. That finding listed four gaps: no-MAJOR threshold, degraded-Gemini policy, final-round path replacement, and "pushed evidence requirement." The plan now states the first three (plan line 230), and line 202 requires recording reviewed commit/review artifacts/validation evidence in an issue comment before applying `status:plan-review` — but nowhere does it state that the plan, ledger, and review artifacts must be committed and pushed (remote-visible) before the label. The only "push" language in the plan is about `llm-wiki` (line 128). Relatedly, `legal-sanity-scan.sh --diff-only` currently exits 1 on the untracked review artifacts ("untracked public-surface candidate" × 4), confirming the evidence chain cannot pass until artifacts are at least staged — the plan should make commit-and-push an explicit pre-label step rather than an implicit one.
- [MINOR] Residual review-state drift of the same class Codex r1 rated MAJOR. The plan header (line 10) still labels the r1 artifacts as "pending target set" even though r1 is complete with recorded MAJOR verdicts (lines 279–285), and an untracked `scripts/review/results/2026-07-02-plan-52-gemini-r2.md` (sanitized UNAVAILABLE) already exists on disk but is recorded nowhere in the plan's review summary or the ledger's `#52` review-artifacts cell (which says "unsupported-client r1"). The r2 round is visibly in flight, so this is bookkeeping lag rather than a leak, but the header and ledger must be reconciled to the final round before evidence posting or the no-MAJOR quorum record is ambiguous about which round counts.

Checked:
- Read the full #52 plan, `docs/plans/README.md`, `docs/plans/ace-share-ingestion-wave-coordination.md`, and all three r1 review artifacts; mapped every r1 finding to its patch. Confirmed patched: sanitized Gemini artifact (no host paths, explicit UNAVAILABLE, consistent with plan summary and ledger "unavailable: unsupported-client r1"); measured-report contradiction (Files-to-Change line 184, Deliverable line 128, acceptance line 270, and risk line 298 now make the `docs/case-studies/` page synthetic-fixture-only with measured results private-sidecar-only until #63); stale issue-body blocker (line 202 + acceptance line 271 require correcting or superseding it); comment-scan intent (pseudocode line 174, commands, acceptance line 271).
- Verified #70 is treated as manifest-evidence integration, not part of the #65–#69 split, in plan lines 34/64/291, the ledger's Wave 0 Split Registry (lists only #65–#69), and the README plan index (line 36).
- Verified #51 is treated as unapproved umbrella (plan lines 29, 291) while #65–#70 are consumed as closed contracts; confirmed `.planning/plan-approved/` markers exist for 50, 62, 65–71 and none for 51/52/61/63, matching the plan's Evidence block.
- Verified TDD coverage for the two r1 gaps: `test_cap_violating_sampling_request_is_rejected` (#67 caps, line 213) and `test_durable_output_fields_require_61_gate` (#61 durable/lifecycle blocking, line 223), plus fail-closed empty-registry test (line 212) consistent with the actually-empty `trusted_evidence: []` in `artifacts/ace-manifest-freshness/trusted-evidence-registry.json` and the fail-closed language in `config/ace-bounded-sampling-firewall-contract.json` (issues 52–60 in `downstream_issue_range`, `MISSING_62_EVIDENCE_POINTER`/`UNTRUSTED_62_EVIDENCE` reason codes).
- Verified route vocabulary, `#52` `wave_class=ingestion_wave`, `requires_manifest_snapshot_id=true`, and `successful_routed_items`/`eligible_candidate_items` success fields against `artifacts/ace-wave0-ledger-schema.json` and the ledger's Structural Wave Gate Registry — the plan's contract table and metric formula match exactly.
- Verified `mkdocs.yml` (`docs_dir: docs`, no case-studies wave-1 page in nav) and `.github/workflows/pages.yml` (builds/deploys all of `docs/` on push to main), confirming the plan's risk claim that unlisted `docs/` pages still deploy; the synthetic-only constraint is consistent with existing practice of deploying `docs/plans/` as public methodology.
- Verified both scanners accept the flags the plan uses (`--scan-public-path` append on both; `--diff-only` on the legal scanner) and traced their path-resolution/classification code paths (basis of finding 1).
- Ran the plan's verification set against current artifacts: `validate_ace_epic_wave_coordination.py` PASS, `validate_ace_wave0_schema_contract.py` PASS, public-surface scan over the three plan docs plus all three r1 artifacts PASS, legal scan over the same set PASS, `git diff --check` clean; `legal-sanity-scan.sh --diff-only` FAIL only on untracked review artifacts (basis of finding 2). Confirmed referenced files exist: `skills/validate_skill.py`, `.github/workflows/validate.yml`, `tests/fixtures/ace-manifest-freshness/valid-operational-evidence.json`, and all four wave-0 config contracts.
