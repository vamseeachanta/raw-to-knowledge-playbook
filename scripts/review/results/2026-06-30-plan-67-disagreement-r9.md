# Disagreement report — plan #67 (2026-06-30)

## Verdicts

| Provider | Verdict |
|---|---|
| claude | MINOR |
| codex | MAJOR |
| gemini | UNAVAILABLE (gemini CLI failed, rc=1: Warning: Basic terminal detected (TERM=dumb). Visual rendering will be limited. For the best experience, use a terminal emulator with truecolor support. Warning: 256-color support not detected. Using a terminal with at least 256-color support is recommended for a better visual experience. Error authenticating: IneligibleTierError: This client is no longer supported for Gemini Code Assist for indiv) |

## Findings unique to each provider

A finding is 'unique to X' if its text appears in X's artifact but not
verbatim in any other provider's artifact.

### claude

- **The Adversarial Review Summary reports a blanket "Overall result: MAJOR" (line 532) that the body has already superseded, with no per-finding resolved/open status.** I verified that every r8 finding is patched in the current text: Codex r8 F1 (output-shape under-specified) → now closed by §Output Shape and Route/Store Boundary (lines 204–214) + `test_output_shape_route_store_is_metadata_only` (line 401); Codex r8 F2 (no `blocked_pending_62_contract` test) → `test_blocked_pending_62_contract_is_failure_result_not_authorization` (line 410) + Runtime blocked fixture (line 240); Claude r8 F1/F2 (overstated allow-path) → Deliverable rewritten to defer the operational allow-path to `#70` (line 154); Claude r8 F3 (hand-maintained scan drift) → derived-manifest pseudocode (lines 350–361) + `test_review_artifact_discovery_blocks_unscanned_rerun_artifacts` (line 440); Claude r8 F5 → gate rule now states the effective Claude-AND-Codex rule plainly (line 499); Claude r8 F6 → acceptance line 480 now matches CI's `uv run skills/validate_skill.py`. The risk: an approver reading only the summary sees "MAJOR" and cannot tell these are resolved. The summary should mark r8 rows as *patched, pending re-review* rather than leave a standing MAJOR verdict against text that no longer contains the defects. **(MINOR — documentation hygiene, not a code defect.)**
- **The Fixture Payload Contract under-lists what `good-request.json` must contain, contradicting `required_sampling_fields` and its own required-field test.** Line 238 gives good-request's "Required target fields" as only `target_issue=67`, `request_class=metadata_only_fixture`, `fixture_scope=firewall_validator_self_check`. But `required_sampling_fields` (line 169) requires *every* request to also carry manifest source, seed, sort rule, per-bucket cap, max files, max bytes, output shape, route target, and logical target store — and `test_bounded_sampling_fields_are_required` (line 400) asserts that missing any of "manifest source, seed, sort rule, per-bucket cap, max files, max bytes, request class, or output shape fails." An implementer building `good-request.json` from the Fixture Payload row literally would produce a fixture missing ~9 required fields that then **fails the plan's own validator**. Fix: either state that the column lists only the *distinguishing* fields, or enumerate the full required set for the happy-path fixture. **(MINOR — resolvable by cross-reference, but an implementation trap.)**
- **The reframed private-read guard (`test_validator_refuses_private_reads_when_ace_share_root_is_set`, line 413; acceptance line 469) still risks being a caller-less guard.** The plan states `#67` does zero source-root reads and "will pass with `ACE_SHARE_ROOT` unset" (line 46). The r8-patched test now calls a *named* helper directly and asserts it returns `ACE_SOURCE_ROOT_ACCESS_FORBIDDEN` — an improvement over the vacuous version. But because the validator has no real code path that touches the root in this unit, the helper has **no invocation site**; the test proves the helper returns the sentinel in isolation, not that any real operation is actually gated by it. To make the boundary non-vacuous, specify that the helper wraps a representative access primitive and add a test that a source-root-touching operation *routes through* it. **(MINOR — residual of Claude r8 F4, which was itself rated MINOR.)**
- **The plan's characterization of the live `#67` skill bindings (line 29) is misleading about set membership, and the issue's "Method issues: #1, #12" binding is not captured.** The live issue body names skill groups: `content-triage-and-exclusion, source-extraction-coverage, format-coverage-ledger, public-private-routing, adversarial-verify-loop` — a five-set that **omits `page-shape-contract`** (which is in `#65` `bound_skill_groups`) and **includes `source-extraction-coverage`** (which is not). The plan frames this as the issue "additionally requires `source-extraction-coverage`" on top of the `#65` five (line 29), implying issue-set ⊇ #65-set, which is false — the issue *drops* `page-shape-contract`. Functionally the Artifact Map (lines 114–115) scans all six, so coverage is fine, but the narrative misstates the relationship. Separately, the issue body's `Method issues: #1, #12` are never surfaced in the plan even though the Pseudocode validates "method issue bindings" (line 293). **(MINOR — verify the reconciliation prose and record the method-issue binding.)**
- **The review gate is structurally a two-of-two unanimity that is one Gemini-recovery away from changing meaning, and the plan does not say what happens if Gemini becomes available.** Line 499 correctly states the *current* effective rule (both Claude and Codex no-MAJOR) given Gemini's `IneligibleTierError`. But acceptance criterion line 450 phrases it as "at least two usable provider results and no usable provider returns MAJOR." If Gemini ever returns a usable MAJOR, the same wording flips from "need both of two" to "need all-clear of three" without any plan change — a silent tightening. State whether Gemini recovery raises the floor to three usable providers or the gate stays pinned to Claude+Codex. **(MINOR.)**
- I reviewed the plan against: file/directory existence for all Artifact-Map and Files-to-Change paths; every `#65` enum/registry claim (route/store, caps, `bound_skill_groups`, `canonical_wave_registry`, `requires_manifest_snapshot_id`); the coordination-ledger manifest keys and caps; the parent scanner's `--scan-public-path` CLI and its four committed-file pattern groups; an **empirical** self-block run of the parent scanner over the plan file; the live `#67` issue body; and a per-finding trace of all r8 MAJORs against the current text. I did **not** find a substantiable new MAJOR: the core safety property — that no request in this approval unit can authorize operational sampling (downstream fails closed, `#67` fixtures "cannot authorize operational sampling," all output is `metadata_only_request_record`) — holds under each bypass I tried (target_issue spoofing to `#67`, `control_plane_proof` abuse, fixture-scope misuse against `#52–60`).

### codex

- Plan contradicts itself on the accepted request surface. `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md:154` says this unit will “accept only #67 metadata-only self-check fixture records,” but the Request Class Mapping allows `control_plane_proof` for #51, #61, #62, and #63 at lines 194-197, and line 200 says requests matching the table are valid while non-matching requests fail. The TDD row at line 403 also treats those control-plane targets as covered allowed targets. The plan must decide whether `control_plane_proof` requests pass, fail closed, or are deferred; right now an implementation can add out-of-scope accepted request classes while still satisfying the mapping table.
- Raw manifest read denial misses one of the plan’s own allowed manifest sources. The plan includes `INDEX.md` in the six allowed manifest sources at line 48 and promises raw manifest reads are denied at lines 184 and 432. The inherited parent scanner’s `MANIFEST_PATH_PATTERN` in `scripts/validate_ace_epic_wave_coordination.py:145` omits `INDEX.md`; its raw-read denial at lines 155-158 only catches paths matching that pattern. Empirically, parent scan passed a temp public artifact containing a raw-read example for `INDEX.md` while denying the equivalent example for `assets.json`. A generic `test_raw_manifest_read_class_is_denied` is insufficient; #67 needs per-source denial coverage or an explicit #67-only extension for `INDEX.md`.
- The “closed” command grammar is only closed at the category level, not at the executable-token level. The contract field at plan line 181 lists classes like traversal and raw manifest read, and line 277 says command verbs will be neutral enum values, but no section defines the concrete syntax/token set that maps to each class. The TDD rows at lines 429-433 test one class at a time, not every inherited parent pattern in `scripts/validate_ace_epic_wave_coordination.py:146-158` or every allowed manifest key. This leaves the classifier free to pass tests with a narrow representative per class while missing real denied commands.

### gemini

- (none)
