review_artifact_role: public_history
normalization_note: denied-token literal substitutions only; provider verdict unchanged

I've verified the plan's claims against the live repo, the GitHub issues, the parent validator, the coordination doc, and the committed review artifacts. Here is my adversarial review.

`★ Insight ─────────────────────────────────────`
- The plan's safety model is self-referential: the #51 plan lives at `docs/plans/…` (a published MkDocs surface), so the plan must survive the very scanner it mandates. I ran that scanner (`validate_ace_epic_wave_coordination.py --scan-public-path`) against the plan, the #63 plan, README, the coordination doc, and the committed r16–r19 artifacts — all PASS. So the most obvious self-inflicted failure mode is already defused; the real findings are elsewhere.
- A 19-round review log with both active providers returning MAJOR *every* round and the third (Gemini) UNAVAILABLE every round is itself diagnostic data: it signals a spec too large to converge, not a sequence of independent small fixes.
`─────────────────────────────────────────────────`

## Verdict
MAJOR

## Retrieval
- Read the full plan, `docs/plans/2026-06-29-issue-51-…-sampling-protocol.md` lines 1–706.
- Verified existence of all cited repo files: `docs/01/04/05/07/18/19-*.md`, `docs/plans/README.md`, `docs/plans/ace-share-ingestion-wave-coordination.md`, the five cited `skills/*/SKILL.md`, `skills/README.md`, `skills/validate_skill.py`, `scripts/validate_ace_epic_wave_coordination.py`, `tests/test_validate_ace_epic_wave_coordination.py`, `.github/workflows/validate.yml` — all EXIST. Confirmed the four to-be-created configs/scripts are MISSING as the plan claims (`config/ace-public-output-contract.json`, `config/ace-public-surface-deny-list.json`, etc.).
- Fetched live GitHub issues: #50–#63 all OPEN; #52–#60 = ingestion waves 1–9, matching the plan's matrix.
- Read `scripts/validate_ace_epic_wave_coordination.py` lines 88–150 and 300–645 (`PRIVATE_LEAK_PATTERNS`, `DENIED_TRAVERSAL_PATTERNS`, `PRIVATE_SOURCE_FIELD_ASSIGNMENT_PATTERNS`, `validate_public_artifact_paths`, `_extract_review_verdict`); confirmed `--scan-public-path` exists and applies leak/traversal/assignment patterns.
- **Executed** the plan's mandated pre-label fallback scan over the #51 plan + #63 plan + README + coordination doc → `PASS` (exit 0). Re-ran it over committed r16–r19 artifacts → `PASS`.
- Read `.github/workflows/validate.yml` (45 lines) — confirms it does not yet invoke `validate_ace_wave0_control_plane.py` (the plan's "Modify" is real).
- Grepped `sha256` across `docs/**/*.md` + `skills/**/*.md`: ~25 files hit, including this plan itself (23 hits), 8 skills, and 6 sibling plans — the sweep surface the plan describes.
- Fetched issue #51 body and read the #63 plan (178 lines); checked `review_artifact_role` tags and verdict headings in committed artifacts.

## Findings

1. **Scope vastly exceeds what issue #51 asks, pulling #61/#63-owned work forward (root cause of non-convergence).** Issue #51's acceptance criteria (live body) ask for: a 9-field ledger (source path, extension family, content class, sensitivity, target store, extraction level, verification state, expected yield, exclusion reason), 4-way routing, an issue/skill map, bounded sampling, and a closeout rule. The plan instead specifies a 33-field ledger (§Pseudocode lines 148–157), a CSPRNG token generator (`scripts/ace_public_tokens.py`), a repo-wide `sha256` policy sweep that edits `docs/07`, `docs/19`, the parent epic plan #50, and the #63 plan (Files to Change lines 497–503), a shared JSON contract for #63 to consume, review-artifact normalization/role machinery, and a degraded-quorum review policy — across ~25 files, 5 new modules, and 40+ TDD tests for a wave explicitly carrying "0% content ingestion" (§Complexity line 705). By the plan's own ownership boundaries (lines 50, 55, 395–411) durable token storage is #61's and public certification is #63's, yet #51 still implements the token grammar, the shared contract, and edits sibling/parent plans. This is the most likely reason the plan has not converged.

2. **The plan has never passed a clean review round — 19 consecutive MAJORs from both active providers.** §Adversarial Review Summary lines 636–687 show Claude and Codex returning MAJOR (or, once, MINOR for Claude r18) in every round r3–r19, with the Overall Result (line 688) stating "r19 returned MAJOR from both active providers… This plan remains draft-only and must not move to `status:plan-review`." This review continues the pattern (Findings 1, 4). A spec that surfaces fresh MAJORs after 19 patch cycles is evidence the contract is too large to verify, not a contract one round away from done.

3. **The plan's own T3 review gate is structurally unsatisfiable in this environment.** Every round r3–r19 records "Gemini UNAVAILABLE… auth failed with rc=41" (lines 638–686), and the degraded-quorum policy (lines 450–461 and Pre-Label checklist line 627) states "Gemini noninteractive auth/config failure is not a quota outage and does not automatically authorize downgrade; `status:plan-review` is blocked until Gemini is restored or the user explicitly approves a one-round degraded review quorum." Since Gemini is consistently broken here, the plan cannot reach `status:plan-review` through its documented quorum without an out-of-band user waiver that is not a plan deliverable. This is an acknowledged but unresolved blocker to the plan's stated goal.

4. **The coordination doc's #51 review pointer is stale relative to committed evidence.** `docs/plans/ace-share-ingestion-wave-coordination.md` line 38 records "Claude: …plan-51-claude-r15.md; Codex: …codex-r15.md; Gemini: unavailable: auth rc=41 r15", but committed artifacts run through r19 (`git ls-files` shows tracked claude/codex/gemini/disagreement r16–r19) and the plan's own summary covers r19. The plan defers updating this to "during plan-review" (Files to Change line 505), yet §Parent #50 Pre-Plan-Review Support (lines 519–521) and the Pre-Label checklist (line 626) require the coordination support kept green/aligned *before* plan-review. As written, the status-evidence trail is internally inconsistent now.

5. **`config/ace-public-output-contract.json` is a hard cross-issue ordering dependency that will leave #63's already-written tests red.** The #63 plan already declares tests `test_loads_shared_public_output_contract` and `test_allows_opaque_public_source_tokens` keyed on `config/ace-public-output-contract.json` (#63 plan lines 135, 137, 146), but that file is MISSING (verified) and is created by #51 (Files to Change line 489). The plan describes #63 as "consuming" the config but never flags that #63's committed acceptance tests cannot pass until #51 merges. Minor sequencing gap, but it undercuts the "modify #63 only if drift is found" framing (line 503): the #63 plan already references the config as authoritative, so that work item is largely already done — inflating the plan's apparent remaining scope.

6. **Minor field-name drift between the schema definition and acceptance criteria.** §Pseudocode line 157 names `skill_group` and `method_issues`; Acceptance Criteria line 578 names "skill" (not `skill_group`) and omits `method_issues`. For a plan whose entire value is mechanically-enforceable field exactness, a validator written to one spelling and a reviewer checking the other will disagree. Reconcile to one canonical token set.

## Blockers
- **Finding 3** — the Gemini-unavailable review gate blocks `status:plan-review` and is not resolvable by the implementation; it requires an explicit, recorded user waiver before any status transition. Must be resolved (waiver or restored Gemini) before the plan can advance.
- **Finding 4** — the stale r15 pointer in the coordination doc must be updated to the actual committed review identity before the plan's status-evidence trail is consistent enough to support plan-review.
- **Finding 1** — the scope/ownership expansion should be explicitly adjudicated with the user (trim to issue #51's stated ACs, or get sign-off that the cross-wave governance is intentionally front-loaded) before implementation begins, because it is the probable cause of the 19-round non-convergence in Finding 2.

Findings 5 and 6 are MINOR and need not block implementation, but should be fixed in the same patch.
