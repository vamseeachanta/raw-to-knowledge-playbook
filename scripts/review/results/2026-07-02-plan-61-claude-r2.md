# Plan Review: #61 Claude r2

Verdict: MINOR

Findings:
- MINOR: Reset rules said source changes reset all affected artifacts, but the transition table omitted `candidate -> stale_requires_rescreen` and `rejected -> stale_requires_rescreen`.
- MINOR: `% excluded` did not define behavior when `total_classified_items == 0`.
- MINOR: The skill eval oracle needed deterministic markers, such as `issue: 61` metadata and an `ace-61-` ID prefix.
- MINOR: The source-ban test pattern set needed to cover unrestricted `jq`, raw manifest reads through shell utilities, and recursive manifest grep.
- MINOR: The plan needed an explicit MkDocs/nav exposure check before creating the contract doc under `docs/case-studies/`.
- MINOR: The filename/header date mismatch and Gemini r2 unavailable disposition needed explicit notes.

Checks run:
- Reviewed the patched #61 plan text after r1 corrections.
- Checked lifecycle transition coverage against invalidation/reset rules.
- Checked metric status vocabulary for zero denominators.
- Checked skill eval and source-ban test oracles for deterministic failure conditions.
- Checked repository MkDocs configuration shape for publication-exposure risk.

Disposition:
- Findings are patched in the plan-review draft.
