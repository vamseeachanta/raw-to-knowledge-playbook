# Plan #72 Review - Codex r1

Scope: `docs/plans/2026-07-02-issue-72-generalize-public-surface-review-selector-snapshot-modes.md`

## Verdict

MAJOR

## Findings

1. MAJOR - The second reproduction proof was not executable as printed. The heredoc only imported `validate_issue_comment_snapshot_file` and included comments, then claimed snapshot errors. Running that body produced no output. The plan needed a real command that builds a synthetic #72 snapshot, calls the validator, and records the actual output.
2. MAJOR - The draft omitted `scripts/ace_public_surface_rules.py` and the contract allow-context path rules. Current allow-context path validation was also hard-coded to `issue-68` and `plan-68`, so non-68 review artifacts using valid forensic allow contexts would fail or pressure later blanket exemptions. The plan needed to add this surface and tests for allowed #72 paths plus issue-64/unlisted rejection.
3. MAJOR - The acceptance criteria promised scans over changed docs, tests, scanner modules, config, review artifacts, and sidecars, but the explicit verification list scanned only the plan document. The plan needed explicit full-surface public-scan commands and review-artifact selector/generic scan commands.

## Verified Checks

- Verified current issue-68 hard-coding in selector, snapshot, URL, and review artifact regex code.
- Executed the printed snapshot reproduction body and confirmed it produced no output.
- Checked current allow-context path rules and contract JSON for `issue-68`/`plan-68` path hard-coding.
- Ran the public-surface scan over the draft plan; it passed for the plan document only.
- Confirmed no `scripts/review/results/2026-07-02-plan-72-*-r1.md` artifacts existed at review time.

## Disposition

Patched in the local draft after r1. R2 review required before `status:plan-review`.
