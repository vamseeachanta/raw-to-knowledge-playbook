review_artifact_role: public_history
normalization_note: denied-token literal substitutions only; provider verdict unchanged

I have enough to render an adversarial verdict. Let me capture the decisive evidence in one insight before writing it up.

`★ Insight ─────────────────────────────────────`
- The parent scanner's `DENIED_TRAVERSAL_PATTERNS` split into two enforcement modes: most patterns require a *manifest token* co-located (`[denied-find-manifest-token]`), but `os\.walk\s*\(`, `\[denied-python-rglob-token]\s*\(`, and `\bls\s+-R\b` fire **unconditionally**. The only escape is `_allowed_denied_traversal_policy_prose`, which demands a hardcoded phrase ("denied-command prose", "unbounded traversal", …) *on the same line*. That makes pass/fail depend on incidental word co-occurrence, not on whether the line is actually dangerous.
- The committed `codex-r18.md:21` and `disagreement-r18.md:25` artifacts contain `[denied-python-walk-token]` and `[denied-recursive-list-token]` and pass the pre-label scan **only** because the same line happens to contain the whitelisted phrase "denied-command prose." That's a coincidence the plan's normalization rules never guarantee.
- The plan articulates a membership-inference threat model for hashes (line 48) but exempts its own share-path disclosures from it by fiat — a gap in the core security argument, not the periphery.
`─────────────────────────────────────────────────`

## Verdict
MAJOR

## Retrieval
- Read the full plan `docs/plans/2026-06-29-issue-51-ace-wave-0-corpus-ledger-routing-firewall-sampling-protocol.md` (lines 1–683, in two pages).
- Read `scripts/review/plan-review-prompt.md` (the review contract).
- Verified file existence for all claimed EXISTS/MISSING paths (docs 01/04/05/07/18/19, all 5 bound skills, `skills/README.md`, the four to-create scripts/artifacts) — all match the plan's Evidence block.
- Verified referenced siblings exist: `docs/plans/ace-share-ingestion-wave-coordination.md`, `scripts/validate_ace_epic_wave_coordination.py`, `tests/test_validate_ace_epic_wave_coordination.py`, the #50/#63 plan files, `docs/plans/README.md`, `skills/validate_skill.py`, `.github/workflows/validate.yml`.
- Ran `gh issue list` — confirmed #50 (`status:plan-approved`, epic), #51 OPEN, and the full #52–#63 child set exist with the claimed roles (#52–#60 = waves 1–9, #61/#62/#63 = cross-wave gates).
- Read `scripts/validate_ace_epic_wave_coordination.py` lines 60–194 and 560–674 — confirmed `--scan-public-path` exists, routes through `validate_public_artifact_paths`, and applies both `PRIVATE_LEAK_PATTERNS` and `DENIED_TRAVERSAL_PATTERNS`; inspected the pattern definitions and the two allow-escapes.
- Confirmed `secrets.token_hex(16)` yields 32 hex chars → matches `pst_[0-9a-f]{32}` (token grammar is internally correct).
- Grepped the #51 plan for every parent-scanner trip pattern (unconditional traversal, cmd+manifest combos, `/mnt…ace`, confidential terms, email, id/name assignments) — plan body is clean.
- Grepped all 12 git-tracked plan-51 review artifacts (r16–r18) for the same patterns; found `[denied-python-walk-token]`/`[denied-recursive-list-token]` in `codex-r18.md:21` and `disagreement-r18.md:25`.

## Findings

1. **The membership-inference threat model the plan invents for hashes is not applied to its own share-path disclosures — an unjustified exemption in the core security argument.** Line 48 makes the governance decision that public ACE artifacts must not publish source pointers because "a raw hash … can still reveal membership in a private ACE corpus." Lines 81–87 and 131–138 then publish seven verbatim share-relative paths — including the private share's *internal index structure* `_cad-index/cad-readability-index.tsv`, `.ace-knowledge/index.db`, and `docs/master-index.jsonl` — in `docs/plans/`, which line 312 explicitly declares "already a public methodology surface under MkDocs and must be treated as published." Line 79 deliberately moves exact size/mtime to "a private/non-public evidence sidecar if needed," yet keeps the *paths* public. The plan never states why a share-internal path is public-safe under its own membership-inference model while a file size is not. For `assets.json`/`master-index.jsonl` the plan offers a "named by the issue body" rationale (lines 30–31), but `.ace-knowledge/index.db` (line 34) and `_cad-index/` (line 32) carry no such justification — they are simply whitelisted. This contradicts the ledger field `share_relative_path_private_only`, which lines 144/285/564 designate private-only and reject in public surfaces.

2. **The Pre-Label fallback scan (line 603) passes on the committed review artifacts only by coincidence, making #51's status gate brittle.** The checklist requires running `validate_ace_epic_wave_coordination.py --scan-public-path` over "every retained plan-51 review artifact." That scanner's `os\.walk\s*\(`, `\[denied-python-rglob-token]\s*\(`, and `\bls\s+-R\b` patterns fire unconditionally (no manifest token required); the only escape, `_allowed_denied_traversal_policy_prose` (validator lines 630–633), requires one of six hardcoded phrases on the *same line*. The committed `scripts/review/results/2026-06-29-plan-51-codex-r18.md:21` and `…-disagreement-r18.md:25` both contain `[denied-python-walk-token]` and `[denied-recursive-list-token]`, and survive the scan only because that line happens to also contain the whitelisted phrase "denied-command prose." The plan's normalization policy (lines 442–451) tokenizes "source-bearing denied-command examples" but nowhere requires co-locating a policy-prose term on every line that mentions an unconditional denied token. Any future cited/committed artifact that mentions `[denied-python-rglob-token](`, `[denied-python-walk-token](`, or `[denied-recursive-list-token]` without that exact phrase will fail the pre-label scan and silently block the status transition.

3. **The downstream nonzero-denominator requirement is under-specified at #51 authoring time and risks fabricated success data.** Line 546 (`test_zero_denominator_is_non_computing_sentinel`) and line 586 require #52–#60 to "reject zero denominator" / carry a "nonzero denominator," but those waves have ingested nothing when #51's forward-reference wave map is authored, so no real measured denominator exists. Line 548 (`test_every_downstream_wave_has_issue_skill_and_validator`) lists the required downstream fields and omits measured numerator/denominator entirely. The plan never resolves whether #52–#60 wave-map rows must populate a denominator: if they must, the #51 fixture has to invent nonzero placeholders (fabricated metrics in a control-plane contract); if they may omit it, line 546's "reject zero" rule is only conditionally meaningful. The contract is ambiguous to an implementer. (MINOR)

4. **`requires_manifest_snapshot_id` is asserted as both "owned by #51" and "consumed from the parent coordination doc," and #51 forbids itself from modifying the parent's matrix — a latent two-source-of-truth hazard.** Lines 248–255 say `requires_manifest_snapshot_id` "is a per-issue sampling boolean owned by #51," while `docs/plans/ace-share-ingestion-wave-coordination.md` "also carries a Manifest gate text column" that #51 "consumes and scans … but does not modify." Test `test_manifest_gate_boolean_matches_coordination_text` (line 547) requires the two to agree, but with #51 unable to edit the parent and the parent owned under #50, any future drift in the parent's Manifest-gate column fails #51's validator with no in-#51 remediation path. The plan should name which artifact is authoritative on disagreement. (MINOR)

## Blockers
- **Finding 1** — resolve before implementation. Either (a) state and enforce the governing rule that distinguishes the seven public-safe metadata/index paths from `share_relative_path_private_only` per-source paths (and justify it against the line-48 membership-inference model), or (b) move the share-internal paths (`.ace-knowledge/index.db`, `_cad-index/*`) to the same private evidence sidecar already used for sizes/mtimes. As written, the plan's publication firewall publishes the private share's internal structure while its own threat model forbids exactly that kind of membership disclosure.
- **Finding 2** — resolve before relying on the Pre-Label checklist. Make the review-artifact scan robust to unconditional denied-token literals (e.g., require normalization to tokenize `[denied-python-walk-token](`/`[denied-python-rglob-token](`/`[denied-recursive-list-token]` mentions, or add a deterministic artifact-scoped allow-context), so the status gate does not depend on incidental phrase co-occurrence.

Findings 3 and 4 are MINOR and need not block implementation, but both should be clarified in the contract text to avoid implementer ambiguity.
