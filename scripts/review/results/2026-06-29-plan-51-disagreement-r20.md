review_artifact_role: public_history
normalization_note: denied-token literal substitutions only; provider verdict unchanged

# Disagreement report — plan #51 (2026-06-29)

## Verdicts

| Provider | Verdict |
|---|---|
| claude | MAJOR |
| codex | MAJOR |
| gemini | UNAVAILABLE (gemini CLI failed, rc=41: Error authenticating: FatalAuthenticationError: Manual authorization is required but the current session is non-interactive. Please run the Gemini CLI in an interactive terminal to log in, provide a GEMINI_API_KEY, or ensure Application Default Credentials are configured.     at initOauthClient (file:///home/vamsee/.npm-global/lib/node_modules/@google/gemini-cli/bundle/chunk-VLV2BYPM.js:269720:13)) |

## Findings unique to each provider

A finding is 'unique to X' if its text appears in X's artifact but not
verbatim in any other provider's artifact.

### claude

- **Scope vastly exceeds what issue #51 asks, pulling #61/#63-owned work forward (root cause of non-convergence).** Issue #51's acceptance criteria (live body) ask for: a 9-field ledger (source path, extension family, content class, sensitivity, target store, extraction level, verification state, expected yield, exclusion reason), 4-way routing, an issue/skill map, bounded sampling, and a closeout rule. The plan instead specifies a 33-field ledger (§Pseudocode lines 148–157), a CSPRNG token generator (`scripts/ace_public_tokens.py`), a repo-wide `sha256` policy sweep that edits `docs/07`, `docs/19`, the parent epic plan #50, and the #63 plan (Files to Change lines 497–503), a shared JSON contract for #63 to consume, review-artifact normalization/role machinery, and a degraded-quorum review policy — across ~25 files, 5 new modules, and 40+ TDD tests for a wave explicitly carrying "0% content ingestion" (§Complexity line 705). By the plan's own ownership boundaries (lines 50, 55, 395–411) durable token storage is #61's and public certification is #63's, yet #51 still implements the token grammar, the shared contract, and edits sibling/parent plans. This is the most likely reason the plan has not converged.
- **The plan has never passed a clean review round — 19 consecutive MAJORs from both active providers.** §Adversarial Review Summary lines 636–687 show Claude and Codex returning MAJOR (or, once, MINOR for Claude r18) in every round r3–r19, with the Overall Result (line 688) stating "r19 returned MAJOR from both active providers… This plan remains draft-only and must not move to `status:plan-review`." This review continues the pattern (Findings 1, 4). A spec that surfaces fresh MAJORs after 19 patch cycles is evidence the contract is too large to verify, not a contract one round away from done.
- **The plan's own T3 review gate is structurally unsatisfiable in this environment.** Every round r3–r19 records "Gemini UNAVAILABLE… auth failed with rc=41" (lines 638–686), and the degraded-quorum policy (lines 450–461 and Pre-Label checklist line 627) states "Gemini noninteractive auth/config failure is not a quota outage and does not automatically authorize downgrade; `status:plan-review` is blocked until Gemini is restored or the user explicitly approves a one-round degraded review quorum." Since Gemini is consistently broken here, the plan cannot reach `status:plan-review` through its documented quorum without an out-of-band user waiver that is not a plan deliverable. This is an acknowledged but unresolved blocker to the plan's stated goal.
- **The coordination doc's #51 review pointer is stale relative to committed evidence.** `docs/plans/ace-share-ingestion-wave-coordination.md` line 38 records "Claude: …plan-51-claude-r15.md; Codex: …codex-r15.md; Gemini: unavailable: auth rc=41 r15", but committed artifacts run through r19 (`git ls-files` shows tracked claude/codex/gemini/disagreement r16–r19) and the plan's own summary covers r19. The plan defers updating this to "during plan-review" (Files to Change line 505), yet §Parent #50 Pre-Plan-Review Support (lines 519–521) and the Pre-Label checklist (line 626) require the coordination support kept green/aligned *before* plan-review. As written, the status-evidence trail is internally inconsistent now.
- **`config/ace-public-output-contract.json` is a hard cross-issue ordering dependency that will leave #63's already-written tests red.** The #63 plan already declares tests `test_loads_shared_public_output_contract` and `test_allows_opaque_public_source_tokens` keyed on `config/ace-public-output-contract.json` (#63 plan lines 135, 137, 146), but that file is MISSING (verified) and is created by #51 (Files to Change line 489). The plan describes #63 as "consuming" the config but never flags that #63's committed acceptance tests cannot pass until #51 merges. Minor sequencing gap, but it undercuts the "modify #63 only if drift is found" framing (line 503): the #63 plan already references the config as authoritative, so that work item is largely already done — inflating the plan's apparent remaining scope.
- **Minor field-name drift between the schema definition and acceptance criteria.** §Pseudocode line 157 names `skill_group` and `method_issues`; Acceptance Criteria line 578 names "skill" (not `skill_group`) and omits `method_issues`. For a plan whose entire value is mechanically-enforceable field exactness, a validator written to one spelling and a reviewer checking the other will disagree. Reconcile to one canonical token set.

### codex

- Plan self-scan scope is contradictory and currently self-blocking. `docs/plans/2026-06-29-issue-51...md:298-307` and `:395-405` include test files in #51 public-surface scan scope, and `:554` says the scan input includes “parent coordination validator/tests.” But `:595` says “Test source files run through unit tests and restricted bad-fixture harnesses rather than the generic public-text scan.” Empirically, scanning `tests/test_validate_ace_epic_wave_coordination.py` with the parent fallback produces 26 denials because the test file intentionally contains denied commands and private-field fixtures at `tests/test_validate_ace_epic_wave_coordination.py:617-635` and `:826-830`. The plan must choose one rule and encode it; as written, either CI self-blocks or declared scan coverage is false.
- The pre-label fallback scanner cannot enforce the raw digest/table-row policy the plan relies on before #51 exists. The plan says table rows containing real values and source-like raw digests are rejected everywhere (`docs/plans/2026-06-29-issue-51...md:357-360`, `:584`, `:595`) and says pre-label public surfaces are scanned by `scripts/validate_ace_epic_wave_coordination.py --scan-public-path` before the #51 validator exists (`:623`). But the fallback only checks key/value assignment regexes in `scripts/validate_ace_epic_wave_coordination.py:99-102`; it passed both `source hash: <64hex>` and `| source_sha256 | <64hex> |` synthetic fixtures. That leaves a status-transition path where forbidden hash evidence can be posted and still pass the required fallback scan.
- The plan omits the inherited legal/security closeout gate. The user-provided AGENTS hard gate says code must pass `scripts/legal/legal-sanity-scan.sh`, but the plan’s closeout acceptance commands list only the wave validator, unit tests, workflow invocation, ACE_SHARE_ROOT-unset behavior, and `skills/validate_skill.py` at `docs/plans/2026-06-29-issue-51...md:609-613`. The script is also absent in this repo. Since #51 creates scripts, config, fixtures, and workflow changes, the plan needs either to add the legal scan artifact/gate or explicitly resolve the missing required gate before implementation.

### gemini

- (none)
