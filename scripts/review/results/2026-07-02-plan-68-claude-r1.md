# #68 Formal Plan Review - Claude r1

Reviewer: Claude CLI
Scope: `docs/plans/2026-06-30-issue-68-ace-public-surface-self-scan-control-plane.md`
Mode: read-only adversarial provider review
Verdict: MINOR

## Findings

1. MINOR - The plan did not yet record the r2 preparatory review round even though r2 artifacts existed and one r2 lane had returned MAJOR before later fixes. Required fix: update the header, artifact map, and summary table to record r2 disposition and the final review round used as transition evidence.

2. MINOR - The #66 import boundary omitted `forbidden_request_keys` and `fixture_set_ids`. Required fix: add both to the imported/drift-checked field list and to the key-denial test matrix.

3. MINOR - The allow-context table used HTML-comment sentinels for paths that included strict JSON and Python source. Required fix: define per-file-type embedding or exclude JSON/Python from sentinel contexts.

4. MINOR - The plan-review transition checklist required scanning planned/refetched evidence but did not name which existing scanner performs that pre-implementation check or how scratch evidence relates to the reviewed remote head. Required fix: name existing scanners and commit/push ordering.

5. MINOR - The coordination fallback unit test can become vacuous again if the row is reworded. Required fix: add a transition-commit test assertion that the replacement fixture actually changed.

6. MINOR - `label_evidence_refetch` was listed as a snapshot phase without validation semantics. Required fix: remove it or define pairing rules.

## Verified Checks

- Verified the r2 coordination-row fix and targeted schema unit test.
- Verified remaining stale #69 wording was historical only.
- Verified provider selector enum was closed.
- Checked #66 contract fields and identified missing `forbidden_request_keys` / `fixture_set_ids` import coverage.
- Ran the legal scan over tracked public surfaces and explicit #68 plan/r2 artifacts.
- Checked the #68 status surfaces remained non-authorizing.

## Limitations

- Read-only review. No files edited, committed, pushed, or GitHub labels/comments mutated.
- Did not run full CI-equivalent or inspect every r1 subagent artifact line-by-line.
