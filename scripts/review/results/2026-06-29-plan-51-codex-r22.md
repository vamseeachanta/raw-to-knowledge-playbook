review_artifact_role: public_history
normalization_note: denied-token/private-field example substitutions only; provider verdict unchanged

## Verdict
MAJOR

## Retrieval
- Read `docs/plans/2026-06-29-issue-51-ace-wave-0-corpus-ledger-routing-firewall-sampling-protocol.md` lines 1-766, with focused checks on lines 190-430, 430-690, and 687-760.
- Read `scripts/validate_ace_epic_wave_coordination.py` lines 1-260, 560-730; grep’d for `--scan-public-path`, private-field patterns, digest patterns, metadata/source-path handling, `wave_class`, and review-verdict parsing.
- Read `tests/test_validate_ace_epic_wave_coordination.py` lines 1-240.
- Read `docs/plans/ace-share-ingestion-wave-coordination.md` lines 1-130.
- Read `docs/plans/2026-06-29-issue-63-ace-public-output-redaction-and-identifier-canary.md` lines 1-170.
- Read `.github/workflows/validate.yml` lines 1-44.
- Grep’d `docs/**/*.md`, `skills/**/*.md`, top-level skill markdown, and relevant plans for `sha256`, `source hash`, `provenance pointer`, `source_sha256`, `private_lookup_key`, and `public_source_token`.
- Grep’d `scripts/review/results/2026-06-29-plan-51-*.md` for `review_artifact_role`, verdict headings, and reviewed-draft identity fields.
- Checked `git status --short`, `git ls-files scripts/review/results/2026-06-29-plan-51-*`, and file existence for planned artifacts/config/legal/test fixture paths.
- Ran `UV_CACHE_DIR=.claude/state/uv-cache uv run python scripts/validate_ace_epic_wave_coordination.py --scan-public-path docs/plans/2026-06-29-issue-51-ace-wave-0-corpus-ledger-routing-firewall-sampling-protocol.md`.
- Ran synthetic `--scan-public-path` probes against the parent fallback scanner for private-field Markdown table rows and unlisted `<ACE_METADATA_EVIDENCE_EXAMPLE>` metadata evidence rows.
- Ran `UV_CACHE_DIR=.claude/state/uv-cache uv run python -m unittest tests.test_validate_ace_epic_wave_coordination`, `UV_CACHE_DIR=.claude/state/uv-cache uv run skills/validate_skill.py`, and `git diff --check`.
- Fetched live GitHub issue bodies for #51 and #63 via `gh issue view --json number,title,state,labels,body,url`.

## Findings
1. Plan §Pre-Label Evidence Checklist line 678 relies on `scripts/validate_ace_epic_wave_coordination.py --scan-public-path` before the #51 scanner exists, and says that fallback “must apply ... assigned private-source-field/source-hash/token patterns.” The current fallback only catches colon/equals assignments in `scripts/validate_ace_epic_wave_coordination.py:114-120`; a synthetic public file containing Markdown rows <PRIVATE_FIELD_TABLE_ROW_EXAMPLES> passed with `PASS: ACE epic wave coordination valid`. This contradicts the plan’s own field-name policy at lines 385-386 and acceptance line 647, which say table rows/assigned private source fields are rejected before comment/status transition.

2. Plan lines 36, 358-366, 595, and 651-652 make the seven fixed metadata evidence paths the only allowed public `<ROOT_ABSTRACTION_SLASH_PATH>` rows. The pre-label scanner path at line 678 cannot enforce that: a synthetic row `EXISTS <ACE_METADATA_EVIDENCE_EXAMPLE> type=file details=withheld_public` passed the parent fallback scanner. That leaves a pre-implementation status-transition route where an unlisted item-level/private share path can be posted or scanned as “safe” before `scripts/validate_ace_wave0_control_plane.py` exists.

3. The plan’s embedded review status is stale and still says the current plan “remains draft-only” because r21 returned active-provider MAJORs. Plan lines 745-749 list Claude r21 MAJOR, Codex r21 MAJOR, Gemini UNAVAILABLE, and state that the next patch must address r21 findings. The current plan text now includes fixes for several named r21 blockers, e.g. private-field placeholders at lines 213-225, legal scan creation at lines 484-495 and 536-537, and tracked/local-transient review artifact scope at lines 479-483. The summary is no longer an accurate status artifact for the current draft.

## Blockers
- Finding 1: harden the parent fallback scanner or change the pre-label gate so private-field Markdown table rows cannot pass before #51 exists.
- Finding 2: harden the parent fallback scanner or remove reliance on it for fixed metadata-path enforcement before plan-review.
- Finding 3 must be corrected before using the plan as status evidence, but it is secondary to the scanner gaps.
