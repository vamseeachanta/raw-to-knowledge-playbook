# Plan Review: #70 r1 Claude Lane

Verdict: MAJOR

## Findings

- MAJOR: The plan's operational provenance boundary was path-only and overclaimed "operational sampling." The #62 validator treats `artifacts/ace-manifest-freshness/` and `tests/fixtures/ace-manifest-freshness/` as allowed roots; the plan distinguished fixture vs operational mainly by directory while also disclaiming real ACE source reads during planning. Required fix: state that a reviewed #62-produced operational artifact is an implementation precondition, or scope #70 to fail-closed wiring until that artifact exists.
- MAJOR: The trusted registry was framed as a CI-safe trust anchor, but CI could only check consistency among committed metadata. The plan did not separate human-vetted allowlist review from ordinary artifact writes. Required fix: reframe the registry as a human-reviewed allowlist, add write-control/reviewer checklist requirements, and make the boundary meaningful.
- MINOR: The pseudocode double-loaded the artifact: #62 pointer validation loads and validates it, then #70 loaded it again for authorization status. Required fix: derive authorization from the same parsed record used for validation.
- MINOR: The plan duplicated #62 minimal pointer validation with a separate self-attestation check, creating divergent error semantics. Required fix: use #62 validation as the authoritative minimality check and map errors consistently.
- MINOR: Draft review artifact references were listed while still missing. Required fix: do not advance to `status:plan-review` until actual artifacts exist and are recorded.

## Checks

- Read `docs/plans/2026-07-01-issue-70-ace-67-62-manifest-evidence-contract-integration.md`.
- Read `scripts/ace_manifest_freshness_operational.py` and verified `validate_request_pointer`, `_validate_pointer_artifact`, `_valid_artifact_ref`, `_safe_artifact_path`, and `_validate_authorization`.
- Verified #67 approval marker and implementation artifacts are missing.
- Verified #67 plan defers the first operational allow-path to #70.
- Verified #70 is not in the #65 wave-0 split registry.
