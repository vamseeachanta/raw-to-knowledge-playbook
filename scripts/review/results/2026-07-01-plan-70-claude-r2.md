# Plan Review: #70 r2 Claude Lane

Verdict: MINOR

## Findings

- MINOR: The plan said #70 would use #62 `validate_request_pointer` while also requiring a single authorization-time load/parse. The current #62 function opens and parses the artifact internally and returns only errors. Required fix: clarify that #70 will import #62 pointer constants and `validate_operational_evidence`, use one #70 authorization-time byte read/parse, and avoid relying on `validate_request_pointer` as the authorization read unless a parse-returning #62 helper is added through an approved follow-on.
- MINOR: The artifact integrity digest definition mixed "canonical" and "same bytes." Required fix: pin the algorithm and input bytes, such as raw-byte SHA-256 with no JSON reserialization.
- MINOR: The legal/security closeout gate hardcoded `scripts/legal/legal-sanity-scan.sh`, but #69 has not implemented the artifact yet. Required fix: treat the path as provisional and defer to #69's approved artifact path.
- INFORMATIONAL: Public-scan workflow mechanics should state that each #70 public path is appended as an explicit `--scan-public-path` argument to the parent scanner.

## Checks

- Read `docs/plans/2026-07-01-issue-70-ace-67-62-manifest-evidence-contract-integration.md`.
- Read `scripts/ace_manifest_freshness_operational.py`.
- Read `config/ace-manifest-evidence-contract.json`.
- Read `tests/fixtures/ace-manifest-freshness/valid-operational-evidence.json`.
- Read `.github/workflows/validate.yml`.
- Read `scripts/validate_ace_wave0_schema_contract.py`.
- Checked `scripts/legal`, `artifacts/ace-manifest-freshness/`, and scanner/legal path availability.
