I now have enough verified evidence. Every correctness-critical claim in the plan checks out against disk, and — importantly — the current body text has been patched to resolve all r8 findings. Let me give the educational context, then the review.

`★ Insight ─────────────────────────────────────`
- This is a **self-referential governance artifact**: the plan's "product" is a scanner that judges whether example commands in committed docs are runnable-and-unbounded. The adversarial payoff isn't "will it crash" but "do the plan's asserted enums match `artifacts/ace-wave0-ledger-schema.json`, and does its own text survive its own scanner." Both are falsifiable against files on disk.
- The plan's central safety trick is genuinely clever and I verified it empirically: `#65` private terms (`source_id`, `source_sha256`, …) must appear in `#67` fixtures, and the parent scanner's `PRIVATE_SOURCE_FIELD_ASSIGNMENT_PATTERNS` (line 125) fires on `field[:=]value` — but a JSON **array value** (`["source_id", …]`) is delimited by `,`/`]`, never `:`/`=`, so it slips the pattern. The plan's constraint "private terms may appear only as `term_refs` values" (line 253) is precisely calibrated to that regex, not arbitrary.
- A recurring failure mode in fail-closed designs is that the **deny path is exhaustively tested while the allow path is never exercised** — the plan resolves this the correct way, by explicitly *deferring* the operational allow-path to `#70` rather than pretending to test it.
`─────────────────────────────────────────────────`

## Verdict
MINOR

Every correctness-critical claim I could falsify is **true**, and the current body resolves each r8 MAJOR finding cited in its own Adversarial Review Summary. What remains are specification/hygiene ambiguities, none of which block implementation.

## Retrieval
- Read the full plan `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md` (lines 1–549).
- File existence sweep: confirmed `EXISTS` for the five parent artifacts, `docs/plans/README.md`, coordination ledger, `.github/workflows/validate.yml`, `.planning/plan-approved/65.md`, `skills/validate_skill.py`, all six named skill docs incl. `skills/source-extraction-coverage/SKILL.md`; confirmed `MISSING` for `config/`, contract, classifier, validator, tests, fixtures, and `.planning/plan-approved/67.md` — all matching the plan's Evidence block (lines 79–91).
- Confirmed `scripts/legal/legal-sanity-scan.sh` is **absent** — the plan's `NO_LEGAL_SCAN_SCRIPT` conditional-closeout block (line 482) is honest.
- `artifacts/ace-wave0-ledger-schema.json`: verified `route_targets`/`logical_target_stores`/`route_store_matrix` (lines 12–28), `metadata_only → metadata_ledger_store` (line 27), `bound_skill_groups` = exactly the five named (lines 160–166, no `source-extraction-coverage`), `canonical_wave_registry` issues 51–63 with `wave_class` and `requires_manifest_snapshot_id` flags exactly as the plan's Request Class Mapping asserts (#52–60 ingestion_wave/true; #61 storage_lifecycle_gate/false; #62 manifest_freshness_gate/false; #63 public_canary_gate/false), and `#64`/`#67` absent from the registry. Confirmed `private_source_field_terms` (lines 115–121) is the real key behind "#65 private schema term."
- `docs/plans/ace-share-ingestion-wave-coordination.md` lines 16–22: six manifest keys and 200-rows / 25-files / 1048576-bytes caps match verbatim, incl. the "bounded precomputed sidecar" byte-cap exception.
- `scripts/validate_ace_epic_wave_coordination.py`: verified `--scan-public-path` argparse (lines 782–799); the pattern groups actually applied to committed files by `validate_public_artifact_paths` (lines 715–749): `DENIED_TRAVERSAL_PATTERNS`, `PRIVATE_LEAK_PATTERNS`, `PRIVATE_SOURCE_FIELD_ASSIGNMENT_PATTERNS`, `SOURCE_LIKE_RAW_DIGEST_PATTERNS`, ACE source-root token evidence check; and the prose allowlist `DENIED_TRAVERSAL_POLICY_PROSE_TERMS` (lines 161–166).
- **Empirical:** ran `uv run python scripts/validate_ace_epic_wave_coordination.py --scan-public-path <the #67 plan file>` → `PASS`, exit 0. The current plan body does **not** self-block.
- `.github/workflows/validate.yml` lines 19–43: confirmed CI form `uv run python scripts/…` for validators and `uv run skills/validate_skill.py` (no `python`) for skill validation.
- Read `scripts/review/results/2026-06-30-plan-67-claude-r8.md` and `-codex-r8.md` in full; cross-checked each of their findings against the current body.
- `gh issue view 67`: confirmed OPEN, labels `strengthening,lane:codex,priority:high`; read the body's skill-binding and method-issue lines.

## Findings

1. **The Adversarial Review Summary reports a blanket "Overall result: MAJOR" (line 532) that the body has already superseded, with no per-finding resolved/open status.** I verified that every r8 finding is patched in the current text: Codex r8 F1 (output-shape under-specified) → now closed by §Output Shape and Route/Store Boundary (lines 204–214) + `test_output_shape_route_store_is_metadata_only` (line 401); Codex r8 F2 (no `blocked_pending_62_contract` test) → `test_blocked_pending_62_contract_is_failure_result_not_authorization` (line 410) + Runtime blocked fixture (line 240); Claude r8 F1/F2 (overstated allow-path) → Deliverable rewritten to defer the operational allow-path to `#70` (line 154); Claude r8 F3 (hand-maintained scan drift) → derived-manifest pseudocode (lines 350–361) + `test_review_artifact_discovery_blocks_unscanned_rerun_artifacts` (line 440); Claude r8 F5 → gate rule now states the effective Claude-AND-Codex rule plainly (line 499); Claude r8 F6 → acceptance line 480 now matches CI's `uv run skills/validate_skill.py`. The risk: an approver reading only the summary sees "MAJOR" and cannot tell these are resolved. The summary should mark r8 rows as *patched, pending re-review* rather than leave a standing MAJOR verdict against text that no longer contains the defects. **(MINOR — documentation hygiene, not a code defect.)**

2. **The Fixture Payload Contract under-lists what `good-request.json` must contain, contradicting `required_sampling_fields` and its own required-field test.** Line 238 gives good-request's "Required target fields" as only `target_issue=67`, `request_class=metadata_only_fixture`, `fixture_scope=firewall_validator_self_check`. But `required_sampling_fields` (line 169) requires *every* request to also carry manifest source, seed, sort rule, per-bucket cap, max files, max bytes, output shape, route target, and logical target store — and `test_bounded_sampling_fields_are_required` (line 400) asserts that missing any of "manifest source, seed, sort rule, per-bucket cap, max files, max bytes, request class, or output shape fails." An implementer building `good-request.json` from the Fixture Payload row literally would produce a fixture missing ~9 required fields that then **fails the plan's own validator**. Fix: either state that the column lists only the *distinguishing* fields, or enumerate the full required set for the happy-path fixture. **(MINOR — resolvable by cross-reference, but an implementation trap.)**

3. **The reframed private-read guard (`test_validator_refuses_private_reads_when_ace_share_root_is_set`, line 413; acceptance line 469) still risks being a caller-less guard.** The plan states `#67` does zero source-root reads and "will pass with `ACE_SHARE_ROOT` unset" (line 46). The r8-patched test now calls a *named* helper directly and asserts it returns `ACE_SOURCE_ROOT_ACCESS_FORBIDDEN` — an improvement over the vacuous version. But because the validator has no real code path that touches the root in this unit, the helper has **no invocation site**; the test proves the helper returns the sentinel in isolation, not that any real operation is actually gated by it. To make the boundary non-vacuous, specify that the helper wraps a representative access primitive and add a test that a source-root-touching operation *routes through* it. **(MINOR — residual of Claude r8 F4, which was itself rated MINOR.)**

4. **The plan's characterization of the live `#67` skill bindings (line 29) is misleading about set membership, and the issue's "Method issues: #1, #12" binding is not captured.** The live issue body names skill groups: `content-triage-and-exclusion, source-extraction-coverage, format-coverage-ledger, public-private-routing, adversarial-verify-loop` — a five-set that **omits `page-shape-contract`** (which is in `#65` `bound_skill_groups`) and **includes `source-extraction-coverage`** (which is not). The plan frames this as the issue "additionally requires `source-extraction-coverage`" on top of the `#65` five (line 29), implying issue-set ⊇ #65-set, which is false — the issue *drops* `page-shape-contract`. Functionally the Artifact Map (lines 114–115) scans all six, so coverage is fine, but the narrative misstates the relationship. Separately, the issue body's `Method issues: #1, #12` are never surfaced in the plan even though the Pseudocode validates "method issue bindings" (line 293). **(MINOR — verify the reconciliation prose and record the method-issue binding.)**

5. **The review gate is structurally a two-of-two unanimity that is one Gemini-recovery away from changing meaning, and the plan does not say what happens if Gemini becomes available.** Line 499 correctly states the *current* effective rule (both Claude and Codex no-MAJOR) given Gemini's `IneligibleTierError`. But acceptance criterion line 450 phrases it as "at least two usable provider results and no usable provider returns MAJOR." If Gemini ever returns a usable MAJOR, the same wording flips from "need both of two" to "need all-clear of three" without any plan change — a silent tightening. State whether Gemini recovery raises the floor to three usable providers or the gate stays pinned to Claude+Codex. **(MINOR.)**

I reviewed the plan against: file/directory existence for all Artifact-Map and Files-to-Change paths; every `#65` enum/registry claim (route/store, caps, `bound_skill_groups`, `canonical_wave_registry`, `requires_manifest_snapshot_id`); the coordination-ledger manifest keys and caps; the parent scanner's `--scan-public-path` CLI and its four committed-file pattern groups; an **empirical** self-block run of the parent scanner over the plan file; the live `#67` issue body; and a per-finding trace of all r8 MAJORs against the current text. I did **not** find a substantiable new MAJOR: the core safety property — that no request in this approval unit can authorize operational sampling (downstream fails closed, `#67` fixtures "cannot authorize operational sampling," all output is `metadata_only_request_record`) — holds under each bypass I tried (target_issue spoofing to `#67`, `control_plane_proof` abuse, fixture-scope misuse against `#52–60`).

## Blockers
None. All five findings are MINOR. Per the plan's own gate rule (line 499), the plan may proceed to a fresh review round: this Claude review returns **no MAJOR**, so if the paired Codex review of the same text also returns no-MAJOR, the two-usable-provider / no-MAJOR gate is satisfied. Recommend folding findings 1–4 into the same patch cycle before flipping the status label, since 2 is an implementation trap and 1/4 are accuracy issues in the plan's own self-description.
