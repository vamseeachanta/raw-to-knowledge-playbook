# #68 Formal Plan Review - Claude r2

Reviewer: Claude CLI
Scope: `docs/plans/2026-06-30-issue-68-ace-public-surface-self-scan-control-plane.md`
Mode: read-only adversarial provider review
Verdict: MAJOR

## Findings

1. MAJOR - The pre-approval label-evidence workflow still relied on a temporary body outside the repository while also requiring the #69 legal scan. The existing legal scanner only accepts repository-relative targets, so the workflow was not executable as written. Required fix: use a classified repo-relative scratch path or another existing scanner that can legally scan the body.

2. MAJOR - The plan claimed the preparatory subagent lanes reached APPROVE by r3, but the r3 artifacts were not retained or mapped. Required fix: add the actual r3 artifacts or remove the APPROVE-by-r3 claims.

3. MINOR - Current status surfaces drifted between `draft` and `blocked-draft`. Required fix: normalize the current non-authorizing state before the next provider round.

## Gate Effect

This r2 wave blocks `status:plan-review`. A fresh same-round provider review is required after patching.

## Limitations

- Read-only review. No files edited, committed, pushed, or GitHub labels/comments mutated.
- Gemini was unavailable in the same formal provider wave.
