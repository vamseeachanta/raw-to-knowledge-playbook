---
review_artifact_role: public_history
normalization_note: Local absolute runtime paths and non-ASCII formatting, if present, are tokenized in this public copy; authoritative verdict values preserved.
---

# Disagreement report - plan #51 (2026-06-30)

## Verdicts

| Provider | Verdict |
|---|---|
| claude | MAJOR |
| codex | MAJOR |
| gemini | UNAVAILABLE (gemini CLI failed, rc=41: Error authenticating: FatalAuthenticationError: Manual authorization is required but the current session is non-interactive. Please run the Gemini CLI in an interactive terminal to log in, provide a GEMINI_API_KEY, or ensure Application Default Credentials are configured.     at initOauthClient (<LOCAL_FILE_URI>)) |

## Findings unique to each provider

A finding is 'unique to X' if its text appears in X's artifact but not
verbatim in any other provider's artifact.

### claude

- **The plan is self-declared not-ready; its own review log shows 25 consecutive non-converged rounds.** The Adversarial Review Summary (lines 671-739) records r3 through r25 with *every* round returning MAJOR from at least one active provider, and the "Overall result" (line 741) states verbatim: "This plan remains draft-only and must not move to `status:plan-review`." Per the review prompt's rule 3 (default to non-approve; APPROVE only after affirmatively clearing every correctness-critical gap), an APPROVE is impossible - the plan itself asserts unresolved blockers. Nothing in the current draft resolves the r25 Claude/Codex MAJORs (they are addressed "intended to address," not verified cleared).
- **Scope non-convergence is an unresolved blocker the plan explicitly defers to the user.** Line 741: "the user must decide whether to split #51 further or explicitly accept the broad bundled scope before any implementation approval request." A single "wave 0 control plane" issue bundles: ledger schema (28 fields), a closed route enum + route->store matrix, verification-state enum, opaque CSPRNG token grammar + generator module, a generic public-surface scanner, a sampling/executable-context firewall, a legal deny-list + bash wrapper, a degraded-quorum policy, #63 plan reconciliation, and parent #50 validator support - materializing as ~13 new files (lines 516-529) and 27 TDD tests (lines 557-599). Claude r20/r23/r24/r25 and Codex repeatedly raised this; the plan never makes the decision, it records the open question. An open scope decision of this magnitude is a BLOCKER, not a risk note.
- **The central safety mechanism - the self-scanner over "this plan" - is specified in self-referential prose, not a machine-checkable contract.** Lines 314-328 and 391-403 require `scripts/ace_public_surface_scan.py` to scan "this plan" and "every tracked/cited plan-51 review artifact" for denied-command tokens, path-bearing `ACE_SHARE_ROOT` rows, private field names, and the `pst_` grammar. But this plan *contains all of those as definitional content* (e.g. `os.walk`, `.rglob`, `.read_text`, `python -c`, pipes at lines 298-299; `<METADATA_EVIDENCE_SHAPE>` rows at lines 82-88; the `pst_[0-9a-f]{32}` grammar at line 185). The plan's only defense is context discrimination - "fixed methodology headings, this grammar paragraph, the TDD/acceptance denied-token catalog rows" (lines 304-306) - defined entirely by self-reference. No parseable anchor scheme (delimiter, fenced block id, heading-path allowlist) is specified that an implementer could deterministically build. Claude r6/r7/r16/r25 flagged "anchor-window self-contradictions" and demanded "unique ordered allow-window anchors"; the resolution remains natural-language. This is a MAJOR implementability gap: the scanner either over-blocks (fails its own plan) or under-blocks (lets real leaks through), and the plan gives no mechanical rule to choose.
- **The T3 review quorum is structurally unsatisfiable and the escape hatch is not self-clearable.** The Review Summary shows Gemini `UNAVAILABLE` (auth rc=41) in all 23 logged rounds (e.g. lines 673, 739). The plan defines a degraded-quorum policy (lines 485-496, 750) but conditions it on the user "explicitly approv[ing] a one-round degraded quorum after seeing the auth-failure evidence." The plan therefore cannot reach its own stated next state (`status:plan-review`) without an external, human action that has not occurred. This is a hard transition blocker; flagging it is required even though the root cause is environmental rather than a textual plan defect.
- **Acceptance criteria are compound, defeating atomic pass/fail.** Single checkboxes bundle many independent assertions: line 605 (one checkbox lists ~20 required ledger fields), line 609 (structural review evidence + fail-closed routing + reviewer!=author + #63 ownership disclaimer), line 613 (placeholder grammar + ~7 distinct rejection classes + git-SHA exceptions), line 617 (the full wave_class matrix + registered-row enforcement), line 620 (10+ deny-fixture classes + prose-allowance rules). Claude r23 explicitly required "decompose compound acceptance criteria"; the criteria remain compound. A reviewer cannot mark such a checkbox without hidden partial-failure. MINOR-to-MAJOR depending on how the checkboxes gate closeout.
- **`.legal-deny-list.yaml` parsed by `json` is a maintenance footgun.** Lines 451-454/521 keep the `.yaml` extension but parse the file with Python stdlib `json` and "reject anchors, aliases, block scalars, tabs, comments." JSON-in-a-`.yaml`-file is valid (YAML superset JSON), so the gate works - but any future maintainer editing it with conventional YAML idioms (unquoted keys, `#` comments) silently breaks `json.load`, and the legal/security closeout then fails for a confusing, non-obvious reason. The naming actively misleads. MINOR.
- **Two correctness-critical preconditions are unverifiable at review time and one was last reported failing.** The Pre-Label Checklist (lines 652-653) requires the *live* #51 and #63 GitHub issue bodies to be sanitized/reconciled; Codex r23 (line 732) reported the live #51 issue-body fallback scan *failing*. These cannot be verified from the repo (no `gh`/network in CI by design), so the plan's readiness rests on an external state last logged as red. The plan discloses this as an operator step, but it remains an open precondition, not a cleared one.

### codex

- `docs/plans/2026-06-29-issue-51-ace-wave-0-corpus-ledger-routing-firewall-sampling-protocol.md` lines 497-506 require committed review artifacts to be normalized public copies when provider output contains paths/identifiers, but tracked public-history artifacts still expose local runtime file URIs. Example: `scripts/review/results/2026-06-29-plan-51-gemini-r16.md:4` and `scripts/review/results/2026-06-29-plan-51-gemini-r17.md:4` contain `<LOCAL_FILE_URI>`. `rg` found the same class in multiple tracked Gemini/disagreement artifacts. This violates the plan's own normalization boundary before status transition.
- The pre-label fallback scanner does not catch Finding 1. The plan says the fallback scan must cover tracked/cited review artifacts before `status:plan-review` (`docs/plans/2026-06-29-issue-51-...md` lines 654-658), but `scripts/validate_ace_epic_wave_coordination.py` lines 106-113 only cover `<LOCAL_MOUNT_PATTERN>`, emails, client/customer/project assignments, confidentiality terms, SSNs, and phones. It has no `file://` or `<LOCAL_HOME_PATTERN>` host-path rule. Running it against Gemini r16/r17 returned `PASS`.
- The plan is self-declared non-transitionable. Line 10 says final no-MAJOR review evidence is still pending; lines 488-496 block `status:plan-review` on Gemini restoration or explicit degraded-quorum user approval plus fresh no-MAJOR active-provider verdicts; line 741 says r25 still returned Claude MAJOR, Codex MAJOR, Gemini UNAVAILABLE and that the plan "must not move to `status:plan-review`." That is a current blocker, not a historical note.
- The split-vs-bundled scope decision is still unresolved. Line 741 says the user must decide whether to split #51 further or explicitly accept the broad bundled scope before implementation approval. The Files to Change table still spans the control-plane artifact, four Python modules, legal scanner/config, workflow, five test modules, fixture trees, parent/#63 planning surfaces, issue metadata verification, and five skill edits (`docs/plans/2026-06-29-issue-51-...md` lines 516-543). No recorded user decision is cited.

### gemini

- (none)
