# Plan #72 Review - Claude r1

Scope: `docs/plans/2026-07-02-issue-72-generalize-public-surface-review-selector-snapshot-modes.md`

## Verdict

MINOR

## Findings

1. MINOR - The draft excluded issue number `64` with the wrong rationale. Live GitHub state showed number 64 is an open codex planning PR/meta item related to ACE ingestion waves. The safe exclusion can remain, but the rationale needed to state that #64 is not a canonical `docs/plans/` ACE wave/split/follow-on plan row with retained review artifacts.
2. MINOR - The second reproduction proof was not a runnable transcript. The heredoc body contained only an import and comments but claimed snapshot errors after the terminator. The underlying behavior was true, but the evidence block needed an executable script.
3. MINOR - The plan used `allowed_issue_numbers or equivalent` and did not state what happens to the old singular `review_artifact_selector.issue` key. The contract plan needed to remove or reject the stale key to avoid dual issue-number authorities.
4. MINOR - The test list omitted retained legacy review artifact names such as round-less plan artifacts and `disagreement` provider artifacts. These should remain unselectable after issues 50, 51, and 66 become contract-authorized.
5. MINOR - The file-existence evidence omitted `tests/test_validate_ace_public_surface_scan.py`, even though the plan maps and verifies it.

## Verified Checks

- Verified the hard-coded issue-68 selector, snapshot issue check, URL checks, and review artifact regex in the current scanner code.
- Verified the first selector reproduction command emits the issue-68-only denial.
- Verified a real synthetic #72 planned-comment snapshot currently produces the claimed issue and URL errors.
- Verified the proposed issue enum is consistent across the draft and excludes #64.
- Verified the draft plan itself passes the public-surface scan.

## Disposition

Patched in the local draft after r1. R2 review required before `status:plan-review`.
