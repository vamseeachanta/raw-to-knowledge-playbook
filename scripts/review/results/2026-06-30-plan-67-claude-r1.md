## Verdict
MAJOR

## Retrieval
- Reviewed `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md`.
- Checked issue approval-marker state and planned artifact existence.
- Read `docs/plans/README.md`, `docs/plans/ace-share-ingestion-wave-coordination.md`, `.github/workflows/validate.yml`, `artifacts/ace-wave0-ledger-schema.json`, `scripts/validate_ace_epic_wave_coordination.py`, and `scripts/validate_ace_wave0_schema_contract.py`.
- Ran baseline parent coordination and schema validators successfully before findings.

## Findings
1. The plan routed classifier, validator, and contract JSON through the parent public scanner without stating how the classifier source would avoid self-blocking. A firewall implementation must reference denied command classes and source-root abstractions; committed source must avoid scanner-triggering runnable expressions or carry a narrowly tested allow-context.
2. The executable-context classifier boundary was still prose-heavy. The plan used "closed rules" and "command-like syntax" language without enumerating the discrimination rule reviewers could verify.
3. The six manifest source keys were attributed to the README portfolio gate, but the coordination ledger is the actual source of the six-key list. The plan also needed to reconcile that list with the parent validator's narrower helper pattern.
4. The Files-to-Change table described README and coordination row creation as future work even though those rows were already created in the reviewed draft.
5. The validation commands in acceptance criteria used a local cache-prefix form while CI uses bare `uv run` commands.
6. The plan did not explicitly call out that #67 registry edits are themselves scanned public surfaces.

## Blockers
- Resolve the classifier self-scan/source-safety contract.
- Specify executable/prose discrimination rules concretely.
- Correct manifest-source authority and parent-helper reconciliation.

