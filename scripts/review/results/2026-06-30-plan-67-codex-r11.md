## Verdict
MAJOR

## Retrieval
- Read `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md` lines 1-629.
- Read `docs/plans/README.md` lines 1-58.
- Read `docs/plans/ace-share-ingestion-wave-coordination.md` lines 1-90.
- Read `artifacts/ace-wave0-ledger-schema.json` lines 1-328.
- Read `scripts/validate_ace_epic_wave_coordination.py` lines 1-260 and 715-805; grep'd scanner/approval/public-scan symbols.
- Read `scripts/validate_ace_wave0_schema_contract.py` lines 1-280; grep'd split-registry/public-scan symbols.
- Read `.github/workflows/validate.yml` lines 1-55.
- Read `tests/test_validate_ace_epic_wave_coordination.py` lines 828-979.
- Ran `gh issue view 67` and `gh issue view 65` for live issue labels/body.
- Listed `scripts/review/results/2026-06-30-plan-67-*` review artifacts.
- Ran existing validators/tests: parent public scan against the #67 plan, #65 schema validator, and existing validator unit tests.

## Findings
1. Plan `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md:468` says only the base sampling keys plus exactly one of `target_wave_class`/`fixture_scope` are accepted and unknown keys fail. That excludes `snapshot_evidence`, but the same plan allows/requires `snapshot_evidence` for downstream requests and shape-only metadata fixtures at lines 178, 231, and 257. The TDD contract would reject the planned evidence fixture or force an implementation exception not represented in the exact-key test.

2. Plan `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md:286` says JSON fixtures are executable when request fields carry command, query, source root, manifest operation, or sampling expression. The accepted request key set at lines 178 and 468 defines no such fields, and `test_json_request_command_context_is_executable` at line 501 only says “runtime-built JSON request.” A test can pass by rejecting an unknown key instead of proving the JSON executable-context classifier actually catches denied command content.

3. The “Concrete Denied Token Matrix” is still not concrete enough for approval. Plan lines 304-313 list broad families such as “raw file reader family,” “JSON/query tool family,” and “digest/fingerprint family,” while the existing parent scanner has explicit denial patterns for specific shell/Python/query tokens in `scripts/validate_ace_epic_wave_coordination.py:146-158`. Without enumerating the #67 token fragments in the plan or a referenced contract draft, reviewers cannot verify coverage before implementation.

## Blockers
1. Finding 1 blocks implementation: the request schema and evidence fixtures contradict each other.
2. Finding 2 blocks implementation: JSON executable-context coverage has no valid field surface and can be tested vacuously.
3. Finding 3 blocks implementation: the denial matrix is not reviewable at token level before approval.
