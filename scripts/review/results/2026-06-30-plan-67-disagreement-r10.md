# Disagreement report — plan #67 (2026-06-30)

## Verdicts

| Provider | Verdict |
|---|---|
| claude | MAJOR |
| codex | MAJOR |
| gemini | UNAVAILABLE (gemini CLI failed, rc=1: Warning: Basic terminal detected (TERM=dumb). Visual rendering will be limited. For the best experience, use a terminal emulator with truecolor support. Warning: 256-color support not detected. Using a terminal with at least 256-color support is recommended for a better visual experience. Error authenticating: IneligibleTierError: This client is no longer supported for Gemini Code Assist for indiv) |

## Findings unique to each provider

A finding is 'unique to X' if its text appears in X's artifact but not
verbatim in any other provider's artifact.

### claude

- **[MAJOR] The one committed positive-evidence fixture has a self-contradictory grammar.** §Snapshot Evidence Shape line 234 states `reason_code` is "exactly `MISSING_62_EVIDENCE_CONTRACT`" with **no `evidence_mode` condition**, so read literally it applies to *every* `snapshot_evidence` object — including `evidence_mode=shape_only_fixture`. But lines 226/230 define `shape_only_fixture` as positive parser coverage, and the committed `downstream-shape-only-request.json` (line 247) specifies only `evidence_mode=shape_only_fixture` + `source_issue=62` and never mentions `reason_code`. So the plan simultaneously (a) requires a "blocked" reason code on the positive shape fixture and (b) omits it from that fixture's spec. Likewise `blocked_by_issue`/`follow_on_issue` (lines 232-233) are conditioned only on `blocked_pending_62_contract` and the plan never says whether they are *forbidden* on `shape_only_fixture`. An implementer cannot deterministically decide the required field set for the sole committed fixture that carries evidence — and this is on the firewall's *accept* path, where under-specification risks a false-allow. Fix: state, per `evidence_mode`, exactly which of `reason_code`/`blocked_by_issue`/`follow_on_issue` are required, optional, or forbidden.
- **[MINOR] The committed fixture name `downstream-shape-only-request.json` contradicts its own contents.** §Fixture Payload Contract line 247 gives it `target_issue=67`, `request_class=metadata_only_fixture`, `fixture_scope=firewall_validator_self_check` — it is a #67 self-check fixture, not a downstream request. The genuine downstream case is the runtime-only "Runtime downstream blocked fixture" (line 248). An implementer reading the filename could build an actual `downstream_manifest_backed_sampling` fixture, which would (correctly) fail-closed and contradict the "Passes" expected result. Rename (e.g. `metadata-fixture-with-shape-only-evidence.json`) or annotate.
- **[MINOR] Over-scope is flagged but left unresolved.** The Open Question (line 580) concedes formal review "may require another split if the executable-context classifier and sampling grammar are still too much for one approval unit." The unit bundles a ~25-field closed contract, a six-context classifier with runtime fragment-assembly machinery, a validator, the #62 evidence gate, CI self-scan-manifest derivation, and split-registry edits (44 TDD tests), and has cycled MAJOR across nine rounds. Deferring the split decision into implementation is a real convergence risk; resolve whether the classifier separates from the sampling grammar before requesting approval.
- **[MINOR] Skill-validation command uses an inconsistent invocation form.** Acceptance criterion line 515 is `uv run skills/validate_skill.py`, while the adjacent criteria (lines 511-514) all use `uv run python <path>`. This exact inconsistency was flagged in the recorded Claude r7/r8/r9 reviews (§Adversarial Review Summary) and still persists. Standardize to `uv run python skills/validate_skill.py` so the command is verified, not guessed.
- **[MINOR] Drift test lacks a type contract for imported scalars.** §Proposed Contract Shape says `owner_issue` "exactly `67`" and `method_issue_bindings` "exactly #1 and #12," but `artifacts/ace-wave0-ledger-schema.json:156-159` stores these as JSON **integers** (`[1, 12]`, `"owner_issue": 65`). `test_contract_imports_65_schema_terms` (line 430) compares #67 against #65; if #67 emits strings (`"67"`, `["#1","#12"]`) the equality/drift assertion becomes brittle or vacuous. Specify integer typing.
- **[MINOR] Self-reported verdict is stale for the round meant to clear it.** The header (line 10) and §Adversarial Review Summary "Overall result: MAJOR ... not ready for `status:plan-review`" (line 570) still assert MAJOR. The r9 Codex blockers (request-class pass/fail column, per-source `INDEX.md` raw-read coverage, token-family-granular `denied_token_matrix`) *are* present in the current text, so carrying a self-asserted "Overall result: MAJOR" into the clearing round is misleading governance state. Update the summary to reflect the patched-for-re-review status rather than a stale terminal verdict.

### codex

- `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md` §Proposed Contract Shape says it will create a “closed JSON contract,” but `required_sampling_fields` at line 174 uses prose labels instead of exact JSON keys for core fields such as manifest source, seed, per-bucket row cap, maximum files touched, maximum bytes touched, and evidence requirement. The fixture table at lines 246-248 only pins exact names for a subset (`target_issue`, `request_class`, `fixture_scope`, `output_shape`, `route_target`, `logical_target_store`). The tests at lines 429-448 also describe missing fields in prose. This leaves the contract non-closed at the field-name level.
- `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md` requires a source-root refusal boundary but never names the helper or the entrypoints that must route through it. The pseudocode at lines 376-379 says “the refusal helper”; the test row at line 446 says “source-root boundary/refusal helper”; the acceptance criterion at line 503 says “the named source-root boundary helper.” No function name, module API, or required call sites are specified. A narrow representative operation could pass while another validator path still touches `ACE_SHARE_ROOT`.
- The plan remains explicitly unpassable under its own gate state unless a new same-round paired review also returns no-MAJOR. The header at line 10 records “r9 Claude MINOR, Codex MAJOR”; the gate rule at line 534 says any usable MAJOR keeps #67 draft; line 570 says “Overall result: MAJOR.” `docs/plans/README.md:33` and `docs/plans/ace-share-ingestion-wave-coordination.md:43` also record #67 as draft requiring re-review. This is not an implementation defect, but it blocks any move to `status:plan-review` from this review alone.

### gemini

- (none)
