I have enough verified evidence to write the review. Let me note one key insight before presenting findings.

`★ Insight ─────────────────────────────────────`
- This is a **fail-closed control-plane validator**: in this approval unit *nothing* operational is accepted — only one synthetic self-check fixture (`target_issue=67`, `fixture_scope=firewall_validator_self_check`) passes. That single accepted path is therefore the highest-leverage place to look for spec holes, because it's the only thing the validator can ever say "yes" to.
- The plan leans hard on a "single source of truth" invariant (`#65 canonical_wave_registry`). Any field the contract requires *universally* but sources *only from that registry* breaks for issues that aren't registry members — and `#67` itself is one of those.
- Verified against disk: registry issues are exactly 51–63; `#67` lives only in `wave0_split_registry`, which carries no `requires_manifest_snapshot_id` field.
`─────────────────────────────────────────────────`

## Verdict
MAJOR

## Retrieval
- Read the full plan `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md` (lines 1–630, in two pages).
- Verified file existence: all six "Create" targets (`config/`, contract JSON, classifier, validator, tests, fixtures) confirmed MISSING; all six existing deps (schema, wave0/coordination validators, README, ledger, `.github/workflows/validate.yml`) confirmed EXISTS. `skills/validate_skill.py` EXISTS; `scripts/legal/legal-sanity-scan.sh` confirmed MISSING (matches plan's `NO_LEGAL_SCAN_SCRIPT` closeout gate).
- Verified all six skill docs (`format-coverage-ledger`, `public-private-routing`, `content-triage-and-exclusion`, `page-shape-contract`, `adversarial-verify-loop`, `source-extraction-coverage`) EXIST.
- `docs/plans/ace-share-ingestion-wave-coordination.md:16` — confirmed the six manifest source keys match the plan's Source inventory (line 48) exactly.
- `docs/plans/ace-share-ingestion-wave-coordination.md:18-20` — confirmed caps (200 rows / 25 files / 1048576 bytes) match `maximum_caps` (plan line 179).
- `artifacts/ace-wave0-ledger-schema.json:160-269` — confirmed `bound_skill_groups` (five, no `source-extraction-coverage`), `route_targets`/`route_store_matrix`, and `canonical_wave_registry` issues 51–63 with wave classes `control_plane`/`ingestion_wave`/`storage_lifecycle_gate`/`manifest_freshness_gate`/`public_canary_gate` — all match the Request Class Mapping table (lines 204–211).
- `artifacts/ace-wave0-ledger-schema.json:271-302` — confirmed `#67` is in `wave0_split_registry` only (`plan_path:""`, `status_snapshot:"plan-required"`, `implementation_ready:false`), NOT in `canonical_wave_registry`.
- `gh issue view 67` — confirmed live issue skill set (`content-triage-and-exclusion`, `source-extraction-coverage`, `format-coverage-ledger`, `public-private-routing`, `adversarial-verify-loop`) and method issues (#1, #12) match the plan (lines 29, 176).
- `.github/workflows/validate.yml` — read current content; uses `uv run skills/validate_skill.py` (bare, no `python`).
- `scripts/review/results/` — confirmed plan-67 review artifacts exist for r1–r10 across claude/codex/gemini/disagreement (40 files); highest round on disk is r10, matching the Artifact Map (r1–r10) and Adversarial Review Summary.
- `.planning/plan-approved/` — confirmed markers exist for 50 and 65 only; no 67 marker (matches "no local approval marker" claim, line 72).

## Findings

1. **`requires_manifest_snapshot_id` has no `#65` source of truth for the only passing fixture (`target_issue=67`) — self-contradictory core invariant.** The contract makes `requires_manifest_snapshot_id` a *universally required* top-level key on every request (Proposed Contract Shape, line 178: "`required_sampling_fields`... `requires_manifest_snapshot_id`..."), and pseudocode line 354 asserts it "is present as boolean **from #65**," with `requires_manifest_snapshot_id_gate` (line 184) declared "imported from the #65 canonical wave registry and used as the **single source of truth**." But the accepted self-check fixture has `target_issue=67`, and I verified `#67` is **not** in `canonical_wave_registry` (`artifacts/ace-wave0-ledger-schema.json:178-269`, issues 51–63 only) — it exists only in `wave0_split_registry`, which carries no `requires_manifest_snapshot_id` field. So for `good-request.json` the validator has no `#65` entry to source or check this required boolean against, and the Fixture Payload Contract (line 256) never states its value. The plan simultaneously claims (a) the field is required on every request, (b) its value comes from `#65`, and (c) `#65` is the single source of truth — yet the one request that must pass targets an issue `#65` doesn't cover. An implementer cannot deterministically build the passing fixture or the gate check without diverging from the stated invariant. **This is the blocker.**

2. **`snapshot_evidence` object-level required/optional condition is under-specified where the plan says it is defined.** Line 178 states `snapshot_evidence` "is required only when the evidence-mode table below says so," but the evidence-mode table (lines 247–251) only enumerates fields *within* `snapshot_evidence` per `evidence_mode` — it never states when the `snapshot_evidence` object itself is required at the request level. The actual object-level rule lives elsewhere (pseudocode line 378: "downstream_manifest_backed_sampling requires a complete snapshot_evidence object"). The cross-reference on line 178 points at a table that does not answer the question it promises. MINOR (imprecise pointer, resolvable), but it compounds Finding 1's ambiguity about what a `metadata_only_fixture` with `target_issue=67` must carry.

3. **Acceptance criterion #1 is a self-referential review-gate, not a code-verifiable criterion.** Line 524: "the latest same-round adversarial review has at least two usable provider results and no usable provider returns MAJOR." This criterion is satisfied or falsified *by the very review that is checking it* (this one). It restates the Plan Review Gate Rule (line 574) as an acceptance item, so acceptance of the implementation is gated on a process outcome external to the code. It is not wrong as a lifecycle rule, but as an acceptance criterion it is circular and cannot be discharged by inspecting the delivered artifacts. MINOR.

4. **The firewall accepts exactly one input shape and authorizes zero operational sampling in this unit — verify this is the intended, reviewed scope, not an accident of the fail-closed cascade.** Deliverable (line 162) and Request Class Mapping (line 213) confirm only the `#67` `metadata_only_fixture` row passes; `#51/#61/#62/#63` control-plane rows and `#52–#60` downstream rows all fail closed, and `operational_live` is reserved for `#70`. This is explicitly by design and acknowledged, so it is not a defect — but it means the entire T3 deliverable's positive-path test surface is a single synthetic fixture (Finding 1's fixture). That concentrates all correctness risk on the one path that Finding 1 shows is under-specified. Not an independent blocker; flagged so the scope narrowness is a conscious sign-off. (Reviewed against: is any real allow-path claimed? No — line 162 defers the first operational allow-path to `#70`.)

Checks run that produced **no** finding (silence-is-failure disclosure): manifest source keys match the ledger exactly; caps match the ledger exactly; `bound_skill_groups` vs live-issue skill set reconciliation (union of six, `source-extraction-coverage` excluded from `#65` enum) is correct against both the schema file and the live issue; method bindings `[1,12]` correct; wave classes in the mapping table all exist in `#65`; all cited file paths resolve as claimed (MISSING/EXISTS states all accurate, including the legal-scan absence); review-artifact inventory (r1–r10) matches disk with no un-inventoried rN files; no `#67` approval marker exists (matches the "implementation stays blocked" posture); the `page-shape-contract`-vs-`source-extraction-coverage` swap between `#65` and the live issue is correctly reconciled.

## Blockers
- **Finding 1** must be resolved before implementation: specify the `requires_manifest_snapshot_id` value the `#67` self-check fixture carries **and** the validator's check semantics when `target_issue` is not a `canonical_wave_registry` member (e.g., scope the "single source of truth" gate to registry members only, and state the fixture's literal value), so the plan stops asserting three mutually inconsistent rules about the one accepted path.

(Findings 2–4 are MINOR and do not independently block, but resolving Finding 2 alongside Finding 1 is advised since they share the same `snapshot_evidence`/required-field surface.)
