review_artifact_role: public_history
normalization_note: denied-token literal substitutions only; provider verdict unchanged

# Disagreement report — plan #51 (2026-06-29)

## Verdicts

| Provider | Verdict |
|---|---|
| claude | MAJOR |
| codex | MAJOR |
| gemini | UNAVAILABLE (gemini CLI failed, rc=41: Error authenticating: FatalAuthenticationError: Manual authorization is required but the current session is non-interactive. Please run the Gemini CLI in an interactive terminal to log in, provide a GEMINI_API_KEY, or ensure Application Default Credentials are configured.     at initOauthClient (file:///home/vamsee/.npm-global/lib/node_modules/@google/gemini-cli/bundle/chunk-VLV2BYPM.js:269720:13)) |

## Findings unique to each provider

A finding is 'unique to X' if its text appears in X's artifact but not
verbatim in any other provider's artifact.

### claude

- **The membership-inference threat model the plan invents for hashes is not applied to its own share-path disclosures — an unjustified exemption in the core security argument.** Line 48 makes the governance decision that public ACE artifacts must not publish source pointers because "a raw hash … can still reveal membership in a private ACE corpus." Lines 81–87 and 131–138 then publish seven verbatim share-relative paths — including the private share's *internal index structure* `_cad-index/cad-readability-index.tsv`, `.ace-knowledge/index.db`, and `docs/master-index.jsonl` — in `docs/plans/`, which line 312 explicitly declares "already a public methodology surface under MkDocs and must be treated as published." Line 79 deliberately moves exact size/mtime to "a private/non-public evidence sidecar if needed," yet keeps the *paths* public. The plan never states why a share-internal path is public-safe under its own membership-inference model while a file size is not. For `assets.json`/`master-index.jsonl` the plan offers a "named by the issue body" rationale (lines 30–31), but `.ace-knowledge/index.db` (line 34) and `_cad-index/` (line 32) carry no such justification — they are simply whitelisted. This contradicts the ledger field `share_relative_path_private_only`, which lines 144/285/564 designate private-only and reject in public surfaces.
- **The Pre-Label fallback scan (line 603) passes on the committed review artifacts only by coincidence, making #51's status gate brittle.** The checklist requires running `validate_ace_epic_wave_coordination.py --scan-public-path` over "every retained plan-51 review artifact." That scanner's `os\.walk\s*\(`, `\[denied-python-rglob-token]\s*\(`, and `\bls\s+-R\b` patterns fire unconditionally (no manifest token required); the only escape, `_allowed_denied_traversal_policy_prose` (validator lines 630–633), requires one of six hardcoded phrases on the *same line*. The committed `scripts/review/results/2026-06-29-plan-51-codex-r18.md:21` and `…-disagreement-r18.md:25` both contain `[denied-python-walk-token]` and `[denied-recursive-list-token]`, and survive the scan only because that line happens to also contain the whitelisted phrase "denied-command prose." The plan's normalization policy (lines 442–451) tokenizes "source-bearing denied-command examples" but nowhere requires co-locating a policy-prose term on every line that mentions an unconditional denied token. Any future cited/committed artifact that mentions `[denied-python-rglob-token](`, `[denied-python-walk-token](`, or `[denied-recursive-list-token]` without that exact phrase will fail the pre-label scan and silently block the status transition.
- **The downstream nonzero-denominator requirement is under-specified at #51 authoring time and risks fabricated success data.** Line 546 (`test_zero_denominator_is_non_computing_sentinel`) and line 586 require #52–#60 to "reject zero denominator" / carry a "nonzero denominator," but those waves have ingested nothing when #51's forward-reference wave map is authored, so no real measured denominator exists. Line 548 (`test_every_downstream_wave_has_issue_skill_and_validator`) lists the required downstream fields and omits measured numerator/denominator entirely. The plan never resolves whether #52–#60 wave-map rows must populate a denominator: if they must, the #51 fixture has to invent nonzero placeholders (fabricated metrics in a control-plane contract); if they may omit it, line 546's "reject zero" rule is only conditionally meaningful. The contract is ambiguous to an implementer. (MINOR)
- **`requires_manifest_snapshot_id` is asserted as both "owned by #51" and "consumed from the parent coordination doc," and #51 forbids itself from modifying the parent's matrix — a latent two-source-of-truth hazard.** Lines 248–255 say `requires_manifest_snapshot_id` "is a per-issue sampling boolean owned by #51," while `docs/plans/ace-share-ingestion-wave-coordination.md` "also carries a Manifest gate text column" that #51 "consumes and scans … but does not modify." Test `test_manifest_gate_boolean_matches_coordination_text` (line 547) requires the two to agree, but with #51 unable to edit the parent and the parent owned under #50, any future drift in the parent's Manifest-gate column fails #51's validator with no in-#51 remediation path. The plan should name which artifact is authoritative on disagreement. (MINOR)

### codex

- Plan §Pre-Label Evidence Checklist lines 601-604 relies on the parent fallback scanner before `scripts/validate_ace_wave0_control_plane.py` exists, but that scanner cannot enforce the plan’s central raw-source/private-field policy. `scripts/validate_ace_epic_wave_coordination.py:91-112` only checks mount-path/email/client-ish/confidential/SSN/phone patterns plus traversal commands; it has no pattern for assigned `source_id`, assigned `source_sha256`, `private_lookup_key`, private lookup maps, `public_source_token`, or source-like digest values. `tests/test_validate_ace_epic_wave_coordination.py:794-819` likewise only tests email/path/traversal examples. This leaves the pre-label scan unable to prove the issue-body/comment promises in plan lines 601-602.
- The #51 self-scan acceptance still overclaims generic leakage coverage. Plan lines 393-397 explicitly say #51 does not load private deny-lists, does not certify real public exposure, and leaves maintained client/project/internal-name denial to #63. But acceptance line 575 says #51 blocks “personal identifiers” and “proprietary snippets” across public surfaces. Generic regexes can catch some emails/phones or marker phrases; they cannot certify proprietary snippets without a maintained corpus/list, which the plan forbids #51 from loading. This is an objective acceptance criterion that cannot be verified by the proposed #51 mechanism.
- The review-history role contract is inconsistent with the current artifact set. Plan line 106 says historical/local `scripts/review/results/2026-06-29-plan-51-*-r*.md` artifacts carry explicit `review_artifact_role`, and lines 423-429 require committed/cited artifacts to be role-tagged while local-transient artifacts are never cited. In the worktree, only r16-r18 are tracked with `review_artifact_role`; r1-r15 exist under the same named artifact glob, are untracked, and lack role tags. The plan’s Adversarial Review Summary cites r3-r15 outcomes at lines 616-654 without specifying whether those local files will be normalized/tagged, committed as public history, or excluded as transient residue.

### gemini

- (none)
