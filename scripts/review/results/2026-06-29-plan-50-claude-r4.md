I have verified all six points against HEAD. Let me synthesize the adversarial r4 review.

`★ Insight ─────────────────────────────────────`
- The r4 fix wave was a *standardization-and-tightening* pass. The highest-yield adversarial target in such a pass is always the spot the standardization didn't reach — and it's here: the deferral-gate language was uniformized across the publication *rows* but one instance of the old weak two-element phrasing survives in a *prose risk note* (#50:197), because grep-and-replace passes that target table rows miss narrative text.
- The deepest correctness move in this patch is #62 forcing `content_fingerprint_status=unavailable` rather than letting size+mtime stand in as a drift signal. This is the rare case where the *safe* answer is to admit ignorance: a bounded-read sentinel that can't see content must say "unavailable," not "compatible," or it would silently green-light drift on exactly the multi-million-row manifests it exists to watch.
`─────────────────────────────────────────────────`

## Provider

Claude (adversarial r4 review of `2be9f80..HEAD`, single commit `5965750`)

## Verdict

**MINOR** — All five r3 residual objectives (points 1–5) are affirmatively resolved at HEAD and no new MAJOR is introduced. One residual stale phrase survives the point-6 sweep. The plan correctly remains `NEEDS FINAL REREVIEW` / not approval-ready, which is the right posture until this r4 review clears it.

## Findings Grouped By Severity

### Resolved (affirmatively verified at HEAD)

- **Point 1 — #50/#51 deferred rows carry the #63 gate: RESOLVED.** #50 rows (`:132-133`) and #51 rows (`:120-121`) now read "until #63 is approved, **the local marker exists, and the public-output canary has a recorded passing result**," matching #57/#58/#59 (`57:109-110`, `58:123-124`, `59:116-117`). The binding acceptance criteria carry the explicit four-element gate: #50 `:169-171` (`status:plan-approved` snapshot + approval marker + implemented validator/canary + recorded passing-command result, split across #61/#62/#63 rows) and #51 `:156`. *Note (non-blocking):* the deferral rows use the condensed three-element phrasing and say "approved" rather than "status:plan-approved", folding "implemented canary" into "has a recorded passing result." This is adequate because the explicit gate lives in the acceptance criteria, but the row text is softer than the literal four-field form.
- **Point 2 — #51 case-study publication gate criterion: RESOLVED.** `#51:156` is a dedicated acceptance criterion applying the full explicit gate to #51's own case study: "Any `docs/index.md` or `mkdocs.yml` publication of the #51 case study requires #63 `status:plan-approved`, local approval marker, implemented redaction canary, and recorded passing-command result." Four elements, explicit.
- **Point 3 — #62 named config fields: RESOLVED.** `max_header_bytes`, `max_under_cap_bytes`, `max_under_cap_rows` appear as the named cap boundary in source inventory (`:30`), pseudocode (`:85`), TDD `test_manifest_caps_are_named` (`:124`), and acceptance (`:136`). The old "implementation byte/row cap" wording is gone everywhere.
- **Point 4 — #62 above-cap/no-sidecar → unavailable, no size/mtime compatible: RESOLVED.** Stated in four mutually reinforcing places: source inventory (`:31`, "size/mtime alone must not produce a `compatible` drift verdict"), pseudocode (`:89-90` set `content_fingerprint_status=unavailable`; `:93` "size/mtime-only evidence cannot classify large manifests as compatible"), acceptance (`:137`), and new TDD `test_large_manifest_without_sidecar_is_unavailable` (`:125`). `unavailable` is a member of the closed drift enum (`:138`), so no contradiction.
- **Point 5 — review summary accuracy / not-approval-ready: RESOLVED.** Summary table (`#50:182-189`) records r1 (Claude/Codex MAJOR, Gemini UNAVAILABLE), r2 (Claude MINOR, Codex MAJOR), r3 (Claude MINOR, Codex APPROVE). The r3 rows match the committed artifacts (`…claude-r3.md` verdict = MINOR; `…codex-r3.md` verdict = APPROVE). Overall result is `NEEDS FINAL REREVIEW` and explicitly notes "r3 MINOR findings were patched after review, so this plan is not ready for `status:plan-review` until available providers confirm no MAJOR findings on the updated head" — accurately flagging that neither r3 provider reviewed the current HEAD `5965750` (Codex r3 reviewed only through `2be9f80`). This r4 review is that updated-head review.

### MINOR

- **m1 — Stale two-element gate phrasing survives in #50 Risks prose (point-6 hit).** `#50:197`: "publication exposure should wait until #63 **is approved and the canary passes**." This is the old weak two-element form (no local-marker / recorded-passing-result element) that the standardization pass eliminated from every publication *row* but missed in this *risk note*. It is non-operational (a narrative risk, not a publication-control row, so it ungates nothing), but it is exactly the phrasing point 6 directs the reviewer to hunt, and it leaves the fleet's gate language internally inconsistent. Bring it to the three/four-element form used in the deferral rows.

### Observations (non-blocking)

- `scripts/review/results/2026-06-29-plan-50-codex-r3.md` has no trailing newline (`\ No newline at end of file`). Cosmetic.
- The committed `codex-r3.md` artifact pins its review to `2be9f80` and `git diff 614e33f..HEAD`, which was HEAD *at the time of that review*; it does not cover commit `5965750`. This is correctly disclosed by the summary's "updated head" caveat, so it is a provenance note, not a defect.
- Point-1 deferral-row wording (`approved` vs `status:plan-approved`; "implemented canary" only implied) — informational; the explicit gate is carried by the acceptance criteria. Not a required fix.

## Required Fixes Before status:plan-review

The r3 residuals are cleared and no MAJOR exists; the plan correctly remains not-ready. To fully converge:

1. **(m1)** Update `#50:197` to the standardized gate phrasing ("…until #63 is approved, the local marker exists, and the public-output canary has a recorded passing result"), so no stale two-element gate language remains anywhere in the fleet.
2. **(optional, point-1 hardening)** Consider lifting the #50/#51 deferral *rows* to the literal four-field form or referencing the acceptance criterion, so the row text matches the explicit gate rather than relying on the reader to cross-reference acceptance.

Neither is MAJOR. All load-bearing artifacts (pseudocode, TDD, acceptance) carry full gate strength, the bounded-read policy is enforced, and the #62 no-sidecar drift-safety hole flagged in r3 m3 is now closed.

## Checked Evidence

- **Diff:** full `git diff 2be9f80..HEAD` read in entirety — single commit `5965750`, 3 plan edits (#50, #51, #62) + 2 new r3 review artifacts.
- **Point 1:** grepped `docs/index.md`/`mkdocs.yml` rows across #50/#51/#57/#58/#59 — all five pairs `Deferred` with the three-element gate; confirmed #50 acceptance `:169-171` and #51 `:156` carry the explicit four-element gate.
- **Point 2:** read `#51:156` acceptance criterion — explicit four-element case-study publication gate present.
- **Point 3:** grepped `max_header_bytes`/`max_under_cap_bytes`/`max_under_cap_rows` in #62 — present in inventory `:30`, pseudocode `:85`, TDD `:124`, acceptance `:136`.
- **Point 4:** read #62 source inventory `:28-31`, pseudocode `:82-93`, acceptance `:136-137`, TDD `:124-125` — `unavailable` classification + size/mtime-cannot-be-compatible stated in all four; `unavailable` confirmed in the closed drift enum.
- **Point 5:** read `#50:180-189` summary table + overall result; opened `claude-r3.md` (MINOR) and `codex-r3.md` (APPROVE) — verdicts match table rows; not-approval-ready posture confirmed.
- **Point 6 (stale-phrase sweep):** grepped `docs/plans/` for "canary passes" (→ one hit, `#50:197`, m1), "redaction canary before publication" (→ NONE), "Modify | docs/index.md"/"Modify | mkdocs.yml" (→ NONE), "implementation byte/row cap"/"implementation byte" (→ NONE).
- **Not done (per instructions):** no files edited; no commits/labels/comments changed; live `gh`/GitHub state not re-queried (review scope is the static diff; the plan defers live status to recorded snapshots).
