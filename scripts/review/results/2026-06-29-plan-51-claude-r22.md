review_artifact_role: public_history
normalization_note: denied-token/private-field example substitutions only; provider verdict unchanged

I have enough verified evidence to write the review. Let me synthesize.

`★ Insight ─────────────────────────────────────`
- The plan's own §Adversarial Review Summary is the most damning evidence against it: a self-reported 21-round history where every round r3–r21 drew a MAJOR. When a plan documents its own non-convergence, the adversarial reviewer's job is partly done — but I still verified the *external* claims (file existence, the #63 cross-reference, the hash-policy premise) because a plan can be both non-converged *and* factually wrong.
- The most interesting structural defect surfaced not from a missing file but from a contradiction between the design prose (line 242: don't parse #63's plan in CI) and a TDD test (line 588: parse #63's plan in CI). Internal contradictions in a 767-line governance doc hide in the gap between narrative sections and the test list.
`─────────────────────────────────────────────────`

## Verdict
MAJOR

## Retrieval
- Read the full plan `docs/plans/2026-06-29-issue-51-ace-wave-0-corpus-ledger-routing-firewall-sampling-protocol.md` (lines 1–767, both pages).
- Verified file existence for all 14 "EXISTS" claims (docs/01, 04, 05, 07, 18, 19; skills/README.md + 5 SKILL.md; skills/validate_skill.py; .github/workflows/validate.yml) — all present.
- Verified the 5 "MISSING/to-create" claims (artifacts/control-plane.md, validate_ace_wave0_control_plane.py, validate_ace_public_artifacts.py, ace_public_tokens.py, config/ace-public-output-contract.json) — all absent, consistent.
- Verified `scripts/validate_ace_epic_wave_coordination.py`, its `tests/`, `docs/plans/ace-share-ingestion-wave-coordination.md`, `docs/plans/README.md`, and the #50/#63 plan files exist.
- Read `validate_public_artifact_paths()` in `scripts/validate_ace_epic_wave_coordination.py:743-790` — confirmed it implements `--scan-public-path`, DENIED_TRAVERSAL_PATTERNS, PRIVATE_LEAK_PATTERNS, PRIVATE_SOURCE_FIELD_ASSIGNMENT_PATTERNS, SOURCE_LIKE_RAW_DIGEST_PATTERNS (the Pre-Label fallback gate is real).
- `gh issue view 51/63` — both OPEN with titles matching the plan's §Evidence and filenames.
- `git ls-files` over `scripts/review/results/` — confirmed r16–r21 tracked, r1–r15 untracked (matches §Artifact Map local_transient claim); 60 untracked plan-51 files.
- Grepped `docs/19-trust-boundary-and-private-mode.md:82` — confirmed `| sha256 provenance pointer | none | none | always safe |` (the plan's reconciliation premise is true).
- Counted the sha256 sweep universe: 68 hits in `docs/**/*.md`, 24 in `skills/**/*.md`, across 17+ files.
- `grep -c ace-public-output-contract.json` in the #63 plan → 11 (line 400's "already references" claim holds).
- `find / -iname AGENTS.md` — no AGENTS.md in this repo; only `~/AGENTS.md` and per-client copies.

## Findings

1. **The plan has not converged and is self-admittedly non-approvable** (§Adversarial Review Summary lines 691–749; §Status line 10). The plan's own table records a MAJOR in *every* round r3–r21, with r21 returning MAJOR from **both** active providers (line 745–747) and "Overall result" (line 749): *"This plan remains draft-only and must not move to status:plan-review."* Per the review prompt's default-to-non-approve rule and the plan's own status (`PENDING final no-MAJOR round`), APPROVE is impossible. The artifact has grown to 767 lines across 21 rounds — escalating length without convergence is itself the signal.

2. **Internal contradiction: #51 CI both must-not and must parse the #63 plan prose.** Pseudocode lines 242–243 state *"#63 consumes this config rather than being parsed by #51 CI through brittle plan-prose matching."* But the TDD test `test_shared_public_output_contract_is_consumed` (line 588) takes *"`config/ace-public-output-contract.json` and #63 plan text"* as input and asserts *"#63 references the shared config instead of redefining or contradicting it."* That unit test (in `tests/test_validate_ace_wave0_control_plane.py`, i.e. #51 CI) *is* brittle #63-plan-prose matching. The two sections directly conflict; an implementer cannot satisfy both. Resolve which surface (config only, or config + #63 plan text) #51 CI actually parses.

3. **Front-loaded scope makes #51 un-reviewable as a single unit** (§Scope Decision lines 119–121; Claude r20 line 742). #51 simultaneously defines: the ledger schema, the closed route/store/verification enums, the routing firewall, the bounded-sampling executable-context grammar, the opaque token grammar + generator module split, a repo-wide sha256 governance sweep + reconciliation of 6+ docs, a shared #63 contract, a repo-local legal scanner, a degraded-provider-quorum policy, and live #63/#51 issue-body reconciliation. Much of this is interface governance the plan itself assigns to #61/#62/#63. The plan names a trim split (line 121: move the source-hash sweep + shared-config creation to a follow-on issue) — that split should be taken *before* approval, not offered as a footnote.

4. **CI is coupled to a hand-maintained sweep artifact over ~92 hits, with no sync mechanism.** `test_existing_governance_docs_and_skills_do_not_publish_raw_hashes` (line 590) requires *every* stable sha256 hit in `docs/**/*.md` and `skills/**/*.md` to be pre-classified in `artifacts/ace-source-hash-policy-sweep.md`. I count 68 hits in docs + 24 in skills across 17+ files. Any future edit that adds a `sha256` mention to any tracked doc/skill breaks CI until the artifact is manually re-synced. The plan specifies determinism of *classification* but provides no mechanism to keep the artifact's hit-set in sync with the corpus — a standing maintenance trap that will red-light unrelated PRs.

5. **The repo-local legal gate is required but its deny patterns are unspecified.** Acceptance criterion line 668 / TDD `test_legal_sanity_scan_is_repo_local_and_executable` (line 620) require `bash scripts/legal/legal-sanity-scan.sh --diff-only` to "fail block-severity fixtures," and both `scripts/legal/legal-sanity-scan.sh` and `.legal-deny-list.yaml` are confirmed absent (to be created). But unlike the token/leak/traversal patterns — which the plan specifies exhaustively — the plan never defines what `.legal-deny-list.yaml` block-severity content must contain. The gate's correctness reduces to whatever the implementer invents, so the "executable legal gate" is asserted but not actually specified.

6. **Stale coordination tracker that is also a self-scan target.** `docs/plans/README.md:29` currently states #51 *"r15 returned MAJOR"* while the plan is at r21. README is on the canonical self-scan/acceptance target list (line 648). The plan defers reconciling it to plan-review time (lines 553–554), but Pseudocode line 284 declares the structural registry authoritative over prose summaries — meaning a known-stale public surface is being carried into a security-firewall plan whose whole thesis is that public surfaces must not be stale/leaky.

7. **"Inherited AGENTS gate" is cited as a hard requirement with no resolvable in-repo path** (lines 498, 537). The legal-scanner deliverable and the T3 degraded-quorum policy are both justified by an *"inherited AGENTS"* rule, but there is no `AGENTS.md` in this repository (root or depth-2); the authority resides only in `~/AGENTS.md`. For a plan this insistent on citable evidence, the controlling rule for two of its deliverables should be cited by path rather than as "inherited."

8. **Source-existence evidence is unverifiable in the review environment** (§Evidence "Bounded source metadata proof" lines 81–91). `ACE_SHARE_ROOT` is unset here, so the seven `EXISTS … details=withheld_public` rows cannot be confirmed. The plan correctly states (line 37) this proof is host-local and *not* correctness-critical for the validator (which must pass with `ACE_SHARE_ROOT` unset) — so this is a disclosure, not a defect in the validator design — but the inventory rationale that drives the fixed-path allowlist rests on unverifiable point-in-time evidence.

## Blockers
- **Finding 1** — the plan's own most recent review round is MAJOR/MAJOR/UNAVAILABLE; it cannot advance to `status:plan-review` and cannot be APPROVE.
- **Finding 2** — the #51-CI-vs-#63-plan-parsing contradiction must be resolved before implementation, or the validator and TDD test list are mutually unsatisfiable.
- **Finding 3** — a scope decision (proceed at full breadth vs. take the named trim split) must be made by the user before another review round; continuing to patch a non-converging 767-line artifact is the failure mode the plan's own "Overall result" warns against.

Findings 4 and 5 should be resolved before implementation (CI brittleness and an under-specified mandatory gate). Findings 6–8 are MINOR and need not block, but 6 should be fixed since the stale surface is itself a self-scan target.
