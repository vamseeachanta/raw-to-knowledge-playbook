Verdict: MINOR

All six r1 findings (three Claude MAJORs, two Codex MAJORs, shared MINORs) are substantively addressed in the revision; I verified each fix against the live code and registries. What remains are precision defects, not gate failures.

Findings:

- [MINOR] The `report_workbook` mechanism is internally contradictory across three plan sections. The mapping table (docs/plans/2026-06-29-issue-53-...md, "Workbook Class and Route Mapping") maps canary `unsupported` flatly to `excluded_workbook`, but `test_report_workbook_overlay_does_not_mutate_canary_class` says the existing class "remains `mixed` or `unsupported`" while "ACE overlay records `report_workbook`" — so an `unsupported` chart-only workbook has two conflicting dispositions. Additionally, the `mixed` row offers two alternative encodings ("`calculation_workbook` plus `report_candidate=true` **or** `report_workbook` overlay") while `test_workbook_classification_closed_values` and the pseudocode treat `report_workbook` as a fourth `workbook_class` enum value; if it is instead a separate overlay field, that closed-values test and Acceptance Criterion 1 are wrong. The trigger rule "when chart/layout evidence dominates" is also undecidable against the current inventory: `inventory_workbook` in skills/xlsx-input-code-output-canary/resources/xlsx_canary.py records chart/merged-range counts but no "layout evidence" signal. Fix: pick one encoding (recommend: `workbook_class` stays a 4-value closed enum; drop `report_candidate=true` as an alternative), state the `unsupported`+charts disposition explicitly, and define the report trigger in terms of fields the inventory actually emits (e.g., `chart_count > 0` with zero/low table count).

- [MINOR] The coordination row for #53 is stale against the r1 evidence the plan itself records, and the Claude r1 fix that asked for it is not yet applied. docs/plans/ace-share-ingestion-wave-coordination.md line 73 still records "Claude: pending; Codex: pending; Gemini: not-run" although all three r1 artifacts are materialized (Claude/Codex MAJOR verdicts; Gemini a sanitized `unavailable: unsupported-client` record — the exact form Claude r1's MINOR finding said to put in "the review summary and the coordination row"). The same row's headline "75-90%; difficulty 3/11" reflects only the `.xlsx` data-workbook row of the revised extension map, which now spans 3/11–6/11 with calculation workbooks at 40-80% and `.xls`/`.xlsm`/`.xlsb` deferred. Since the r2 scan commands include this file, the stale row will be committed as plan-review evidence unless updated. Fix: update the #53 row's review-artifact cell to the r1/r2 artifact paths plus `unavailable: unsupported-client` for Gemini, and refresh the ingestion/difficulty summary to match the revised extension map, in the same commit that moves the label.

- [MINOR] The no-committed-workbook-bytes gate has an `.ods` hole the plan itself opens. The extension map's `.xlsb`/`.ods` row permits "a synthetic fixture proves safe parser behavior," but Acceptance Criterion 4 and `test_runtime_generated_workbook_fixtures_only` forbid only `.xls`, `.xlsx`, `.xlsm`, and `.xlsb` under tracked fixtures (mirroring skills/xlsx-input-code-output-canary/SKILL.md line 87, which also omits `.ods`). A committed binary `.ods` fixture would satisfy every stated gate while violating the rule's intent (no raw workbook bytes in the repo). Fix: add `.ods` (or "any binary spreadsheet container") to the forbidden-extension list in AC 4 and the fixture test, and state that any future `.ods`/`.xlsb` fixture is runtime-generated like the others.

Checked:
- docs/plans/2026-06-29-issue-53-...md (full read) against both r1 artifacts — confirmed disposition of every r1 finding: mapping table + `excluded_workbook` rename present (Claude r1 M1/M6-fix, residue flagged above); fixtures now "CSV/delimited text and workbook inventory JSON only; raw workbook bytes generated in temp dirs at runtime" with two enforcement tests (Claude M2, Codex m3 — fixed); Deliverable now states #67 authorizes metadata-only request records only, success metrics synthetic-only, content-byte pilots require a future approved firewall-extension issue (Claude M3 — fixed); `.xls` downgraded to metadata-only/deferred with denominator exclusion and adapter-issue requirement, `.xlsm` macro/external-link flags added to the `xlsx_canary.py` change scope (Codex M1, Claude m5 — fixed); private measured sidecar deferred on "#61 approval plus implemented durable-output validator evidence" (Codex M2 — fixed); boundary test pins 200 rows / 25 files / 1048576 bytes with import-from-contract requirement (Codex m4 — fixed); scan commands rewritten to r2 artifacts with a materialized-set guard and sanitized-UNAVAILABLE rule for Gemini (Claude m4, Codex m5 — fixed).
- scripts/ace_bounded_sampling_firewall.py — confirmed caps {200, 25, 1048576} (lines 107–112), `output_shape`/`route_target`/`logical_target_store` pinned to the metadata-only boundary for all request classes (lines 281–284), and that the plan's "boundary accepted when other gates pass" test is satisfiable via the `metadata_only_fixture` request class (`AUTHORIZED_METADATA_ONLY_FIXTURE`, line 324) without operational evidence.
- scripts/ace_manifest_evidence_trust.py + artifacts/ace-manifest-freshness/trusted-evidence-registry.json — confirmed `trusted_evidence: []`, empty-registry fail-closed (`UNTRUSTED_62_EVIDENCE`, "no matching row"), and fixture-ref rejection (`FIXTURE_62_EVIDENCE_NOT_OPERATIONAL`), matching the plan's two evidence tests.
- skills/xlsx-input-code-output-canary/SKILL.md + resources/xlsx_canary.py — confirmed closed class set, no-committed-bytes rule (four extensions only), tempdir self-test pattern, cached-values-are-evidence-only, and absence of macro/external-link/layout signals (basis of finding 1's trigger-rule point).
- docs/09-office-formats.md, docs/10-structured-data-and-model-files.md, skills/format-coverage-ledger/SKILL.md — plan's characterizations (formula-graph-to-code with input contract/output proof; dialect/field-count/digest/convention sidecars; formulas/named-ranges/charts known-loss) are accurate; `skills/format-coverage-ledger/resources/` confirmed absent, matching the plan's MISSING evidence block.
- scripts/validate_ace_public_surface_scan.py + scripts/legal/legal-sanity-scan.sh → legal_sanity_scan.py — `--scan-public-path` (append) and `--diff-only` flags exist; both coordination validators named in the command block exist on disk.
- Live state via `gh`: #53 OPEN with exactly `strengthening, lane:codex, priority:high` (no status label); #61 and #63 OPEN `status:plan-review`; #71 CLOSED `status:plan-approved`; `.planning/plan-approved/` contains markers for 50/62/65–71 and none for 53/61/63 — all matching the plan's Resource Intelligence claims.
- docs/plans/README.md #53 row (draft, 2026-07-02, T3, gate notes) — consistent with the plan header; route-target closed set at README line 15 confirms `excluded_no_ingest`/`metadata_only` used in the mapping table are valid #65 route targets and that `excluded_workbook` avoids the collision.
- scripts/review/results/ — all three r1 artifacts materialized (gemini-r1 is a sanitized UNAVAILABLE record with no raw stderr, as the plan claims); claude-r2 exists as a 0-byte placeholder for this round.
