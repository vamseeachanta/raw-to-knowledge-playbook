review_artifact_role: public_history
normalization_note: denied-token/private-field/local-absolute-path substitutions only; provider verdict unchanged

## Verdict
MAJOR

## Retrieval
- Read `docs/plans/2026-06-29-issue-51-ace-wave-0-corpus-ledger-routing-firewall-sampling-protocol.md` lines 1-719.
- Read `scripts/validate_ace_epic_wave_coordination.py` lines 1-810, especially public-scan and review-artifact parsing logic.
- Read `docs/plans/ace-share-ingestion-wave-coordination.md` lines 1-77.
- Read `docs/plans/2026-06-29-issue-63-ace-public-output-redaction-and-identifier-canary.md` lines 1-211.
- Read `docs/plans/README.md` lines 1-41.
- Read `.github/workflows/validate.yml` lines 1-44.
- Read `docs/19-trust-boundary-and-private-mode.md` lines 50-95 and `docs/04-failure-modes.md` lines 75-100.
- Grep’d source-hash/private-field/token terms across #51/#63 plans and governance docs.
- Ran `UV_CACHE_DIR=.claude/state/uv-cache uv run python scripts/validate_ace_epic_wave_coordination.py --scan-public-path docs/plans/2026-06-29-issue-51-ace-wave-0-corpus-ledger-routing-firewall-sampling-protocol.md`: PASS.
- Ran parent validator and unit tests: both PASS.
- Ran `gh issue view 51` and `gh issue view 63` to verify live issue bodies and labels.
- Checked filesystem existence for downstream validator/example/eval paths named in the coordination registry.

## Findings
1. Plan scope still contradicts itself on the source-hash sweep. `docs/plans/2026-06-29-issue-51...md:117` says #51 will not implement the repo-wide source-hash policy sweep, and lines 237-242 assign that sweep and public-methodology edits to #63. But line 712 says, “Implementation must produce a deterministic report for every hit,” which reintroduces the sweep as an implementation requirement in the #51 plan. This is not harmless wording: existing published guidance still says `sha256` pointers are safe (`docs/19-trust-boundary-and-private-mode.md:75`, `docs/19-trust-boundary-and-private-mode.md:82`, `docs/04-failure-modes.md:88`), while #51 asserts raw source hashes remain private-sidecar provenance (`docs/plans/2026-06-29-issue-51...md:47-48`, `:585-586`). The plan must either keep the hash-policy change wholly blocked on #63 or explicitly own the governance change in #51.

2. The legal deny-list requirement is self-blocking or under-specified. The plan requires `.legal-deny-list.yaml` to include a private-root pattern class (`docs/plans/2026-06-29-issue-51...md:439-452`), while the same plan includes `.legal-deny-list.yaml` in the #51 public-surface scan set (`:383-395`, `:594-595`). The current fallback scanner flags raw private mount-path patterns as public leaks (`scripts/validate_ace_epic_wave_coordination.py:106-110`, `:734-747`). A committed literal private mount root in the deny-list would therefore fail the plan’s own scan unless an unplanned exemption is added. The plan also says Bash will parse YAML without `yq` (`docs/plans/2026-06-29-issue-51...md:443-455`) but does not define an exact YAML subset, escaping rules, or regex serialization.

3. The “exact issue/skill binding” control plane can pass with nonexistent downstream bindings. The plan says downstream rows may reference planned validator/canary paths without requiring file existence (`docs/plans/2026-06-29-issue-51...md:245-250`) and the test expectation repeats that downstream path existence is not required (`:566`). In the current coordination registry, #58 and #59 name missing example and eval paths: `examples/simulation-engineering-lane/check.py`, `skills/simulation-engineering-file-lane/evals/evals.json`, `examples/database-geospatial-index-lane/check.py`, and `skills/database-geospatial-structured-index-lane/evals/evals.json` (`docs/plans/ace-share-ingestion-wave-coordination.md:65-66`). Filesystem checks reported all four as missing. If #51 is the control plane that downstream waves must bind to, it needs to distinguish “planned future validator path” from “existing skill/eval binding,” or it will certify placeholders as operational routing.

4. The public-scan target set is inconsistent across plan phases. The pseudocode and generic self-scan boundary include fixed governance docs and planning surfaces (`docs/plans/2026-06-29-issue-51...md:306-320`, `:383-395`, `:595`), but the pre-label checklist only scans `docs/04`, `docs/05`, `docs/07`, `docs/18`, and `docs/19` “if touched” (`:625`). Current CI scans only the coordination doc, #50 plan, README, two skills, and the #50 approval marker (`.github/workflows/validate.yml:21-29`). That means status-transition evidence can skip unchanged public docs that still contradict #51’s raw-hash policy. The plan needs one authoritative scan manifest with explicit phase differences.

## Blockers
- Finding 1: resolve the source-hash sweep ownership/policy contradiction.
- Finding 2: make the legal deny-list self-scan-safe and define the parser/schema precisely.
- Finding 3: prevent the control plane from certifying nonexistent or placeholder downstream bindings.
- Finding 4: make pre-label, CI, and #51 validator scan targets consistent enough that public-surface claims are actually checked.
