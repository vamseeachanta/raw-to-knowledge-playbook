# Plan Review: #71 r1 Claude Lane

Verdict: MAJOR

## Findings

- MAJOR: The draft allowed an empty `plan_path` bypass for non-ready rows whose expected plan file already exists. `artifacts/ace-wave0-ledger-schema.json` records empty plan paths for #68 and #69, but both repo-local plan files exist and README/coordination status surfaces already describe their states. The plan needed to require expected path equality for every #65-#69 row whose expected plan exists, not only for rows with a non-empty `plan_path`.
- MAJOR: The draft did not state explicit target values for #68 and #69 schema normalization. Without target rows, implementation could preserve the current stale `plan-required` state or turn the current schema red without documenting the intended correction.
- MAJOR: Cross-source status consistency was underspecified. The plan referred to README and coordination consistency but did not define normalization or precedence across prefixed schema snapshots, unprefixed README statuses, coordination prose, and #68's blocked-draft state.
- MINOR: The proposed status vocabulary mixed prefixed and unprefixed values, preserving the ambiguity the issue is meant to remove.
- MINOR: The parent #62 handoff plan risked hardcoding #62-owned contract field values in a second production source instead of loading the contract as the authority and checking docs/evidence against the loaded values.
- MINOR: The negative-fixture conversion section did not enumerate all existing denied-literal classes that must be converted before adding a self-scan assertion for `tests/test_validate_ace_epic_wave_coordination.py`.

## Checks

- Read `docs/plans/2026-07-01-issue-71-ace-validator-hardening-non-ready-split-registry.md`.
- Verified `_validate_split_registry()` in `scripts/validate_ace_wave0_schema_contract.py` exits early for `implementation_ready=false` before plan/status checks.
- Verified #68 and #69 plan files exist while `artifacts/ace-wave0-ledger-schema.json` still records empty plan paths and `plan-required` snapshots for those rows.
- Cross-checked status surfaces in `docs/plans/README.md` and `docs/plans/ace-share-ingestion-wave-coordination.md`.
- Checked the #62 contract fields and manifest source list in `config/ace-manifest-evidence-contract.json`.
- Checked that the parent test file is not currently a workflow public-scan target and still contains denied-fixture patterns that must be converted before self-scan.
