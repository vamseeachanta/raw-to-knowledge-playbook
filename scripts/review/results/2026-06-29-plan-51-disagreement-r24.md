review_artifact_role: public_history
normalization_note: denied-token/private-field/local-absolute-path substitutions only; provider verdict unchanged

# Disagreement report — plan #51 (2026-06-29)

## Verdicts

| Provider | Verdict |
|---|---|
| claude | MINOR |
| codex | MAJOR |
| gemini | UNAVAILABLE (gemini CLI failed, rc=41: Error authenticating: FatalAuthenticationError: Manual authorization is required but the current session is non-interactive. Please run the Gemini CLI in an interactive terminal to log in, provide a GEMINI_API_KEY, or ensure Application Default Credentials are configured.     at initOauthClient ([LOCAL_CLI_STACK_PATH])) |

## Findings unique to each provider

A finding is 'unique to X' if its text appears in X's artifact but not
verbatim in any other provider's artifact.

### claude

- **Non-convergence after 23 rounds is itself evidence the work unit is mis-sized (Adversarial Review Summary, lines 638-702).** Both active providers returned MAJOR on *every* round r3→r23 — 21 consecutive rounds — and r23 is still dual-MAJOR (line 702). Reviewers explicitly asked to "split OR explicitly accept" the broad front-loaded scope at r20, r22, and r23 (lines 689, 695, 698). The plan's response was to *accept* rather than split, but a single "T3" issue that creates five new Python modules (`ace_public_tokens.py`, `ace_public_surface_scan.py`, `ace_sampling_firewall.py`, `validate_ace_wave0_control_plane.py`, the legal wrapper), a YAML deny-list, two fixture trees, ~40 TDD tests, and modifies five skills plus three docs plus the workflow (Files to Change, lines 491-517) is several issues' worth of surface. The inability to clear two reviewers across 23 iterations is the strongest available signal that the decomposition, not the wording, is the problem.
- **The plan structurally cannot reach `status:plan-review` without an out-of-band user waiver, and that gate is unresolved (lines 462-473, 711).** Gemini has failed noninteractive auth with `rc=41` for 21 straight rounds (r3-r23). The plan's own degraded-quorum rule says Gemini auth failure "does not automatically authorize downgrade" and `status:plan-review` is "blocked until Gemini is restored or the user explicitly approves a one-round degraded review quorum." No such restoration or approval is recorded. This is a real, currently-open blocker to the plan's advancement that the plan cannot self-resolve.
- **#51's plan-review gate is coupled to mutating sibling issue #63 (lines 374-382, 624; Files to Change line 505).** The Pre-Label Checklist requires editing the *live #63 issue body* and patching `docs/plans/2026-06-29-issue-63-...md` before #51 can transition. #63 is an independently-open issue with its own plan and lane; gating #51's approval on edits to #63 creates a coordination hazard (concurrent edits, ownership ambiguity) that the "one-time plan patch" framing (line 378) does not fully neutralize.
- **Pre-label scanning runs weaker patterns than #51 will eventually enforce, and the plan does not establish sufficiency (line 625).** Because `scripts/validate_ace_wave0_control_plane.py` does not exist until implementation, all pre-label public-surface scanning is delegated to the parent `validate_ace_epic_wave_coordination.py`. I confirmed that validator has leak/traversal/digest patterns — but #51 defines materially richer checks it considers necessary (executable-context classification, the good/bad fixture marker grammar, the metadata-evidence shape, `--scan-fixture-root`). The plan asserts the parent is an adequate "fallback" but provides no evidence that the parent's pattern set covers the #51-specific surfaces (e.g. `.legal-deny-list.yaml`, the new Python modules). The gate that actually runs at label time is thus looser than the one the plan specifies.
- **Several acceptance criteria remain compound despite r23 flagging exactly this (line 698: "decomposing compound acceptance criteria").** Criteria at lines 581, 585, 589, and 594 each bundle 4-6 independent assertions into one checkbox (e.g. line 589 mixes the four-row `wave_class` matrix, the #52-#60 requirement, the ingestion-binding rejection rule, AND the #50 portfolio carve-out). A reviewer cannot check these off atomically, which undercuts their use as a pass/fail gate.
- **`secrets.token_hex(16)` ↔ `pst_[0-9a-f]{32}` prefix is left implicit (lines 182, 188).** `secrets.token_hex(16)` yields 32 hex chars with no `pst_` prefix; the grammar requires `pst_` + 32 hex. The plan never states that `generate_public_source_token()` prepends the literal `pst_`, so an implementer could emit a 32-char token that fails the very grammar the plan mandates. Minor, but it is the one place the token contract is underspecified.

### codex

- Plan scope still contradicts itself on the source-hash sweep. `docs/plans/2026-06-29-issue-51...md:117` says #51 will not implement the repo-wide source-hash policy sweep, and lines 237-242 assign that sweep and public-methodology edits to #63. But line 712 says, “Implementation must produce a deterministic report for every hit,” which reintroduces the sweep as an implementation requirement in the #51 plan. This is not harmless wording: existing published guidance still says `sha256` pointers are safe (`docs/19-trust-boundary-and-private-mode.md:75`, `docs/19-trust-boundary-and-private-mode.md:82`, `docs/04-failure-modes.md:88`), while #51 asserts raw source hashes remain private-sidecar provenance (`docs/plans/2026-06-29-issue-51...md:47-48`, `:585-586`). The plan must either keep the hash-policy change wholly blocked on #63 or explicitly own the governance change in #51.
- The legal deny-list requirement is self-blocking or under-specified. The plan requires `.legal-deny-list.yaml` to include a private-root pattern class (`docs/plans/2026-06-29-issue-51...md:439-452`), while the same plan includes `.legal-deny-list.yaml` in the #51 public-surface scan set (`:383-395`, `:594-595`). The current fallback scanner flags raw private mount-path patterns as public leaks (`scripts/validate_ace_epic_wave_coordination.py:106-110`, `:734-747`). A committed literal private mount root in the deny-list would therefore fail the plan’s own scan unless an unplanned exemption is added. The plan also says Bash will parse YAML without `yq` (`docs/plans/2026-06-29-issue-51...md:443-455`) but does not define an exact YAML subset, escaping rules, or regex serialization.
- The “exact issue/skill binding” control plane can pass with nonexistent downstream bindings. The plan says downstream rows may reference planned validator/canary paths without requiring file existence (`docs/plans/2026-06-29-issue-51...md:245-250`) and the test expectation repeats that downstream path existence is not required (`:566`). In the current coordination registry, #58 and #59 name missing example and eval paths: `examples/simulation-engineering-lane/check.py`, `skills/simulation-engineering-file-lane/evals/evals.json`, `examples/database-geospatial-index-lane/check.py`, and `skills/database-geospatial-structured-index-lane/evals/evals.json` (`docs/plans/ace-share-ingestion-wave-coordination.md:65-66`). Filesystem checks reported all four as missing. If #51 is the control plane that downstream waves must bind to, it needs to distinguish “planned future validator path” from “existing skill/eval binding,” or it will certify placeholders as operational routing.
- The public-scan target set is inconsistent across plan phases. The pseudocode and generic self-scan boundary include fixed governance docs and planning surfaces (`docs/plans/2026-06-29-issue-51...md:306-320`, `:383-395`, `:595`), but the pre-label checklist only scans `docs/04`, `docs/05`, `docs/07`, `docs/18`, and `docs/19` “if touched” (`:625`). Current CI scans only the coordination doc, #50 plan, README, two skills, and the #50 approval marker (`.github/workflows/validate.yml:21-29`). That means status-transition evidence can skip unchanged public docs that still contradict #51’s raw-hash policy. The plan needs one authoritative scan manifest with explicit phase differences.

### gemini

- (none)
