review_artifact_role: public_history
normalization_note: denied-token/private-field/local-absolute-path substitutions only; provider verdict unchanged

I've verified the plan's concrete claims thoroughly. The retrieval checks out remarkably well (expected after 22 rounds), so my adversarial value is in what survives verification: scope non-convergence, an unsatisfiable review gate, and an under-specified self-referential scanner mechanism.

`★ Insight ─────────────────────────────────────`
- This plan is a **self-referential governance artifact**: it lives at `docs/plans/` (an orphan-but-still-built MkDocs page), declares *itself* a public surface (line 350), and must pass the very scanner it specifies. I exploited that by actually running the declared pre-label gate against the committed plan — a plan that fails its own gate is a hard blocker. It passed.
- The hardest correctness problem here is the **definitional-vs-fixture discrimination** (lines 206-209): the same token string must be *legal as grammar prose* and *illegal as fixture content* in the same Markdown file. That's a parser-context problem, not a regex problem, and it's the recurring failure across rounds.
- A T3 review gate with one provider down for 20 consecutive rounds isn't "almost passing" — it's *structurally unsatisfiable* on the mechanical path, which is a different class of blocker than any single finding.
`─────────────────────────────────────────────────`

## Verdict
MAJOR

## Retrieval
- Read the full plan: `docs/plans/2026-06-29-issue-51-...-sampling-protocol.md` lines 1-776 (both pages).
- Read the review prompt: `workspace-hub scripts/review/plan-review-prompt.md` (adjacent checkout path elided).
- Verified file-existence claims: confirmed all 4 plan-declared MISSING files are absent; confirmed all 6 docs + 5 skill `SKILL.md` + `skills/README.md` exist; confirmed the 4 to-be-created config/legal files (`config/ace-public-output-contract.json`, `config/ace-public-surface-deny-list.json`, `.legal-deny-list.yaml`, `scripts/legal/legal-sanity-scan.sh`) are all currently MISSING.
- Fetched live issue `#51` body + labels via `gh`; confirmed labels `strengthening,lane:claude,priority:high` and the 5-bullet acceptance criteria.
- Confirmed states of `#50,#52-#63` via `gh issue view` (all OPEN; #52-#60 are waves 1-9; #61/#62/#63 are the named cross-wave gates).
- Verified `#63` plan references `config/ace-public-output-contract.json` (lines 19,36,65,85,123,135-147) — the shared-config dependency claim holds.
- Read `mkdocs.yml`: has explicit `nav:` (line 49) but no `exclude_docs`/`not_in_nav`, so `docs/plans/**` build as orphan pages → the "treat docs/plans as published" stance is correct.
- Inspected `scripts/validate_ace_epic_wave_coordination.py`: confirmed `--scan-public-path` (line 782), `wave_class`/`requires_manifest_snapshot_id` parsing (lines 86-87, 664-668), Structural Wave Gate Registry parsing (lines 265-300), and `validate_public_artifact_paths()` applying DENIED_TRAVERSAL/PRIVATE_LEAK/PRIVATE_SOURCE_FIELD/SOURCE_LIKE_RAW_DIGEST pattern families + metadata-evidence allowlist.
- Confirmed `docs/plans/ace-share-ingestion-wave-coordination.md` contains the registry (lines 34-38).
- **Ran the declared pre-label gate** `uv run python scripts/validate_ace_epic_wave_coordination.py --scan-public-path <the #51 plan>` → `PASS`, exit 0.
- Inspected `workspace-hub scripts/legal/legal-sanity-scan.sh` (adjacent checkout path elided) (the inherited precedent): parses `.legal-deny-list.yaml` "lightweight, no yq dependency" via grep (lines 108-165).
- Confirmed `.github/workflows/validate.yml` content, `.planning/plan-approved/50.md` exists, `git diff --check` clean, plan committed at `d397871`.
- Confirmed `AGENTS.md` is **absent** from this repo; exists only at `workspace-hub/AGENTS.md` (adjacent checkout path elided).

## Findings

1. **The review gate is structurally unsatisfiable on the mechanical path — Gemini has failed identically (rc=41) for 20 consecutive rounds (r3-r22), per Adversarial Review Summary lines 699-756.** The plan's own degraded-quorum rule (lines 502-513, 688) states Gemini noninteractive auth failure "does not automatically authorize downgrade" and "blocks `status:plan-review` unless Gemini is restored or the user explicitly approves." Twenty rounds of identical failure is not a transient outage; the plan cannot advance via "fresh no-MAJOR round" alone — it *requires* an out-of-band user waiver every time. This is a process blocker that no amount of plan editing resolves.

2. **The plan has not converged after 22 rounds and its own latest verdict is double-MAJOR (line 758).** Issue #51's acceptance criteria are 5 bullets (verified in the live body: ledger schema, route distinction, issue/skill binding, bounded sampling, closeout method-gap rule). The plan expands this to ~30 acceptance criteria, 14 Created files, ~12 Modified files, edits to the #50 and #63 plans, edits to two live GitHub issue bodies, a repo-wide `sha256` hash-policy sweep, a legal scanner, and two config contracts. The Scope Decision (line 122) itself names a split point (move the source-hash sweep + shared-config creation to a follow-on issue). Persistent non-convergence across 22 rounds is direct evidence the artifact is too large to review tractably; the named split should be taken before plan-review, not offered conditionally.

3. **The core self-referential scanner mechanism is asserted but not specified — definitional-vs-fixture discrimination (lines 203-217).** The plan requires the not-yet-written #51 scanner to reject <PUBLIC_TOKEN_MARKER_EXAMPLE> "only when parsed as fixture/control-plane content … never by raw substring matching over methodology prose" (lines 207-209), and similarly for the private-field placeholders (lines 216-217). No parse contract is given for how the scanner decides a line inside the plan's own Markdown is "grammar specification" versus "a parsed ledger row." This same gap recurs in the review history (Codex r11 "marker self-scan/lint sequencing", r17, r18 "structural marker self-scan scoping"). Without a concrete discriminator, the scanner is at risk of either self-blocking the plan or admitting the very strings it must reject — and it is unverifiable until built.

4. **Acceptance criteria bundle multiple independent assertions into single non-atomic checkboxes, defeating objective pass/fail.** E.g. line 645 is a ~150-word paragraph asserting hit-key composition, dual-purpose precedence, two outcome classes, skill-catalog inclusion, and digest-value rules under one `- [ ]`. Line 642 and line 659 are similar. A checkbox that bundles 5+ verifiable conditions cannot be checked atomically; partial satisfaction is indistinguishable from full satisfaction.

5. **The "Repo file existence" evidence block (lines 76-80) is asymmetric/incomplete.** It records MISSING for only 4 of the 14 files the plan will Create — it omits `config/ace-public-output-contract.json`, `config/ace-public-surface-deny-list.json`, `.legal-deny-list.yaml`, and `scripts/legal/legal-sanity-scan.sh`. I verified all four are in fact currently MISSING, so the plan is not wrong — but its evidence section does not actually substantiate the create-state of the files it most depends on (the legal gate and the shared config that #63 already consumes).

6. **The plan binds itself to an "inherited AGENTS gate" / "AGENTS cross-review rule" (lines 504, 543; degraded-quorum policy) that does not exist in this repository.** `AGENTS.md` is present only at `workspace-hub/AGENTS.md` (adjacent checkout path elided). A contributor or reviewer operating within `raw-to-knowledge-playbook` cannot see the governing cross-review/quorum rule the entire plan-review process is predicated on. The plan correctly makes the *legal scanner* repo-local to avoid adjacent-checkout dependence (line 492-493) but leaves the *quorum rule itself* as an invisible cross-repo dependency.

7. **(Verified-not-a-defect, recorded per silence-is-failure.)** I specifically tested and could NOT fault: (a) the plan passes its own declared pre-label scanner (exit 0); (b) the `#63` shared-config dependency is real and reciprocal; (c) the `docs/plans` publication caution is correct given MkDocs orphan-page building; (d) the parent coordination validator genuinely implements the private-field/source-hash/denied-token pattern families the pre-label checklist relies on; (e) the YAML deny-list is parseable by a no-yq bash wrapper per the workspace-hub precedent, so the `.yaml`-vs-"JSON-only configs" wording is not a real contradiction (the JSON-only rule at line 241 is scoped to the two `config/*.json` files). These checks passed.

## Blockers
- **Finding 1** — The T3 quorum cannot be satisfied mechanically while Gemini is down; advancing to `status:plan-review` requires an explicit, recorded user waiver (or Gemini restoration). Must be resolved before any plan-review label.
- **Finding 2** — Scope must be split (per the plan's own line 122 split point) or the user must explicitly accept the front-loaded cross-wave scope on the record; 22 rounds of non-convergence indicates the current bundle is not reviewable as one unit.
- **Finding 3** — The definitional-vs-fixture parse contract for the #51 scanner must be specified concretely (what structural signal marks a line as fixture content vs. grammar prose) before implementation, or the scanner's correctness is unprovable and the plan's self-scan safety claims are unfounded.

Findings 4, 5, and 6 are MINOR and need not block, but 6 is cheap to fix (vendor or link the AGENTS quorum rule into the repo) and worth doing before relying on that rule as the status-transition authority.
