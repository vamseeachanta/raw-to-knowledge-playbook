Normalization note: denied-command and private-source examples in this review artifact are tokenized so the artifact can be safely retained in public review history without becoming its own scanner violation.

I have enough to render a verdict. Let me give a quick educational note on the core defect, then the structured review.

`★ Insight ─────────────────────────────────────`
- The playbook itself warns (doc 02:106) to *"keep `structural_status` and `parse_status` vocabularies strictly"* separate — token reuse across status fields is a known foot-gun in this codebase. A schema whose stated job is to make these vocabularies "machine-checkable and precise" is held to that bar.
- The parent scanner's self-exemption (`_allowed_internal_traversal_line`, line 752) is *filename-locked* to `validate_ace_epic_wave_coordination.py`. Any new validator that "reuses the parent scanner" inherits its denial rules but **not** its self-exemption — a classic asymmetry that bites self-scanning artifacts.
`─────────────────────────────────────────────────`

## Verdict
MAJOR

## Findings

### 1. [MAJOR] `control_plane_verification_state` collides with the #61 lifecycle enum and `parse_status` on the token `rejected`; the plan's "explicitly separate" claim is false and its test spec is self-contradictory.
- **Evidence:** Plan acceptance criterion (line 209): the enum is *"closed to `not_verified`, `validator_passed`, `independent_review_passed`, and `rejected`, and is explicitly separate from #61 lifecycle states and `page-shape-contract` `parse_status` values."* But `rejected` is a member of all three sets:
  - #61 lifecycle (`docs/plans/README.md:16`): `candidate, provisional, verified, rejected, superseded, stale_requires_rescreen`.
  - `parse_status` (`docs/07-data-governance.md:47`, `skills/page-shape-contract/SKILL.md:37`, `docs/03-verification-playbook.md:91`, `skills/verify-batch/SKILL.md:84`): `... | rejected | deferred`.
  - Plan's verification enum: `... rejected`.
  - TDD row (line 187) asserts *"Exactly four control-plane verification states are accepted **and lifecycle/parse_status values are not accepted** in that field."* This is unsatisfiable as written: `rejected` must be **accepted** (it is one of the four) yet is simultaneously a lifecycle and a `parse_status` value the test claims is **rejected**.
- **Why it matters:** The entire deliverable (line 99) is a schema that "proves the closed route/store/verification … contract." A field whose value `rejected` is ambiguous across three distinct status vocabularies is exactly the vocabulary collision the playbook prohibits (`docs/02-pipeline-architecture.md:106`), and it ships an acceptance criterion that is factually wrong plus a test that can only "pass" by silently omitting the colliding token — masking the defect rather than catching it.
- **Required fix:** Either rename the control-plane terminal value (e.g. `verification_rejected`) so the four states are genuinely disjoint from lifecycle/`parse_status`, or drop the blanket "explicitly separate" claim and replace it with a precise statement ("distinct *field*; value `rejected` is intentionally shared and disambiguated by field name"), and rewrite the TDD assertion so its negative samples are the genuinely-foreign tokens (`candidate`, `provisional`, `superseded`, `stale_requires_rescreen`, `deferred`, `provisional-unverified`) — never `rejected`.

### 2. [MINOR] Route-enum ownership drift: README still says route targets are "owned by #51," but the plan makes #65 the definer, and the planned README edit does not reconcile this.
- **Evidence:** `docs/plans/README.md:15`: *"Route targets are a closed set owned by [#51]: `public_llm_wiki`, `private_sidecar`, `metadata_only`, and `excluded_no_ingest`."* Plan line 29/206: #65 "will define the schema and route-store vocabulary" and acceptance "Route targets are closed to [the four]." Files-to-Change (line 169) only edits README to *"Mark #65 as planned/reviewed … without implying approval"* — it does not move or qualify the "owned by #51" assertion.
- **Why it matters:** After #65 lands, the prose source of truth (README) and the machine-readable schema artifact will disagree on who owns the route enum, reintroducing the parent/child boundary confusion the split was meant to remove.
- **Required fix:** Add an explicit ownership reconciliation to the README edit — e.g. "route enum *conceptually* owned by #51 umbrella, *machine-defined* by #65 schema" — or quote #51's umbrella delegation so the two artifacts cannot contradict.

### 3. [MINOR] Self-scan feasibility: the plan requires the reused parent scanner to scan its own validator and test sources, but the parent scanner's self-exemption is filename-locked and will not cover the new files.
- **Evidence:** Plan acceptance line 214 requires scanning *"… schema JSON, validator, unit tests, workflow …"* with the reused parent scanner (pseudocode line 153: *"reuse the parent public scanner where possible"*). The parent scanner denies `[denied-python-rglob-token]`/`[denied-python-walk-token]` denied-command prose (`scripts/validate_ace_epic_wave_coordination.py:153-154`) and only exempts them when `path.name == "validate_ace_epic_wave_coordination.py"` (line 752-758). The new files (`scripts/validate_ace_wave0_schema_contract.py`, `tests/test_validate_ace_wave0_schema_contract.py`) are **not** exempted. The existing parent test embeds a literal `[private-source-field-assignment-token]` fixture (test line 882) and `<LOCAL_MOUNT_PATTERN>` fixtures and gets away with it **only because the test file is never self-scanned** — a freedom the #65 test file does not have.
- **Why it matters:** If the #65 validator walks the review-results directory with `.rglob`/`os.walk`, or the #65 test embeds a literal `field: value` / hex digest fixture, the self-scan it mandates will flag its own committed sources — the plan addresses committed *negative-fixture* examples (line 215) but never the validator/test-source rglob/os.walk-sentinel collision.
- **Required fix:** State explicitly that the #65 validator must use non-recursive `Path.glob()` (not `.rglob`/`os.walk`) for its review-artifact discovery and that all detection literals/fixtures in both new files are fragment-assembled, OR extend the scanner's self-exemption to the new filenames and add a test proving the new validator and test files pass their own scan (analogous to `test_validator_source_is_not_self_blocking_public_artifact`, test line 971).

## Checks Performed
- Read the plan: `docs/plans/2026-06-30-issue-65-ace-wave-0-ledger-schema-route-store-matrix.md`.
- Read `docs/plans/README.md` (route enum ownership line 15; #61 lifecycle line 16; split registry lines 45-55) and `docs/plans/ace-share-ingestion-wave-coordination.md` (split registry lines 35-45; child ledger; structural registry).
- Read the validator `scripts/validate_ace_epic_wave_coordination.py` in full: confirmed `EXPECTED_CHILD_ISSUES = range(51,64)`, `EXPECTED_EXECUTABLE_BINDINGS[51] = {scripts/validate_ace_wave0_control_plane.py}` (no collision with #65's `scripts/validate_ace_wave0_schema_contract.py`), the leak/traversal/private-field/source-digest scan patterns, and the filename-locked `_allowed_internal_traversal_line` self-exemption.
- Read `tests/test_validate_ace_epic_wave_coordination.py` (confirmed fixture-as-literal `source_id:` pattern at line 882 and the self-block guard test at 971).
- Read `.github/workflows/validate.yml` (static `--scan-public-path` list; no #65 wiring yet, consistent with "after approved implementation").
- Read governance docs `docs/19-trust-boundary-and-private-mode.md`, `docs/07-data-governance.md` (trust-label `rejected` at line 47), `docs/01-document-taxonomy.md`.
- Grepped `parse_status`/lifecycle vocabularies across `docs/` and `skills/`: confirmed `rejected` ∈ `{parse_status}`, ∈ `{#61 lifecycle}`, and ∈ plan's control-plane enum.
- Grepped the #65 plan for leak/traversal/private-field/digest patterns: none present, so the plan file itself currently passes the parent scanner (route names `private_sidecar`/`private_sidecar_store` are not in `PRIVATE_SOURCE_FIELDS`, so no false trip).
- Confirmed #65-#69 rows live only under the split-registry sections, not under `## Child Wave Ledger`/`## Structural Wave Gate Registry`, so they are not parsed as "unexpected child issues" by the parent validator.

## Missing Evidence
- I could not verify runtime behavior: the new validator, test, and schema JSON do not exist yet (confirmed `MISSING` in the plan's own evidence block, lines 69-71), so the self-scan feasibility (Finding 3) and the unsatisfiable-test claim (Finding 1) are assessed from the spec text, not from executing code.
- I did not independently confirm the live GitHub issue labels for #65/#66-#69 (`strengthening,lane:claude,priority:high` per plan line 54) — no network/`gh` call was made; the plan and coordination both treat live labels as pre-label evidence only.
- I did not read the full bodies of all five bound skills (`format-coverage-ledger`, `public-private-routing`, `content-triage-and-exclusion`, `adversarial-verify-loop`) beyond the `parse_status`/lifecycle greps; the plan's "conditional modify" of those docs is contingent on implementation and not yet realized, so it could not be verified.
- The three formal review artifacts named in the Artifact Map (`…plan-65-{claude,codex,gemini}.md`) do not exist yet, consistent with the plan's PENDING status; their eventual contents (and whether they would pass the #65 self-scan) could not be checked.
