# Plan Review: #71 r1 Codex Lane

Verdict: MAJOR

## Findings

- MAJOR: The plan's file-existence evidence was stale. It said the #71 plan file was missing even though the file existed locally and was the review target.
- MAJOR: The draft did not close the empty-plan-path bypass for #68 and #69. Live issue/status checks and repo-local files showed #69 already in plan-review and #68 with a blocked-draft plan, while the schema still recorded empty paths and `plan-required`.
- MAJOR: The draft did not pin an exact status-source precedence rule. README, coordination, plan headers, and schema snapshots use different vocabularies; without a precedence map, the proposed drift tests would be nondeterministic.
- MINOR: The #62-related expansion was in scope because the issue comment explicitly requested six-source manifest membership, scan-safe negative fixtures, and #62 handoff enforcement. The defect was not scope creep; it was the risk of making a second hardcoded contract authority.
- MINOR: The parent test source currently fails the parent public scanner when scanned directly. The plan needed to require conversion of every current denied-literal class before adding the self-scan assertion.

## Checks

- Read `docs/plans/2026-07-01-issue-71-ace-validator-hardening-non-ready-split-registry.md`, `docs/plans/README.md`, `docs/plans/ace-share-ingestion-wave-coordination.md`, `artifacts/ace-wave0-ledger-schema.json`, both validators, parent tests, and the #62 contract.
- Verified the #66/#67 mutated non-ready regression still returns zero errors in the current validator.
- Verified the #71 issue body and owner comment with `gh issue view`.
- Verified #66, #67, #68, and #69 live labels with `gh issue view`.
- Ran a targeted public scan of the #71 draft plan successfully.
- Ran a direct public scan of `tests/test_validate_ace_epic_wave_coordination.py`; it failed on existing denied fixture patterns, confirming the self-scan conversion risk.

## Process note

The Codex CLI timed out while attempting a nonessential cleanup-skill lookup after producing the findings above. This artifact records the findings emitted before the timeout and is intentionally treated as a MAJOR r1 review, not as an approval.
