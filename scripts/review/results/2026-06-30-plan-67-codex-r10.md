## Verdict
MAJOR

## Retrieval
- Read inline plan body and verified local plan path `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md`; grep'd cited sections at lines 164-187, 197-248, 376-379, 429-477, 484-510, and 532-570.
- Read `artifacts/ace-wave0-ledger-schema.json` lines 1-330, including route/store matrix, bound skills, canonical registry, and wave0 split registry.
- Read `docs/plans/ace-share-ingestion-wave-coordination.md` lines 1-90 for manifest sources, caps, structural wave registry, and #67 status.
- Read `scripts/validate_ace_epic_wave_coordination.py` lines 145-158 and 774-799 for denied traversal patterns and `--scan-public-path`.
- Read `.github/workflows/validate.yml` lines 1-44.
- Grep'd `scripts/review/results/2026-06-30-plan-67-*.md`, especially r9 Claude/Codex/disagreement artifacts.
- Ran `gh issue view` for #65, #67, and #70.
- Ran `python scripts/validate_ace_epic_wave_coordination.py`, `python scripts/validate_ace_wave0_schema_contract.py`, and `python -m unittest tests.test_validate_ace_epic_wave_coordination tests.test_validate_ace_wave0_schema_contract`.
- Ran parent public scanner over the #67 plan and r9 review artifacts with `--scan-public-path`.

## Findings
1. `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md` §Proposed Contract Shape says it will create a “closed JSON contract,” but `required_sampling_fields` at line 174 uses prose labels instead of exact JSON keys for core fields such as manifest source, seed, per-bucket row cap, maximum files touched, maximum bytes touched, and evidence requirement. The fixture table at lines 246-248 only pins exact names for a subset (`target_issue`, `request_class`, `fixture_scope`, `output_shape`, `route_target`, `logical_target_store`). The tests at lines 429-448 also describe missing fields in prose. This leaves the contract non-closed at the field-name level.

2. `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md` requires a source-root refusal boundary but never names the helper or the entrypoints that must route through it. The pseudocode at lines 376-379 says “the refusal helper”; the test row at line 446 says “source-root boundary/refusal helper”; the acceptance criterion at line 503 says “the named source-root boundary helper.” No function name, module API, or required call sites are specified. A narrow representative operation could pass while another validator path still touches `ACE_SHARE_ROOT`.

3. The plan remains explicitly unpassable under its own gate state unless a new same-round paired review also returns no-MAJOR. The header at line 10 records “r9 Claude MINOR, Codex MAJOR”; the gate rule at line 534 says any usable MAJOR keeps #67 draft; line 570 says “Overall result: MAJOR.” `docs/plans/README.md:33` and `docs/plans/ace-share-ingestion-wave-coordination.md:43` also record #67 as draft requiring re-review. This is not an implementation defect, but it blocks any move to `status:plan-review` from this review alone.

## Blockers
- Finding 1: Pin exact JSON key names for every required sampling field and add unknown-key/extra-field rejection coverage before implementation.
- Finding 2: Name the source-root refusal API and enumerate the validator/classifier call sites that must use it before implementation.
