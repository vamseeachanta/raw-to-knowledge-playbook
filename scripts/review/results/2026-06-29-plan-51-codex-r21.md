review_artifact_role: public_history
normalization_note: denied-token literal substitutions only; provider verdict unchanged

## Verdict
MAJOR

## Retrieval
- Attempted to read `config/agents/codex/MEMORY.runtime.md` and `.claude/skills/coordination/issue-planning-mode/SKILL.md`; both paths are absent in this checkout.
- Enumerated repo files with `rg --files`; confirmed the #51 plan, #50/#63 plans, coordination validator/tests, workflow, skills, and review artifacts present.
- Read `docs/plans/2026-06-29-issue-51-ace-wave-0-corpus-ledger-routing-firewall-sampling-protocol.md` with line numbers, including lines 104-110, 148-162, 184-222, 296-365, 540-565, 586-606, 620-638, and 642-701.
- Read `scripts/validate_ace_epic_wave_coordination.py` lines 1-160, 360-460, and 560-700.
- Read `tests/test_validate_ace_epic_wave_coordination.py` lines 800-865.
- Read `.github/workflows/validate.yml` lines 1-37.
- Grep’d `docs/plans/ace-share-ingestion-wave-coordination.md`, `docs/plans/README.md`, and `docs/plans/2026-06-29-issue-63-ace-public-output-redaction-and-identifier-canary.md` for status, public-token, hash, and scan-contract claims.
- Grep’d `docs/**/*.md` and `skills/**/*.md` for `sha256`, `source hash`, `provenance pointer`, and `source_sha256`.
- Grep’d `scripts/review/results/2026-06-29-plan-51-*.md` for verdict headings, reviewed-draft identity fields, and `review_artifact_role`.
- Checked `git status --short` and `git ls-files scripts/review/results/2026-06-29-plan-51-*.md` to distinguish tracked review artifacts from untracked local residue.
- Ran `UV_CACHE_DIR=.claude/state/uv-cache uv run python scripts/validate_ace_epic_wave_coordination.py` default, with `--scan-public-path` for the #51 plan, for the r20 status-evidence artifacts, and for the full `scripts/review/results` directory.
- Checked for `scripts/legal/legal-sanity-scan.sh`, `.legal-deny-list.yaml`, and `scripts/legal/**`; none exist.
- Fetched live GitHub issue bodies for #51 and #63 with `gh issue view ... --json number,title,state,labels,body,url`.

## Findings
1. The plan has no safe representation for required private ledger fields in canonical good/public fixtures. Plan lines 152-161 require ledger fields including `source_id`, `source_sha256`, `private_lookup_key`, and `share_relative_path_private_only`; lines 302-313 and acceptance line 604 put `tests/fixtures/ace-wave0-control-plane/good/` and the control-plane artifact in the public-surface scan set; lines 297-300, 353-364, 546, and 593 reject assigned private field values, key-value rows, path-bearing values, and raw digests everywhere. The only defined fixture marker grammar is for `public_source_token` at lines 199-203. Without an explicit placeholder grammar for the private-only required fields, full good ledger rows either fail the scanner or cannot exercise the required schema.

2. The legal/security gate fallback is not executable from this repo as written. Acceptance line 623 says that if `scripts/legal/legal-sanity-scan.sh` is absent, closeout must run an equivalent `.legal-deny-list.yaml` diff scan. I verified `scripts/legal/legal-sanity-scan.sh`, `scripts/legal/**`, and `.legal-deny-list.yaml` are all absent, and the plan does not add a legal config/script or name a concrete fallback command. That leaves a hard gate dependent on an unspecified external artifact.

3. The review-artifact scope is still internally loose. Artifact map lines 108-110 say historical/local `scripts/review/results/2026-06-29-plan-51-*-r*.md` artifacts carry explicit `review_artifact_role` and that every retained `plan-51-*.md` artifact is scanned. Pre-label line 633 narrows this to status-evidence artifacts and excludes untracked local r*-round residue. The current worktree has many untracked r1-r15 artifacts without role tags, and scanning the whole `scripts/review/results` directory fails on old transcripts. The plan should make the artifact map match the pre-label rule: tracked/cited `status_evidence` or `public_history` only, with local-transient residue explicitly outside the scan target.

## Blockers
- Finding 1: define a public-safe placeholder/fixture grammar for required private ledger fields, or restrict canonical good fixtures to schema-only content with separate in-memory row tests.
- Finding 2: add or name a concrete legal/security fallback command and required deny-list source, or add the missing repo-local legal scan artifact before implementation closeout depends on it.
