I've verified the plan's factual claims exhaustively against the actual sources. Nearly every retrieval claim is accurate. My adversarial diligence dissolved two candidate findings (the retained review artifacts do *not* trip the real parent scanner — bare `os.walk`/`.rglob` without a paren don't match `\.rglob\s*\(`), but surfaced a genuine contradiction in the accept-path grammar. Here's my review.

`★ Insight ─────────────────────────────────────`
- The parent scanner's denied patterns (`scripts/validate_ace_epic_wave_coordination.py:146-159`) are deliberately *syntactic*: `\.rglob\s*\(` requires a literal open-paren, so prose that mentions `.rglob` in backticks passes. That is why the whole plan can describe denied classes without self-blocking — but it also means safety rests on an exact-form regex, not on semantics.
- The most dangerous place to leave ambiguity in a firewall is the *accept* path, not the deny path. A deny-path gap over-blocks (annoying, safe); an accept-path gap can silently authorize. That's the lens I used to weight the snapshot-evidence finding as the blocker.
`─────────────────────────────────────────────────`

## Verdict
MAJOR

## Retrieval
- Read the full plan `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md` (lines 1-587).
- Verified file existence for all Artifact-Map paths: confirmed `artifacts/ace-wave0-ledger-schema.json`, `scripts/validate_ace_wave0_schema_contract.py`, `scripts/validate_ace_epic_wave_coordination.py`, `docs/plans/README.md`, `docs/plans/ace-share-ingestion-wave-coordination.md`, `.github/workflows/validate.yml`, `.planning/plan-approved/65.md`, `skills/validate_skill.py`, and all six SKILL.md docs exist; confirmed `config/`, `scripts/ace_bounded_sampling_firewall.py`, `scripts/validate_ace_bounded_sampling_firewall.py`, and `scripts/legal/legal-sanity-scan.sh` are absent (as the plan states).
- `docs/plans/ace-share-ingestion-wave-coordination.md:16-22` — confirmed the six manifest keys and caps (200 rows / 25 files / 1048576 bytes) match the plan exactly.
- `gh issue view 67` — confirmed live issue body: method issues `#1, #12`; skill set `content-triage-and-exclusion, source-extraction-coverage, format-coverage-ledger, public-private-routing, adversarial-verify-loop` (includes `source-extraction-coverage`, omits `page-shape-contract`) — matches the plan's reconciliation.
- `artifacts/ace-wave0-ledger-schema.json:12-29` — confirmed `route_targets`, `logical_target_stores`, and `route_store_matrix` all contain `metadata_only` / `metadata_ledger_store` and the exact pairing; the plan's rejected-value list matches the other three routes/stores.
- `artifacts/ace-wave0-ledger-schema.json:156-269` — confirmed `method_issue_bindings=[1,12]`, `bound_skill_groups` (exactly the five the plan claims), and `canonical_wave_registry` per-issue `requires_manifest_snapshot_id` values for #51-#63.
- `scripts/validate_ace_epic_wave_coordination.py:145-159, 775-799` — confirmed `--scan-public-path` argument exists and is list-valued; read the full `DENIED_TRAVERSAL_PATTERNS` and `MANIFEST_PATH_PATTERN`.
- Grepped all 36 retained `plan-67` review artifacts against the *actual* denied regexes; confirmed none trip the real scanner (the `os.walk`/`.rglob` mentions in `...-claude-r5.md:22` lack the required `(`).

## Findings

1. **[MAJOR] The one committed positive-evidence fixture has a self-contradictory grammar.** §Snapshot Evidence Shape line 234 states `reason_code` is "exactly `MISSING_62_EVIDENCE_CONTRACT`" with **no `evidence_mode` condition**, so read literally it applies to *every* `snapshot_evidence` object — including `evidence_mode=shape_only_fixture`. But lines 226/230 define `shape_only_fixture` as positive parser coverage, and the committed `downstream-shape-only-request.json` (line 247) specifies only `evidence_mode=shape_only_fixture` + `source_issue=62` and never mentions `reason_code`. So the plan simultaneously (a) requires a "blocked" reason code on the positive shape fixture and (b) omits it from that fixture's spec. Likewise `blocked_by_issue`/`follow_on_issue` (lines 232-233) are conditioned only on `blocked_pending_62_contract` and the plan never says whether they are *forbidden* on `shape_only_fixture`. An implementer cannot deterministically decide the required field set for the sole committed fixture that carries evidence — and this is on the firewall's *accept* path, where under-specification risks a false-allow. Fix: state, per `evidence_mode`, exactly which of `reason_code`/`blocked_by_issue`/`follow_on_issue` are required, optional, or forbidden.

2. **[MINOR] The committed fixture name `downstream-shape-only-request.json` contradicts its own contents.** §Fixture Payload Contract line 247 gives it `target_issue=67`, `request_class=metadata_only_fixture`, `fixture_scope=firewall_validator_self_check` — it is a #67 self-check fixture, not a downstream request. The genuine downstream case is the runtime-only "Runtime downstream blocked fixture" (line 248). An implementer reading the filename could build an actual `downstream_manifest_backed_sampling` fixture, which would (correctly) fail-closed and contradict the "Passes" expected result. Rename (e.g. `metadata-fixture-with-shape-only-evidence.json`) or annotate.

3. **[MINOR] Over-scope is flagged but left unresolved.** The Open Question (line 580) concedes formal review "may require another split if the executable-context classifier and sampling grammar are still too much for one approval unit." The unit bundles a ~25-field closed contract, a six-context classifier with runtime fragment-assembly machinery, a validator, the #62 evidence gate, CI self-scan-manifest derivation, and split-registry edits (44 TDD tests), and has cycled MAJOR across nine rounds. Deferring the split decision into implementation is a real convergence risk; resolve whether the classifier separates from the sampling grammar before requesting approval.

4. **[MINOR] Skill-validation command uses an inconsistent invocation form.** Acceptance criterion line 515 is `uv run skills/validate_skill.py`, while the adjacent criteria (lines 511-514) all use `uv run python <path>`. This exact inconsistency was flagged in the recorded Claude r7/r8/r9 reviews (§Adversarial Review Summary) and still persists. Standardize to `uv run python skills/validate_skill.py` so the command is verified, not guessed.

5. **[MINOR] Drift test lacks a type contract for imported scalars.** §Proposed Contract Shape says `owner_issue` "exactly `67`" and `method_issue_bindings` "exactly #1 and #12," but `artifacts/ace-wave0-ledger-schema.json:156-159` stores these as JSON **integers** (`[1, 12]`, `"owner_issue": 65`). `test_contract_imports_65_schema_terms` (line 430) compares #67 against #65; if #67 emits strings (`"67"`, `["#1","#12"]`) the equality/drift assertion becomes brittle or vacuous. Specify integer typing.

6. **[MINOR] Self-reported verdict is stale for the round meant to clear it.** The header (line 10) and §Adversarial Review Summary "Overall result: MAJOR ... not ready for `status:plan-review`" (line 570) still assert MAJOR. The r9 Codex blockers (request-class pass/fail column, per-source `INDEX.md` raw-read coverage, token-family-granular `denied_token_matrix`) *are* present in the current text, so carrying a self-asserted "Overall result: MAJOR" into the clearing round is misleading governance state. Update the summary to reflect the patched-for-re-review status rather than a stale terminal verdict.

## Blockers
- **Finding 1** — the `shape_only_fixture` vs `reason_code`/`blocked_by_issue`/`follow_on_issue` contradiction must be resolved before implementation, because it leaves the firewall's only committed accept-path fixture grammar undefined. All other findings are MINOR and do not individually block.

(Reviewed against: file/path existence, manifest-key and cap authority, #65 route/store/skill/wave-registry vocabulary, live issue-body bindings, parent-scanner argument and denied-pattern reality, and retained-artifact self-scan safety. The plan's *factual* retrieval is accurate throughout; the blocker is an internal grammar contradiction, not a bad citation.)
