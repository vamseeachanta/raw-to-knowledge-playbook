# Disagreement report — plan #67 (2026-06-30)

## Verdicts

| Provider | Verdict |
|---|---|
| claude | MAJOR |
| codex | MAJOR |
| gemini | UNAVAILABLE (gemini CLI failed, rc=1: Warning: Basic terminal detected (TERM=dumb). Visual rendering will be limited. For the best experience, use a terminal emulator with truecolor support. Warning: 256-color support not detected. Using a terminal with at least 256-color support is recommended for a better visual experience. Error authenticating: IneligibleTierError: This client is no longer supported for Gemini Code Assist for indiv) |

## Findings unique to each provider

A finding is 'unique to X' if its text appears in X's artifact but not
verbatim in any other provider's artifact.

### claude

- **`requires_manifest_snapshot_id` has no `#65` source of truth for the only passing fixture (`target_issue=67`) — self-contradictory core invariant.** The contract makes `requires_manifest_snapshot_id` a *universally required* top-level key on every request (Proposed Contract Shape, line 178: "`required_sampling_fields`... `requires_manifest_snapshot_id`..."), and pseudocode line 354 asserts it "is present as boolean **from #65**," with `requires_manifest_snapshot_id_gate` (line 184) declared "imported from the #65 canonical wave registry and used as the **single source of truth**." But the accepted self-check fixture has `target_issue=67`, and I verified `#67` is **not** in `canonical_wave_registry` (`artifacts/ace-wave0-ledger-schema.json:178-269`, issues 51–63 only) — it exists only in `wave0_split_registry`, which carries no `requires_manifest_snapshot_id` field. So for `good-request.json` the validator has no `#65` entry to source or check this required boolean against, and the Fixture Payload Contract (line 256) never states its value. The plan simultaneously claims (a) the field is required on every request, (b) its value comes from `#65`, and (c) `#65` is the single source of truth — yet the one request that must pass targets an issue `#65` doesn't cover. An implementer cannot deterministically build the passing fixture or the gate check without diverging from the stated invariant. **This is the blocker.**
- **`snapshot_evidence` object-level required/optional condition is under-specified where the plan says it is defined.** Line 178 states `snapshot_evidence` "is required only when the evidence-mode table below says so," but the evidence-mode table (lines 247–251) only enumerates fields *within* `snapshot_evidence` per `evidence_mode` — it never states when the `snapshot_evidence` object itself is required at the request level. The actual object-level rule lives elsewhere (pseudocode line 378: "downstream_manifest_backed_sampling requires a complete snapshot_evidence object"). The cross-reference on line 178 points at a table that does not answer the question it promises. MINOR (imprecise pointer, resolvable), but it compounds Finding 1's ambiguity about what a `metadata_only_fixture` with `target_issue=67` must carry.
- **Acceptance criterion #1 is a self-referential review-gate, not a code-verifiable criterion.** Line 524: "the latest same-round adversarial review has at least two usable provider results and no usable provider returns MAJOR." This criterion is satisfied or falsified *by the very review that is checking it* (this one). It restates the Plan Review Gate Rule (line 574) as an acceptance item, so acceptance of the implementation is gated on a process outcome external to the code. It is not wrong as a lifecycle rule, but as an acceptance criterion it is circular and cannot be discharged by inspecting the delivered artifacts. MINOR.
- **The firewall accepts exactly one input shape and authorizes zero operational sampling in this unit — verify this is the intended, reviewed scope, not an accident of the fail-closed cascade.** Deliverable (line 162) and Request Class Mapping (line 213) confirm only the `#67` `metadata_only_fixture` row passes; `#51/#61/#62/#63` control-plane rows and `#52–#60` downstream rows all fail closed, and `operational_live` is reserved for `#70`. This is explicitly by design and acknowledged, so it is not a defect — but it means the entire T3 deliverable's positive-path test surface is a single synthetic fixture (Finding 1's fixture). That concentrates all correctness risk on the one path that Finding 1 shows is under-specified. Not an independent blocker; flagged so the scope narrowness is a conscious sign-off. (Reviewed against: is any real allow-path claimed? No — line 162 defers the first operational allow-path to `#70`.)
- Checks run that produced **no** finding (silence-is-failure disclosure): manifest source keys match the ledger exactly; caps match the ledger exactly; `bound_skill_groups` vs live-issue skill set reconciliation (union of six, `source-extraction-coverage` excluded from `#65` enum) is correct against both the schema file and the live issue; method bindings `[1,12]` correct; wave classes in the mapping table all exist in `#65`; all cited file paths resolve as claimed (MISSING/EXISTS states all accurate, including the legal-scan absence); review-artifact inventory (r1–r10) matches disk with no un-inventoried rN files; no `#67` approval marker exists (matches the "implementation stays blocked" posture); the `page-shape-contract`-vs-`source-extraction-coverage` swap between `#65` and the live issue is correctly reconciled.

### codex

- Plan `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md:468` says only the base sampling keys plus exactly one of `target_wave_class`/`fixture_scope` are accepted and unknown keys fail. That excludes `snapshot_evidence`, but the same plan allows/requires `snapshot_evidence` for downstream requests and shape-only metadata fixtures at lines 178, 231, and 257. The TDD contract would reject the planned evidence fixture or force an implementation exception not represented in the exact-key test.
- Plan `docs/plans/2026-06-30-issue-67-ace-wave-0-bounded-sampling-firewall.md:286` says JSON fixtures are executable when request fields carry command, query, source root, manifest operation, or sampling expression. The accepted request key set at lines 178 and 468 defines no such fields, and `test_json_request_command_context_is_executable` at line 501 only says “runtime-built JSON request.” A test can pass by rejecting an unknown key instead of proving the JSON executable-context classifier actually catches denied command content.
- The “Concrete Denied Token Matrix” is still not concrete enough for approval. Plan lines 304-313 list broad families such as “raw file reader family,” “JSON/query tool family,” and “digest/fingerprint family,” while the existing parent scanner has explicit denial patterns for specific shell/Python/query tokens in `scripts/validate_ace_epic_wave_coordination.py:146-158`. Without enumerating the #67 token fragments in the plan or a referenced contract draft, reviewers cannot verify coverage before implementation.

### gemini

- (none)
