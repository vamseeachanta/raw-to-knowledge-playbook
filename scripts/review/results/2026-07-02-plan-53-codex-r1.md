Verdict: MAJOR

Findings:
- [MAJOR] `.xls` ingestion is overclaimed without a concrete parser or test path. The plan assigns `.xls` “60-85%” useful ingestion in `docs/plans/2026-06-29-issue-53-ace-wave-2-spreadsheets-csv-calculation-workbook-ingestion-lane.md:47`, but the live helper is `.xlsx`-centered: `skills/xlsx-input-code-output-canary/resources/xlsx_canary.py:209-217` calls `openpyxl.load_workbook()` before calamine is only used for sheet names, and the canary fixture IDs are all `*-xlsx` at `skills/xlsx-input-code-output-canary/resources/xlsx_canary.py:38-49`. The TDD list has no legacy `.xls` case. Fix: either downgrade `.xls` to metadata-only/excluded until an adapter exists, or add a real `.xls` adapter plus parser/version-loss tests and fixtures.

- [MAJOR] The artifact map creates a path for private measured ACE output before the #61 durable-output gate. `docs/plans/2026-06-29-issue-53-ace-wave-2-spreadsheets-csv-calculation-workbook-ingestion-lane.md:100` says “Private measured ACE sidecar” is deferred only to approved implementation and private/off-public route. That conflicts with `docs/plans/README.md:17` and `docs/plans/ace-share-ingestion-wave-coordination.md:73`, which block target paths, durable stores, lifecycle state, and persistent metrics until #61 is approved and implemented. Fix: explicitly defer any measured private sidecar, target path, lifecycle field, persistent metric, or private store write until #61 has `status:plan-approved`, approval marker, implemented validator, and passing-command evidence.

- [MINOR] Fixture wording risks committing raw workbook bytes. `docs/plans/2026-06-29-issue-53-ace-wave-2-spreadsheets-csv-calculation-workbook-ingestion-lane.md:158` says to create “synthetic workbook” fixtures, while `skills/xlsx-input-code-output-canary/SKILL.md:32` and `:87` forbid repo-local raw workbook bytes. Fix: state fixtures are JSON inventories or runtime-generated temp workbooks only, and add a test that no `.xls/.xlsx/.xlsm/.xlsb` bytes are committed.

- [MINOR] #67 cap values are not pinned in the #53 plan. The plan says “per-bucket/file/byte caps” at `docs/plans/2026-06-29-issue-53-ace-wave-2-spreadsheets-csv-calculation-workbook-ingestion-lane.md:123` and has only an over-cap test at `:188`; the implemented #67 contract requires 200 rows, 25 files, and 1048576 bytes in `scripts/ace_bounded_sampling_firewall.py:107-112`. Fix: add exact boundary tests for those values and require importing the #67 contract rather than duplicating caps.

- [MINOR] The planned scan command is not currently executable as written. `docs/plans/2026-06-29-issue-53-ace-wave-2-spreadsheets-csv-calculation-workbook-ingestion-lane.md:224-237` scans Codex/Gemini r1 artifacts that do not exist, and the existing Claude r1 artifact is 0 bytes. Fix: update the command list only after final review artifacts exist, or make the command explicitly reference the final materialized artifact set.

Checked:
- Read the target plan and all named supporting files.
- Verified live issue labels for #50, #51, #53, #61, #62, #63, #70, and #71 with `gh issue view`.
- Verified `artifacts/ace-manifest-freshness/trusted-evidence-registry.json` has empty `trusted_evidence`.
- Ran `uv run python scripts/validate_ace_epic_wave_coordination.py`: PASS.
- Ran `uv run python scripts/validate_ace_wave0_schema_contract.py`: PASS.
- Ran the plan’s public-surface scan command: FAIL, missing Codex/Gemini r1 artifacts.
- Ran the plan’s legal scan command: FAIL, missing Codex r1 artifact.
- Ran workbook canary `self-test`, `manifest-check`, and `skills/validate_skill.py --strict`: PASS.