# Synthetic Method Note

This fixture represents hand-authored Markdown with useful ingestion guidance.
It explains a bounded sampling decision, records a parse status, and keeps
public-facing tokens abstract.
