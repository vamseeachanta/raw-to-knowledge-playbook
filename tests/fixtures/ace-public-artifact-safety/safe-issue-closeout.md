ACE public-output canary closeout fixture.

This body contains public methodology evidence only. It references the issue 63
validator, public-safe docs, and synthetic fixtures without host paths, personal
identifier shapes, secrets, raw digest values, private lookup material, or copied
private snippets.
